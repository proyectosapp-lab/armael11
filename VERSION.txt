Arma el 11 - version 2026-09-20 y64 (un arreglo subido tiene que verse hoy)

  · SE SUBEN TRES ARCHIVOS: frescura.mjs, publicar.mjs, probar-frescura.mjs.
    Ninguno empieza con punto, o sea que los tres se suben arrastrando.

  · QUE ARREGLA. El 20/9, un sabado, Atletico-Real Madrid salio en la app con
    los dos equipos en 4-3-3. El arreglo del dibujo estaba escrito, probado y
    subido desde la noche anterior: ligas-api.mjs ya calcula el dibujo
    habitual de cada equipo y juego.js ya traduce "Attacker" a delantero.

    No servia de nada. El sello de `ligas` dura 24 horas. El paso se salteaba
    con toda prolijidad y el archivo publicado seguia siendo el que habia
    hecho el codigo VIEJO.

    El sello contestaba "esto se bajo hace poco". La pregunta que faltaba es
    "¿y lo bajo ESTE codigo?".

  · COMO. Cada paso caro lleva ahora, ademas de la hora, una FIRMA: un hash
    de los archivos que lo producen. Si cambio cualquiera de ellos, lo
    guardado lo hizo otro programa y no cuenta como fresco.

      ligas    ligas-api.mjs, juego.js, fecha-de-liga.mjs, ligas.json
      juego    datos-juego.mjs, juego.js, formaciones.mjs, clubes.json
      tabla    stats-api.mjs
      fantasy  fantasy-api.mjs, fantasy.mjs, fases.mjs, fases-reglas.mjs
      puntos   puntos-api.mjs, fantasy.mjs

    La lista se elige a mano y no siguiendo los `import`: seguir los imports
    meteria frescura.mjs en todos los pasos, y entonces tocar un comentario
    costaria una corrida completa.

  · LO QUE CUESTA. Una corrida completa cada vez que se toca uno de esos
    archivos, y solo esa vez. Es exactamente el precio que uno quiere pagar:
    hoy son 1.200 pedidos de los 7.500 del dia.

  · AL SUBIR ESTE ZIP pasa eso mismo, una vez: no hay ninguna firma guardada
    todavia, asi que la primera corrida rehace los pasos caros. Es lo que
    hace falta para que los dibujos aparezcan hoy y no manana. NO hace falta
    apretar "Run workflow".

  · LO QUE NO ARREGLA. El cartel de "Formaciones tentativas" es otra cosa y
    sigue igual: dice que todavia no hay once del DT publicado para ese
    partido. Eso lo trae la ronda corta, cada 15 minutos, desde 120 minutos
    antes del partido, y lo escribe en sitio/datos/onces-<liga>.js. Si el
    cartel sigue apareciendo con el partido encima, hay que mirar el log de
    la ronda corta en Actions: imprime un renglon por liga con cuantos
    partidos vio en ventana y cuantos onces encontro.

  · PROBADO. probar-frescura.mjs pasa de 22 a 34 casos. Los nuevos cubren que
    la firma no dependa del orden, que un archivo que falta tambien cuente
    como cambio, que un sello fresco con otra firma se rehaga, y —el que mas
    importa— que los archivos nombrados en las listas EXISTAN: una ruta mal
    escrita ahi se firma como "falta", la firma nunca coincide, y el paso mas
    caro del dia se reharia en CADA corrida. Serian 1.200 pedidos cada quince
    minutos y la cuota se acabaria en una hora.

Arma el 11 - version 2026-09-20 y63 (el minimo de iOS)

  · SE SUBE ESTE SOLO. Cambia UN archivo: codemagic.yaml.

  · PRIMERO LO QUE IMPORTA: LA COMPILACION SALIO. "Although delivery was
    successful": Arma el 11 version 1.0, build 10, entro a App Store Connect.
    Compilo, firmo y subio. Diez compilaciones.

    Lo que faltaba, en orden, y cada una escondida detras de la anterior:
      1. La clave de API con rol App Manager, que no llega a Certificates,
         Identifiers & Profiles. Tenia que ser Admin.
      2. El certificado, que no existia.
      3. La clave privada del certificado, que la Mac no puede inventar en
         cada corrida.
      4. El perfil de aprovisionamiento, que es otra cosa que el certificado
         y vive en otra pantalla.

    Las cuatro daban el MISMO mensaje: "No matching profiles found". Por eso
    la unica decision que de verdad destrabo esto fue sacar la firma
    automatica un rato para que el error quedara escrito en un log.

  · LO QUE AVISO APPLE, Y NO ES UN RECHAZO:

      ITMS-90068: MinimumOSVersion too low. This app has a MinimumOSVersion
      of 14.0. Starting in Spring 2027, all iOS apps must have a
      MinimumOSVersion of 15.0 or later.

    La subida entro igual. Es una fecha con nombre y apellido, y arreglarlo
    ahora cuesta dos lineas.

  · EL ARREGLO. Capacitor genera el proyecto con iOS 14.0. Se sube a 15.0 en
    el proyecto de Xcode Y en el Podfile, y va ANTES de `cap sync` -que es el
    que corre `pod install`-: los dos tienen que decir lo mismo antes de que
    CocoaPods resuelva nada, o las dependencias quedan compiladas para una
    version y la app para otra.

    15.0 y no mas. Es lo que Apple pide y no deja afuera a nadie que
    importe: un iPhone que no llega a iOS 15 es un 6 o anterior, de 2014, que
    ademas no podria con la cancha.

    Y el paso imprime que quedo puesto, en las dos partes. Esa es la leccion
    de la noche: lo que no se imprime, no se sabe.

  · LO QUE SIGUE, QUE NO ES CODIGO: la build 10 tiene que aparecer en App
    Store Connect -> TestFlight. Ahi se instala en un iPhone de verdad y se
    prueba. Recien despues se aprieta a mano el boton de mandar a revision:
    `submit_to_app_store` sigue en false a proposito.

Arma el 11 - version 2026-09-20 y62 (se nombra lo que hay)

  · SE SUBE ESTE SOLO. Cambia UN archivo: codemagic.yaml.

  · ANTES DE SUBIRLO HAY QUE CREAR EL PERFIL. Ver abajo. Sin eso, este zip
    falla igual pero con un error distinto -y ese error SI dice cual de las
    dos piezas falta, que es la mejora-.

  · LA NOVENA COMPILACION VOLVIO A DECIR "No matching profiles found" CON EL
    CERTIFICADO YA CREADO. Y eso cierra el diagnostico: el certificado era
    la MITAD. Firmar necesita dos cosas, y Codemagic las guarda en dos
    cajones distintos de la misma pantalla:

      iOS certificates          -> `armael11`, production, vence 19/9/2027
      iOS provisioning profiles -> VACIO

    La firma automatica estaba buscando un perfil en un cajon vacio.

  · QUE SON LAS DOS COSAS, PORQUE CUESTA:
      El CERTIFICADO dice "esta persona puede firmar apps". Es la identidad.
      El PERFIL dice "esta app, firmada con ese certificado, puede
      instalarse en tal lado". Es el permiso, y es por app.
    Tener uno sin el otro no sirve para nada, y por eso el mensaje generico
    de Codemagic -"no encuentro perfiles"- aparecio tres veces por tres
    motivos distintos.

  · EL CAMBIO: SE NOMBRAN LAS DOS PIEZAS EN VEZ DE PEDIR QUE LAS BUSQUE.

      ios_signing:
        certificates:
          - armael11
        provisioning_profiles:
          - armael11

    Antes decia `distribution_type` + `bundle_identifier`, que es pedirle a
    Codemagic que salga a buscar o crear las dos cosas por su cuenta. Ahora
    estan guardadas, tienen nombre, y se las nombra. Es la forma mas aburrida
    y la unica que no depende de que algo le pregunte a Apple en el momento
    equivocado.

    Y si alguna se renombra, la compilacion falla diciendo CUAL falta, en vez
    del mensaje generico de siempre.

  · LO QUE HAY QUE HACER EN EL NAVEGADOR, UNA VEZ

    En developer.apple.com -> Certificates, Identifiers & Profiles ->
    Profiles -> el mas (+):

      Tipo:        App Store Connect  (en Distribution, NO Development)
      App ID:      com.armael11.app
      Certificado: el que dice "Fausto Manuel Vergara", vence 19/9/2027
      Nombre:      armael11

    Se descarga un archivo `.mobileprovision`.

    Despues, en Codemagic -> Settings -> Code signing identities ->
    pestana "iOS provisioning profiles" -> subir ese archivo con
    Reference name `armael11`, exactamente igual que el certificado.

    Si esa pestana tiene un boton para traerlos de Apple, tambien sirve:
    con el perfil ya creado, lo encuentra. Lo que importa es que quede
    listado con ese nombre.

Arma el 11 - version 2026-09-20 y61 (la firma, con el renglon que faltaba)

  · SE SUBE ESTE SOLO. Cambia UN archivo: codemagic.yaml.

  · EL RENGLON QUE APARECIO, despues de mover la firma a un paso propio:

      Cannot save Signing Certificates without certificate private key

    Eso es todo lo que hacia falta y es muy concreto. `fetch-signing-files`
    pidio el certificado a Apple y lo consiguio, pero para GUARDARLO en el
    llavero necesita la CLAVE PRIVADA que le corresponde. Un certificado sin
    su clave privada no firma nada: es la mitad de un par.

    Y esa clave privada NO la puede inventar la Mac en cada corrida. Si lo
    hiciera, cada compilacion crearia un certificado nuevo, y Apple deja
    tener dos certificados de distribucion. En tres compilaciones se acaban.

  · EL ARREGLO: VUELVE LA FIRMA AUTOMATICA. Manejar esa clave privada entre
    corridas -generarla una vez, guardarla, reusarla- es EXACTAMENTE lo que
    hace el bloque `ios_signing` de Codemagic. Por eso vuelve.

  · Y NO, NO ES DAR VUELTAS. La historia completa, que queda escrita adentro
    del propio archivo:

      1. El bloque estaba, y las compilaciones 1 y 2 murieron con "No
         matching profiles found" ANTES de correr un solo paso. Sin log.
      2. Se saco y se pidio la firma a mano, para que el error quedara
         escrito. Funciono: la compilacion 6 dijo con todas las letras que
         faltaba la clave privada.
      3. Con eso sabido, la firma automatica es la respuesta.

    Y lo que hacia fallar a la 1 y a la 2 ya no esta: la clave de API tenia
    rol App Manager, que no llega a Certificates, Identifiers & Profiles.
    Ahora tiene Admin. El paso a mano no fue una vuelta al pescuezo: fue el
    que consiguio el renglon que decia que arreglar.

  · LO QUE QUEDA DEL PASO A MANO: `xcode-project use-profiles`, que mete el
    perfil adentro del proyecto de Xcode recien generado, y un `ls` de los
    perfiles disponibles. Ese `ls` es a proposito: si algo vuelve a fallar
    ahi, que haya un renglon que leer en vez de una adivinanza. La leccion
    de las compilaciones 1 y 2 se queda aunque el codigo vuelva atras.

  · SI VUELVE A FALLAR, EL PLAN B ESTA PENSADO: Codemagic puede generar la
    clave privada del certificado desde su propia pantalla, sin terminal
    (Settings -> Code signing identities -> iOS certificates). No hace falta
    generarla a mano ni que pase por ningun lado.

  · Y ALGO PARA MIRAR SI APARECE UN ERROR DE LIMITE: la compilacion 6 pudo
    haber creado un certificado de distribucion en la cuenta de Apple del
    que nadie tiene la clave privada. Es un certificado huerfano y ocupa uno
    de los dos lugares. Si en algun momento Apple dice que no se pueden
    crear mas, es ese, y se revoca en developer.apple.com.

Arma el 11 - version 2026-09-20 y60 (fuera el marcador mas probable)

  · SE SUBE ESTE SOLO. Incluye y59.

  · FAUSTO: "deja de mostrar el resultado mas repetido. Genera confusion y
    por mera estadistica siempre el 1 a 1 se va a repetir mas que la
    mayoria. Una victoria puede ser 1 a 0, 2 a 0, 2 a 1, etc., y las
    categorias de empates son mas reducidas, por eso el resultado se repite
    mas".

    Tiene razon y el argumento es exacto. Es la trampa clasica de la MODA
    contra la SUMA. El marcador mas repetido de 6.000 partidos casi siempre
    es un empate o un 1-0, no porque el modelo crea que va a terminar asi
    sino porque los empates concentran su probabilidad en pocos marcadores
    -1-1, 0-0, 2-2- mientras que ganar la reparte entre muchos. Sumados, los
    marcadores de victoria pueden valer 55% y aun asi el mas repetido de
    todos ser un 1-1 con 9%.

    O sea que se podia leer "el modelo dice 1-1" arriba de una barra que
    decia 55% de ganar, y las dos cosas eran ciertas al mismo tiempo. Un
    numero que hay que explicar para que no confunda es un numero que no va.

  · QUE SE SACO, EXACTAMENTE

    - El tanteador grande ANTES de ver el partido. Ahora, hasta que no toques
      "Ver uno de los 6.000 partidos posibles", no hay ningun marcador en
      pantalla: esta la barra, el veredicto y el xG.
    - El renglon "el mas probable era 1 - 1 (9%)" que aparecia DEBAJO del
      partido que viste.
    - La fila "Mas probable" de las dos tablas de comparacion (tu once
      contra el del DT, y la que agrega lo que paso). Comparar dos modas
      entre si es todavia peor que mostrar una sola.

  · QUE QUEDA, Y POR QUE

    - LA BARRA. Es la SUMA de todos los marcadores de cada clase, que es
      justo lo que la moda no es.
    - EL PARTIDO SORTEADO, cuando lo pedis. Ese se presenta por lo que es:
      "uno de los 6.000 posibles", y debajo dice cada cuanto sale uno asi.
    - EL xG, que no depende de ningun marcador.

  · LO QUE NO SE TOCO: el calculo. `simular()` sigue devolviendo `marcador` y
    `probMarcador`, y tiene que seguir haciendolo: la rareza del partido que
    se sortea se mide contra ellos, y la poda de marcadores insolitos
    tambien. Se saco de la PANTALLA, no del modelo. Sacarlo del modelo
    hubiera sido romper otra cosa para arreglar esta.

  · LA PRUEBA QUE MAS IMPORTA, REESCRITA. En `probar-sitio.cjs` habia un
    bloque llamado "NO ES UN DADO", que nacio del video de un tester tocando
    Simular doce veces y sacando doce resultados distintos. Su primer caso
    era "sin ver el partido, el numero grande es el MAS PROBABLE", y ese
    caso ahora dice lo contrario: "sin ver el partido NO SE MUESTRA NINGUN
    MARCADOR", mas otro que exige que el texto tampoco lo nombre.

    El resto del bloque no cambia y es el que de verdad sostiene la promesa:
    volver a simular sin cambiar nada no gasta ni cambia, cambiar una
    perilla si simula de nuevo, y volver exactamente a como estaba devuelve
    exactamente los mismos numeros. 328 casos en el navegador (eran 327).

  · PENDIENTE PARA LA FICHA DE LA APP STORE: la descripcion dice "cuanto
    ganas, cuanto empatas, cuanto perdes y cual es el marcador mas
    probable". Esa ultima frase hay que sacarla antes de enviar. Queda
    corregida en `ficha-app-store.md`.

Arma el 11 - version 2026-09-20 y59 (el bug de los delanteros, y el dibujo de cada equipo)

  · SE SUBE ESTE SOLO. Es el mas importante de los ultimos cinco zips: esto
    estaba ROTO Y PUBLICADO, y lo ven los testers.

  · FAUSTO: "no esta actualizando las alineaciones, todos los equipos salen
    4 3 3 y acomoda los jugadores en cualquier lado".

    Son DOS bugs distintos que se veian como uno, los dos en las otras diez
    ligas. En las paginas de los clubes argentinos no pasaba, y por eso no
    se habia notado antes.

  · BUG 1: TODOS LOS DELANTEROS SIN PUESTO. API-Football contesta el puesto
    en DOS formatos segun a que le preguntes:

      /fixtures/players -> games.position = "G" | "D" | "M" | "F"
      /players/squads   -> position       = "Goalkeeper" | "Defender" |
                                            "Midfielder" | "Attacker"

    `ligas-api.mjs` tomaba la PRIMERA LETRA del segundo formato. Funciona
    para tres de los cuatro: G, D, M... y **A**. "Attacker" empieza con A y
    la categoria del juego es **F**.

    Resultado: todos los delanteros de las otras diez ligas quedaban con
    puesto "A", que no existe en ninguna formacion. `autoXI` los buscaba
    entre los "F" y no encontraba ninguno, asi que caian en la rama de
    improvisar y salian repartidos por toda la cancha. Eso es literalmente
    "acomoda los jugadores en cualquier lado".

    Y hay una segunda mitad del dano, peor y menos visible:
    `penalPuesto("A","F")` no encuentra esa combinacion y devuelve el 0,5
    por defecto. O sea que un delantero PAGABA CASTIGO POR JUGAR DE
    DELANTERO. No ensuciaba solo el dibujo: ensuciaba la simulacion, que es
    el producto entero.

    La tabla estaba BIEN en `app.tpl.html` y MAL en `ligas-api.mjs`. Tenerla
    dos veces fue el bug. Ahora hay una sola, `puestoDe` en `juego.js`, que
    es el archivo que ven las dos puntas: el navegador y los scripts que
    bajan los datos.

  · Y SE CURA SOLO, HOY. Los archivos de liga que estan publicados AHORA
    tienen la "A" adentro. `puestoDe` tambien la traduce, y `poolDeLiga` la
    llama al leer. O sea que con subir este zip se arregla, sin esperar las
    24 horas que tarda en rehacerse un archivo de liga.

  · BUG 2: 4-3-3 PARA TODOS. `simularDeLiga` tenia
    `J.formA = FORMA_DE_RESPALDO` clavado para los dos lados, con un
    comentario que decia que el archivo de liga no traia el dibujo. Era
    cierto y era arreglable.

    Es exactamente la misma queja que los testers hicieron en agosto -"toma
    todas las formaciones como 4-3-3, no las adapta al equipo"-, que
    arreglamos para los clubes argentinos y que seguia rota para las otras
    diez ligas. Es una de las tres cosas que le dijimos a Google que estaban
    corregidas.

    El dato NO CUESTA UN PEDIDO MAS: cada `/fixtures/players` que
    `ligas-api.mjs` ya baja para los ratings dice, por jugador, si fue
    titular y en que categoria jugo. Contar los titulares da el dibujo de
    ese partido; la moda de los ultimos cinco da el habitual. Es la misma
    funcion que usa la pagina de un club.

    Ahora el archivo de liga trae `f` por equipo y la pantalla lo usa. Si un
    equipo no tiene suficientes partidos, `formacionHabitual` devuelve null
    y se usa el de respaldo SOLO para ese equipo: un null quiere decir "no
    se", no "4-3-3".

  · PRUEBAS. 93 en `probar-once.mjs` (eran 85) y 327 en el navegador (eran
    322). Las que importan:

    - '"Attacker" es F, NO "A"'. El caso exacto del bug.
    - "un delantero de delantero no paga castigo". La otra mitad del dano.
    - "las categorias del traductor son las de las formaciones": compara los
      valores de la tabla contra las categorias que salen de FORMS. Si
      alguien agrega una quinta categoria a una y no a la otra, se cae.
    - En el navegador, un archivo de liga con la letra "A" adentro A
      PROPOSITO -es lo que hay publicado ahora mismo- y se exige que el once
      salga entero y sin nadie fuera de puesto.
    - Que un dibujo raro en un archivo bajado ("9-9-9") NO llegue a
      `slotsDe`.

Arma el 11 - version 2026-09-19 y58 (la foto sale del sitio publicado)

  · SE SUBE ESTE SOLO. Cambia dos archivos: empaquetar-ios.mjs y su prueba.

  · LO QUE DIJO EL LOG DE LA COMPILACION 5:

      ⚠ el sitio venia CON publicidad: saque 2 script(s) de 2 pagina(s) y el ads.txt
      ✓ ni un byte de AdSense en el paquete (15 archivos mirados)

    Dos cosas, las dos importantes.

    La primera: EL ARREGLO DE LA PUBLICIDAD FUNCIONA. El sitio venia con
    ella y el paquete quedo limpio. Ese problema esta cerrado.

    La segunda: DOS paginas. QUINCE archivos. O sea que la compilacion
    armaba un sitio practicamente vacio, y ahi estaba el problema de fondo.

  · POR QUE PASABA. `construir-sitio.mjs` arma una pagina por club, pero
    solo de los clubes CUYOS DATOS ESTAN BAJADOS. Y esos datos -`sitio/datos`-
    no viven en el repositorio: viven en el cache del workflow de GitHub,
    que Codemagic no ve. La Mac de Codemagic no tiene con que armar el
    sitio, y no lo dice: arma lo que puede.

  · EL ARREGLO: LA FOTO SALE DE armael11.com. Ya no se arma el sitio en la
    Mac. Se BAJA el sitio publicado, siguiendo los enlaces de cada pagina:
    la portada, las paginas de club, los archivos de datos que cada una
    menciona, los manifiestos y los iconos que los manifiestos nombran.

    Arregla el problema y ademas es mas honesto. La foto es exactamente lo
    que ve un visitante en el momento de compilar: los partidos de la fecha,
    los onces que trajo la ronda corta, los textos que esten arriba. No es
    "una version del sitio armada de nuevo": es el sitio.

    Y de paso desaparece la dependencia con `SIN_PUBLICIDAD=1`, que en
    Codemagic no hacia efecto. El sitio publicado TIENE publicidad -esta
    bien, es un sitio web- y el empaquetado se la saca. Punto.

  · COMO SE PRUEBA ALGO QUE BAJA DE INTERNET SIN INTERNET. El descargador se
    pasa de afuera, igual que en `datos-ios.js`. En las pruebas el "sitio"
    son ocho cadenas de texto en un objeto, y se verifica que con UNA sola
    semilla -la portada- llegue a las ocho cosas siguiendo enlaces. El unico
    pedazo que no se puede probar aca es el `fetch` de una linea.

    11 casos en `probar-empaquetado-ios.mjs` (eran 5). Los que mas valen no
    son los que prueban que baje:

    - Que un 404 NO corte la bajada entera. Un club sin pagina todavia no
      puede convertir "falta un club" en "no hay app".
    - Que sin red devuelva vacio en vez de tirar.
    - Que no pida dos veces el mismo archivo.
    - Que mire los `url()` del CSS incrustado: ahi viven las fuentes, y sin
      eso la app quedaria sin tipografia y nadie se daria cuenta hasta
      verla en un iPhone.
    - Que el `?c=` y el `#ancla` se saquen: son la MISMA pagina, y sin eso
      se baja dos veces y se guarda con un nombre que el navegador nunca va
      a pedir.

Arma el 11 - version 2026-09-19 y57 (los nombres de los equipos, y la publicidad afuera de verdad)

  · SE SUBE ESTE SOLO.

  · LO QUE DIJO EL LOG, QUE ES LO IMPORTANTE. Con el guardia mostrando la
    prueba se vio lo que pasaba:

      s.src="https://pagead2.googlesyndication.com/..." client=ca-pub-2032...
      · ads.txt
          google.com, pub-2032639837955609, DIRECT, f08c47fec0942fa0

    O sea que el sitio se armo CON publicidad aunque se pidio sin. El
    `ads.txt` lo confirma: con SIN_PUBLICIDAD=1 ese archivo se BORRA, y ahi
    estaba. La variable no hizo efecto en la Mac de Codemagic.

  · EL ARREGLO NO ES AVERIGUAR POR QUE. Es no depender de eso. Ahora hay
    tres defensas en fila y no una:

      1. `SIN_PUBLICIDAD=1`: el script ni se escribe.
      2. NUEVO: despues de copiar, se SACA del paquete aunque este.
      3. Se comprueba el resultado y se termina con error si sobrevivio algo.

    Parecen la misma cosa tres veces y no lo son. La primera depende de que
    la version de `construir-sitio.mjs` de esa maquina entienda la variable
    —y el log demostro que eso no se puede dar por sentado—. La segunda no
    depende de nadie: mira el archivo que va a viajar y saca lo que hay.

  · COMO SE SACA, Y POR QUE NO CON UNA EXPRESION REGULAR. Se borra el
    <script> ENTERO que contenga el dominio, recorriendo etiqueta por
    etiqueta. La manera obvia de escribir esto es una expresion regular
    sobre el archivo entero, y esa expresion empieza en el primer `<script`
    y termina en el ULTIMO `</script>`: se come la app completa y deja una
    pagina en blanco que igual pasa cualquier comprobacion de "no hay
    AdSense". Seria la peor forma posible de aprobar la prueba.

    Por eso `probar-empaquetado-ios.mjs` (5 casos) prueba sobre todo lo que
    NO tiene que pasar: que sobrevivan el <script> de los datos, la funcion
    `simular`, el titulo, el principio y el final de la pagina.

  · LOS NOMBRES DE LOS EQUIPOS. Fausto: "la app sigue mostrando tu equipo y
    rival cuando queres simular un partido, pero eso se presta a confusion
    cuando queres simular un partido de la liga de Peru".

    Tiene razon, y la distincion es exacta. En la pagina de un club, A es el
    equipo del hincha y "tu / el rival" es la voz del producto: esta bien y
    es parte de lo que hace que la app se sienta de uno. En un partido de
    otra liga no hay ningun "vos": Alianza Lima no es tu equipo y Sporting
    Cristal no es el rival. Son dos ajenos, y el unico texto que no confunde
    es el nombre de cada uno.

    Cambian siete lugares, todos los que decian "tu" o "rival" sobre un
    EQUIPO: el titulo de cada tarjeta de planteo, las dos etiquetas de
    formacion, las dos opciones de expulsados y las dos de goles. Los
    nombres largos se recortan a 16 caracteres con puntos suspensivos:
    "Universitario de Deportes" entero parte la fila de dos columnas y
    desalinea el formulario.

    LO QUE NO CAMBIA, Y ES A PROPOSITO: "tu once" y "tu simulacion" siguen
    diciendo "tu", tambien en Peru. Ese once lo armaste vos de verdad.

  · Y EL GUARDIA DEL PAQUETE VACIO SIGUE AHI. La proxima compilacion va a
    fallar en el, a proposito: los datos no estan en el repositorio, estan
    en el cache del workflow de GitHub. Lo que imprima —cuantas paginas y
    cuantas ligas— es lo que confirma el diagnostico. La foto tiene que
    salir de armael11.com y eso viene en el zip siguiente.

Arma el 11 - version 2026-09-19 y56 (el guardia se planto bien, y mostraba mal)

  · SE SUBE ESTE SOLO. Cambia UN archivo: empaquetar-ios.mjs.

  · LO PRIMERO, QUE ES BUENO: la firma anda. La tercera compilacion paso
    "Preparing build machine", "Fetching app sources" y entro a correr los
    pasos. O sea que la clave con rol Admin y el identificador registrado
    eran las dos cosas que faltaban, y el paso de firma propio hizo lo suyo.

  · Y LO SEGUNDO: SE PLANTO MI PROPIO GUARDIA. El paso 1 corto con

      ME PLANTO. Quedo AdSense adentro de lo que iba a viajar en el .ipa:
        · index.html
        · talleres-cba.html

    Eso es exactamente lo que tenia que pasar. Pero el mensaje era inutil:
    decia QUE archivo y no QUE ENCONTRO. Desde un navegador, con eso no se
    puede arreglar nada — no distingue un <script> de verdad de una mencion
    nuestra en un comentario. Un guardia que se planta tiene la obligacion
    de mostrar la prueba. Ahora imprime el texto encontrado, con setenta
    caracteres a cada lado, y cuantos archivos y paginas miro.

  · LA CAUSA MAS PROBABLE, Y SU ARREGLO. `construir-sitio.mjs` ESCRIBE las
    paginas pero no borra las que sobran, y hace bien: es el que arma el
    sitio, no el que lo limpia. Pero si en `sitio/` quedo un .html de una
    corrida anterior -con la publicidad puesta- el empaquetado lo copia
    adentro del .ipa aunque esta corrida haya armado todo bien.

    Es la regla de siempre: lo generado que sobrevive a su motivo miente.
    Ahora se borran los .html de la raiz antes de empezar. `datos/` NO se
    toca: ahi vive lo que se bajo de la API y volver a pedirlo cuesta cuota.

    Probado aca: mi propia carpeta `sitio/` tenia 35 paginas viejas y la
    corrida de verdad arma 9. Treinta y cinco archivos que nadie habia
    vuelto a mirar desde vaya a saber cuando.

  · Y ABAJO DE TODO ESO APARECIO EL PROBLEMA DE VERDAD, QUE NO ESTABA
    PREVISTO. `construir-sitio.mjs` arma una pagina por club, pero solo de
    los clubes CUYOS DATOS ESTAN BAJADOS. Y esos datos -`sitio/datos`- no
    viven en el repositorio: viven en el cache del workflow de GitHub, que
    Codemagic no ve.

    O sea que una compilacion en la nube puede terminar bien, firmar bien, y
    producir un .ipa con la app adentro y SIN UN SOLO PARTIDO. Eso es peor
    que fallar: sube a TestFlight, se instala, y parece que la app esta
    rota.

    Asi que hay un guardia nuevo: si el paquete no llega a diez paginas de
    club o no trae ni un archivo de liga, se planta y lo explica. Un
    empaquetado a medias no se publica.

  · LO QUE VIENE. La foto tiene que salir de armael11.com, que es donde los
    datos SI estan publicados. El sitio publicado ES la foto. Eso se hace en
    el proximo zip, y no en este, por una razon: desde aca no puedo probar
    una descarga contra armael11.com, y codigo de red sin probar es
    exactamente lo que venimos evitando. Primero que la compilacion confirme
    el diagnostico; despues se escribe el arreglo.

Arma el 11 - version 2026-09-19 y55 (que el error de la firma se pueda leer)

  · SE SUBE ESTE SOLO. Incluye y50 a y54. Cambia UN archivo: codemagic.yaml.

  · QUE PASO. Las dos primeras compilaciones en Codemagic murieron con el
    mismo renglon:

      No matching profiles found for bundle identifier "com.armael11.app"
      and distribution type "app_store"

    Y murieron ANTES de correr un solo paso, asi que no habia log que leer.
    Eso es lo peor que puede pasar cuando se diagnostica a ciegas desde un
    navegador: el error no dice si falta el perfil, si falta el
    identificador, o si la clave de API no tiene permiso para crearlos. Son
    tres problemas distintos con tres arreglos distintos.

  · QUE SE CAMBIO. Codemagic ofrece dos maneras de firmar y comparten el
    mensaje de error:

      1. El bloque `ios_signing:` en el entorno. Codemagic busca el perfil
         antes de empezar y corta si no lo encuentra. Una linea menos, cero
         informacion cuando falla.
      2. Pedirlo a mano en un paso, con la herramienta que Codemagic ya trae
         instalada en la Mac.

    Se paso a la segunda. Ahora la firma es un paso propio que corre
    `app-store-connect fetch-signing-files "$BUNDLE_ID" --type IOS_APP_STORE
    --create`, y lo que conteste Apple queda escrito en el log.

    NO es una solucion al problema: es hacer que el problema se pueda ver.
    Puede ser que con esto ande de una (el `--create` crea lo que falte) o
    puede ser que falle igual, pero diciendo por que.

  · Y UN CARTEL EN CASTELLANO ANTES DE TODO. Si la integracion de App Store
    Connect no llego al entorno del paso, el error de Apple seria confuso.
    Asi que el paso primero comprueba que este, y si no, corta con tres
    renglones que dicen que revisar: que en Codemagic exista una clave
    llamada EXACTAMENTE `armael11`, porque el nombre es lo que las une.

  · LO QUE SE APRENDIO Y QUEDA ESCRITO EN EL ARCHIVO: la clave de API de App
    Store Connect necesita rol ADMIN. Con App Manager alcanza para subir a
    TestFlight pero NO para crear el certificado y el perfil, porque ese rol
    no llega a Certificates, Identifiers & Profiles. Fue mi recomendacion y
    estuvo mal: el criterio de dar el minimo permiso posible es correcto,
    pero el minimo que sirve para firmar es Admin.

Arma el 11 - version 2026-09-19 y54 (el envoltorio de iPhone)

  · SE SUBE ESTE SOLO. Incluye y50 a y53.

  · ESTE ZIP NO CAMBIA NADA DE LA WEB. Todo lo que trae esta apagado hasta
    que corra adentro de un iPhone. El sitio publicado queda identico: 322
    pruebas de navegador en verde, sin una sola diferencia.

  · CORREGIDO ANTES DE SUBIR (19/9, 22:50): el codemagic.yaml declaraba un
    grupo de variables `armael11` que no hace falta. Con la integracion de
    App Store Connect, Codemagic se encarga solo de los certificados y los
    perfiles. Un grupo declarado que no existe hace FALLAR la compilacion
    antes de empezar, con un error que no dice que grupo falta. Sacado.
    Lo unico que hay que configurar en Codemagic es la integracion, y se
    tiene que llamar exactamente `armael11`.

  · LOS ARCHIVOS NUEVOS

      datos-ios.js          el refresco de datos adentro del .ipa
      probar-datos-ios.mjs  14 pruebas
      empaquetar-ios.mjs    arma el `app/www` que viaja en el .ipa
      capacitor.config.json el envoltorio
      codemagic.yaml        compila en una Mac prestada y sube a TestFlight
      package.json          las dependencias de Capacitor (la web no usa una sola)

  · LA DECISION DE FONDO: FOTO ADENTRO + REFRESCO AL ABRIR. En la web los
    datos son archivos que el navegador baja cada vez. En un .ipa no: lo que
    se empaqueto el dia de la compilacion es lo que hay, y cambiarlo
    significa mandar una version nueva a revision de Apple y esperar dos
    dias. Una app de futbol con la fecha congelada no sirve.

    Las tres salidas se evaluaron:

      a) Solo la foto. Barata y muerta: una semana despues muestra una fecha
         ya jugada.
      b) Solo la red. Liviana, pero el revisor de Apple INSTALA LA APP Y LA
         PRUEBA SIN CONEXION. Veria una pantalla vacia, y "funciona sin
         conexion" es una de las cuatro cosas con las que justificamos que
         esto no es un sitio reempaquetado (regla 4.2).
      c) Las dos. Arranca de la foto -siempre esta, siempre es instantanea-
         y se pone al dia en segundo plano.

    Se eligio la (c). Y hay una cuarta que NO es opcion: Capacitor permite
    apuntar la app a armael11.com con `server.url`. Es media hora de trabajo
    y es literalmente la TWA de Android otra vez, o sea el caso que la 4.2
    describe como motivo de rechazo. Ademas cargaria el AdSense del sitio.
    El atajo que parece mas corto es el unico que garantiza el rechazo.
    Queda escrito adentro de `capacitor.config.json` para que nadie lo
    "arregle" mas adelante.

  · POR QUE SE PARSEA Y NO SE EVALUA. Lo que se baja son archivos .js
    nuestros, de la forma `window.X = {...}`. La tentacion es meterlos en un
    <script> y dejar que el navegador los corra: dos lineas. No se hace.

    Primero, por Apple: la regla 2.5.2 prohibe descargar y ejecutar codigo
    que cambie lo que la app hace. El JavaScript adentro de un webview es la
    excepcion permitida, pero "bajo un .js de mi servidor y lo ejecuto" es
    justo la frase que un revisor no quiere leer. Bajando DATOS y no codigo,
    la conversacion no existe.

    Segundo, porque es verdad: son datos. Parsearlos con JSON.parse
    significa que ni un archivo corrupto, ni un servidor tomado, ni un
    intermediario pueden hacer nada mas que darnos un objeto feo -que se
    descarta-. Ejecutarlo seria darles la app entera.

  · EL CANDADO DE ADSENSE, RESUELTO. El candado que protege a la app de Play
    mira el `document.referrer` buscando `android-app://`. Adentro de un
    webview de Capacitor ese referrer NO EXISTE -es vacio o
    `capacitor://localhost`-, asi que el HTML del sitio tal cual cargaria
    AdSense adentro del iPhone. Eso rompe la politica de AdSense, cuya
    sancion cae sobre la cuenta entera, Y desmiente la declaracion de
    privacidad de la ficha de la App Store.

    La defensa NO es otra rama en el candado: es sacar el script del
    archivo. `construir-sitio.mjs` entiende `SIN_PUBLICIDAD=1` y arma el
    sitio sin nada de AdSense -ni el script, ni el ads.txt, ni
    `window.SITIO.publicidad`-. LO QUE NO ESTA EN EL ARCHIVO NO SE PUEDE
    PRENDER POR ERROR.

    Y `empaquetar-ios.mjs` revisa archivo por archivo antes de copiar: si
    aparece el dominio de Google en cualquier .html, .js, .txt o .json, SE
    PLANTA y termina con error. No avisa: se planta. Es el unico lugar donde
    esto se puede frenar, porque el .ipa no pasa por publicar.mjs ni por la
    bateria de pruebas.

  · "DATOS DEL VIERNES 19/09". Un iPhone recien instalado y sin conexion
    muestra la foto, que puede tener una fecha ya jugada. Mostrarla como si
    fuera la de ahora es la misma confusion que arreglamos con el aviso de
    formaciones tentativas: la persona no piensa "los datos estan viejos",
    piensa "la app se equivoca". Y una vez que pensó eso, deja de creerle
    tambien a lo que si esta bien. Asi que la app lo dice, con la fecha.
    Seis horas de tolerancia: una fecha se juega en tres dias y el once del
    DT dura una hora.

  · NO SE USA EL SERVICE WORKER. En la web hace exactamente esto. Adentro
    del webview de iOS, con el esquema `capacitor://`, los service workers
    no funcionan de forma confiable: es una limitacion de WKWebView, no algo
    que podamos configurar. Por eso el cacheo, que en la web sale gratis,
    aca hubo que escribirlo. Va en IndexedDB y no en localStorage: el cache
    de un club solo pesa unos 400 KB y localStorage en iPhone tiene unos 5
    MB para TODO, incluida la sesion de la persona.

  · LAS PRUEBAS, Y QUE CUIDAN. 14 casos nuevos, y casi ninguno prueba que
    actualice. Prueban que NO ROMPA:

    - Que un archivo cortado a la mitad -la red se corto, el CDN devolvio
      una pagina de error- NO pise lo que ya teniamos. Sin eso, un refresco
      fallido deja la app sin partidos y no hay vuelta atras hasta la
      proxima red.
    - Que los dos formatos que abren con `window.X=window.X||{}` no
      devuelvan ese `{}` vacio. Es el error que no tira error: no falla, no
      avisa, y borra la liga entera.
    - Que sin red todo quede exactamente como estaba.

  · EL TAMANO. 117 archivos, 6.482 KB. De eso, 2.914 KB son datos -lo que se
    refresca de armael11.com- y el resto es la app, que solo cambia con una
    version nueva. Para un .ipa no es nada.

  · LO QUE FALTA PARA LA v1.1, ANOTADO EN EL PROPIO codemagic.yaml: los
    avisos por APNs. El App ID ya los tiene habilitados y el .p8 esta
    descargado, pero el plugin necesita ademas el entitlement
    `aps-environment` adentro del proyecto de Xcode, y eso hay que verlo
    contra una compilacion de verdad en vez de escribirlo a ciegas. La v1
    sale con las otras cuatro cosas que un navegador no puede hacer: vibra
    en cada gol, comparte con la hoja del sistema, abre los links del feed
    adentro de la app, y funciona sin conexion.

Arma el 11 - version 2026-09-19 y53 (dos formas de perder el once del DT sin que se note)

  · SE SUBE ESTE SOLO. Incluye y50, y51 e y52.

  · QUE SE ARREGLO. `enVentana()` -la funcion que decide si el proximo
    partido de un club esta por empezar- agarraba la PRIMERA clave del cache
    que empezara con `/fixtures?team=`.

    El problema es que el cache de un club NO trae solo los partidos del
    club. `datos-juego.mjs` baja tambien los del RIVAL, para armarle el
    plantel, y lo hace para dos rivales distintos -el del proximo partido y
    el del ultimo-. O sea que ahi adentro hay dos o tres claves que empiezan
    igual.

    Funcionaba por una casualidad: el club se guarda antes que el rival y
    `Object.keys` respeta el orden de insercion. Esa casualidad no estaba
    escrita en ningun lado ni la fijaba ninguna prueba.

  · POR QUE IMPORTA, Y POR QUE NO SE HABRIA NOTADO. El dia que algo reordene
    el archivo, la ronda corta se pone a mirar el calendario del rival. No
    tira error. No rompe ninguna prueba. El sitio publica igual. Lo unico que
    pasa es que el once del DT deja de salir para ese club, y nadie entiende
    por que. Es la misma forma de falla que nos costo una noche con `hayRed`:
    silenciosa, y solo visible en el resultado final.

    HONESTIDAD: no esta confirmado que ESTO sea lo que Fausto vio. Es un
    defecto real encontrado leyendo, y se arregla porque es barato y porque
    una falla silenciosa no se deja parada aunque hoy no este disparando.

  · COMO SE ARREGLO. Se le pasa el `apiId` del club y se elige la clave que
    corresponde. Si ninguna matchea -un cache viejo, otra temporada- vuelve
    al comportamiento de antes en vez de quedarse sin nada.

  · LA PRUEBA QUE LO FIJA. En `probar-formaciones.mjs`, un cache con el
    rival PRIMERO y el club segundo. Si alguien vuelve al `.find()` de antes,
    esa prueba se cae. 36 casos, eran 34.

  · SEGUNDO ARREGLO: EL ONCE DEL DT YA NO SE BORRA SOLO. `datos-juego.mjs`
    arma el cache de cada club DE CERO (`cache = {}`) y lo escribe encima
    del anterior. Para casi todo esta bien: es lo que impide que un cache a
    medias sobreviva para siempre.

    Para una cosa no. El once del DT del proximo partido lo trae la ronda
    corta apenas sale, una hora antes del pitazo, y este paso corre cada
    SEIS HORAS. Si le toca caer justo en esa hora -que es exactamente la
    hora en que la gente abre la app- borraba el once y lo volvia a pedir.
    Y si en ese instante la API no contestaba o se habia acabado la cuota,
    `api()` devuelve `[]` sin hacer ruido: la app pasaba a decir
    "formaciones tentativas" con el once del DT ya publicado en la calle.

    Se arreglaba solo a los quince minutos, en la ronda corta siguiente.
    Pero esos quince minutos son los peores del dia.

    Ahora el once que ya tenemos se ARRASTRA en vez de volver a pedirse.
    Volver a pedirlo no puede traer nada mejor -el once del DT no cambia
    una vez publicado- y si puede traer algo peor. Un once que ya tenemos
    no se cambia por la promesa de volver a conseguirlo. De paso son
    treinta pedidos menos por corrida completa.

  · LO QUE QUEDA ANOTADO Y NO SE TOCO. El cache de un club solo tiene
    partidos de la liga 128. Un partido de Copa Argentina o de Libertadores
    no esta ahi, asi que su once del DT no llega nunca por este camino. No es
    una regresion, es el alcance con el que se construyo; hay que decidirlo,
    no arreglarlo a escondidas.

Arma el 11 - version 2026-09-19 y52 (dejar de adivinar la prueba cerrada)

  · SE SUBE ESTE SOLO. Incluye y50 e y51.

  · NO HAY SQL QUE PEGAR. La funcion `sumar_hito` que ya esta en Supabase
    acepta los codigos nuevos tal cual: el guion entra en su expresion. Se
    sube el zip y empieza a contar solo.

  · POR QUE EXISTE ESTE ZIP. Google rechazo el acceso a produccion con
    "los testers no estuvieron activos durante la prueba cerrada". Puede
    ser cierto o puede no serlo, y el problema de fondo es ese: NO TENEMOS
    COMO SABERLO.

    Play Console con doce testers no muestra casi nada -debajo de cierto
    volumen esconde los numeros- y la app es una TWA, o sea Chrome abriendo
    armael11.com. Un tester que entra escribiendo la direccion en vez de
    tocar el icono es, para Google, un tester que no entro. Es la hipotesis
    mas probable y hasta hoy no habia forma de confirmarla ni de negarla.

    Los contadores de campana no servian: solo cuentan a quien llega con un
    `?c=` en la direccion, que es nadie entre los testers.

  · QUE CUENTA. Por dia, cuantos TELEFONOS DISTINTOS abrieron y cuantos
    simularon, separando la app instalada del navegador:

        uso-app | 2026-09-20 | abrio  |  9
        uso-app | 2026-09-20 | simulo |  6
        uso-web | 2026-09-20 | abrio  | 41

    `uso-app` es la columna que mira Google. Si ese numero no llega a doce
    ningun dia, ahi esta el motivo del rechazo, escrito.

  · LO QUE SIGUE SIN GUARDARSE. Ningun identificador de persona, ninguna
    IP, ninguna hora. Las mismas cuatro columnas de siempre. La UNICA
    diferencia con el contador de campana es que el "ya conte este
    telefono" caduca a medianoche, porque la pregunta no es "cuantos
    llegaron alguna vez" sino "cuantos vuelven cada dia". Ese dato sigue
    viviendo en el telefono y nunca sale de ahi.

  · EL DIA SE CALCULA CON EL RELOJ DEL TELEFONO, NO CON UTC. Es el detalle
    que mas facil se hace mal. En Argentina, de las 21 en adelante UTC ya
    esta en el dia siguiente: con UTC, un tester que abre a las 20 y vuelve
    a las 22 contaria dos veces, y las dos filas caerian en el mismo dia del
    servidor -que si calcula en hora argentina-. Son justo las horas en que
    se miran los partidos. Hay una prueba que finge ese momento exacto.

  · DONDE SE ENGANCHA, Y POR QUE AHI. El "abrio" va adentro de
    `enganchar()` y no arriba de todo. Arriba, `esDeLaTienda()` todavia no
    puede leer la constante `PAQUETE` -declarada mas abajo con `const`, y
    `typeof` NO protege de eso, cosa que ya nos costo una vez- asi que
    caeria en su rama de emergencia y contaria mal justo lo que venimos a
    medir. `enganchar()` corre en cada pintado, decenas de veces en una
    tarde; el propio contador se encarga de que sea una sola vez por dia, y
    hay una prueba en navegador que llama veintidos veces y exige un solo
    envio.

  · PRUEBAS. 28 en `probar-campana.mjs` (eran 21) y 322 en el navegador
    (eran 318). Las cuatro nuevas del navegador fijan lo que las de Node no
    pueden ver: que ALGUIEN llame al contador. Un contador perfecto que
    nadie invoca da cero todos los dias y parece que no entro nadie.

Arma el 11 - version 2026-09-19 y51 (la publicidad, enchufada de verdad)

  · SE SUBE ESTE SOLO. Incluye y50.

  · ads.txt EN LA RAIZ DEL SITIO. AdSense pide verificar la propiedad del
    sitio y ofrece tres metodos: pegar el codigo en el <head>, una
    metaetiqueta, o este archivo. Se eligio el archivo porque es el UNICO
    que no mete nada adentro del HTML, y aca eso importa mas que en otro
    lado: el sitio y la app de Play son el mismo HTML, asi que todo lo que
    se agrega a la pagina viaja tambien adentro de la app.

    Ademas cumple su funcion original, que es el estandar del IAB contra la
    reventa de inventario: sin ads.txt, buena parte de los compradores no
    oferta.

        google.com, pub-2032639837955609, DIRECT, f08c47fec0942fa0

    El `f08c47fec0942fa0` no es un dato nuestro: es el identificador de
    Google como autoridad certificadora, el mismo para todo el mundo.
    Y el id va con `pub-`, NO con `ca-pub-`: ese es el error clasico y hay
    una prueba que lo fija.

    Si se saca la publicidad de sitio.json, el archivo se BORRA. Misma regla
    del CNAME: lo generado que sobrevive a su motivo miente, y un ads.txt
    que autoriza a vender publicidad de un sitio que ya no la tiene es justo
    la clase de mentira que ese archivo existe para evitar.

  · LOS NUMEROS DEL RESPALDO, ACTUALIZADOS. La portada seguia diciendo
    "10.860 partidos de nueve ligas" y eso era de agosto: desde entonces se
    backtestearon cuatro ligas mas. Fausto lo vio.

    El conteo real, sacado de la tabla `backtest_resultado` de Supabase, que
    es publica y se puede auditar:

      Las nueve de antes (corridas afuera)          10.860
      Colombia 865 · Mexico 677 · Peru 610 · Uruguay 333   2.485
      TOTAL                                         13.345 partidos, 13 ligas

    NO se suman Brasil (720) ni Alemania (594) de las corridas nuevas: son
    las mismas ligas que ya estaban contadas arriba, corridas otra vez por
    la pantalla nueva. Contarlas dos veces infla el numero sin haber medido
    nada nuevo.

    Ojo con una confusion facil, y por eso quedo escrita en el codigo: TRECE
    son las ligas MEDIDAS, ONCE las que estan en la app. No son la misma
    lista -Paises Bajos y Uruguay se midieron y no estan- y el numero de la
    portada habla de cuanto se probo el modelo, no de cuantas ligas se
    pueden simular.

    El "1.783 partidos de tres ligas que el modelo nunca habia visto" NO
    cambia: es el de aquella corrida limpia, con las hipotesis escritas
    antes, y es el dato mas fuerte que hay.

    El texto de abajo de las ligas pasa de "mas de 12.000 de doce ligas" a
    "mas de 13.000 de trece", siempre redondeado HACIA ABAJO: un numero que
    se queda corto nunca hay que salir a defenderlo.

  · LA CUENTA DE ADSENSE YA ESTA: cliente ca-pub-2032639837955609 puesto en
    sitio.json. Falta la UNIDAD DE ANUNCIO -el `bloque`-, que se crea en
    AdSense recien cuando Google aprueba el sitio. Hasta entonces la app se
    ve EXACTAMENTE como hasta ahora: entra el script de Google, que es lo
    que le permite a Google revisar, y no se dibuja ningun hueco.

  · EL SCRIPT DE GOOGLE NO ES UNA ETIQUETA SUELTA, Y ESTO PROTEGE LA CUENTA.
    El sitio y la app de Play son EL MISMO HTML -la TWA es Chrome cargando
    armael11.com-, asi que un `<script src>` estatico viajaria tambien
    adentro de la app. AdSense es un producto para sitios web y usarlo
    adentro de una app es una infraccion cuya sancion cae sobre la CUENTA
    ENTERA, que es de donde sale la plata.

    El candado de `sePuedePublicidad()` impedia que se DIBUJE un aviso
    adentro de la app, pero no que el codigo de Google se CARGUE ahi. Ahora
    la carga tambien se decide: seis lineas que miran el referrer y, si
    venimos de la app, no agregan nada. Para un navegador normal -y para el
    robot que revisa el sitio- entra como siempre.

    La comprobacion esta repetida a mano en construir-sitio.mjs en vez de
    reusar `esDeLaTienda()`, y es a proposito: tiene que correr sin depender
    de que ningun otro archivo haya cargado. Si dependiera del orden de los
    <script>, el dia que alguien mueva uno el candado se abre en silencio.


  · HASTA AHORA LAS REGLAS ESTABAN Y EL AVISO NO. `momentoDeAviso` decidia
    que correspondia uno y despues no pasaba nada: no habia ningun hueco
    donde mostrarlo. Esto es la otra mitad.

  · EL FORMATO: UN BANNER DEBAJO DE LA CANCHA, mientras el partido se juega.
    No es un cartel que hay que cerrar para seguir. Es la misma idea que ya
    estaba escrita en el codigo: los veinte segundos SON el aviso, el rato
    durante el cual la app puede mostrar uno sin interrumpir nada. Lo que
    compra el premium es sacarlo Y que la espera pase a seis segundos; si el
    aviso frenara el partido, el premium estaria comprando que la app deje
    de molestar, que es otra clase de producto.

    El hueco lleva alto minimo reservado: si el aviso entra de golpe y
    empuja la cancha hacia abajo justo cuando la persona esta mirando el
    partido, deja de ser algo que se tolera y pasa a ser algo que molesta.
    Y va rotulado "Publicidad". Un aviso disfrazado de contenido es
    exactamente lo que no queremos hacer.

  · TRES CANDADOS, Y EL TERCERO ES EL QUE IMPORTA:

      1. Tiene que haber red configurada.
      2. El que pago no ve ninguno.
      3. ADENTRO DE LA APP DE PLAY, NUNCA. AdSense es un producto para
         SITIOS WEB. Un AdSense corriendo adentro del webview de una app es
         una infraccion de la politica de Google, y la sancion no es sobre
         el aviso: es sobre la cuenta entera, que es de donde sale la plata.
         La app de la tienda va sin avisos hasta que exista AdMob, que es
         otro producto, otra cuenta y otro SDK.

         Ese `if` de una linea, en `sePuedePublicidad()`, es lo unico que
         separa las dos cosas. No se saca. Tiene su propia prueba.

  · DOS DATOS EN sitio.json, Y SON DOS MOMENTOS DISTINTOS:

        "publicidad": { "cliente": "ca-pub-...", "bloque": "1234567890" }

    El `cliente` lo da AdSense al aprobar la cuenta y hace entrar el script.
    El `bloque` es el id de la unidad de anuncio (AdSense -> Anuncios -> Por
    unidad de anuncio -> Display) y es lo que le dice a la app DONDE poner
    el aviso. Con cliente y sin bloque la app se ve exactamente como hasta
    ahora: una cuenta recien aprobada todavia no tiene unidades creadas, y
    ese rato no puede verse roto.

    Hoy los dos estan vacios, o sea apagada. La app no baja un solo script
    de terceros y eso sigue teniendo su prueba.

  · UN SOLO `push` POR HUECO. Google pide un push por unidad; el segundo
    sobre el mismo `ins` tira "All 'ins' elements in the DOM with
    class=adsbygoogle already have ads in them". Como `pintar()` redibuja la
    pantalla muchas veces por partido, sin la marca en el elemento eso
    pasaria en cada cuadro.

  · SI GOOGLE NO CARGA -bloqueador, red caida, cuenta suspendida- el partido
    sigue igual. Todo envuelto en try/catch, como el resto.

  · 8 pruebas nuevas en el navegador y 2 en el armado. Lo que fijan es sobre
    todo lo que NO tiene que pasar: que no aparezca apagada ni forzando la
    bandera, que no la vea el que pago, que NO APAREZCA ADENTRO DE LA APP DE
    PLAY, que no se dibuje con la cuenta aprobada pero sin unidad, y que no
    se empuje dos veces.

  · Y APARECIO UN BICHO VIEJO EN LAS PRUEBAS, QUE VALE ANOTAR. La prueba de
    las miniaturas hacia `window.SITIO = {miniaturas:"ninguna"}`: REEMPLAZABA
    el objeto entero y se llevaba puesto todo lo demas -supabase, cupo,
    avisos, publicidad- para todas las pruebas que venian despues. Era
    invisible mientras ninguna lo mirara. El dia que una prueba nueva leyo
    `SITIO.publicidad` empezo a fallar sin razon aparente, a doscientas
    lineas de distancia del culpable. Ahora toca solo su clave y la
    devuelve.

  · UNA PRUEBA VIEJA SE AJUSTO, Y VALE DECIR POR QUE. Miraba que la palabra
    "adsbygoogle" no apareciera en el HTML. Ahora esa palabra esta adentro
    de nuestro propio JavaScript, en la funcion que arma el hueco, donde es
    codigo inerte hasta que sitio.json tenga el cliente. La propiedad que
    hay que cuidar es "no se baja un solo script de terceros", no "no
    aparece cierta cadena de texto": ahora mira el dominio de Google, que es
    lo que de verdad significa bajar algo. Que ademas no se DIBUJE ningun
    hueco con la publicidad apagada se prueba en el navegador, que es donde
    se puede ver. Cada prueba mira lo que su mundo le deja ver.

    Lo que NO se hizo es escribir el codigo raro para esquivar la prueba.

Arma el 11 - version 2026-09-19 y50 (contador de campana, botones de verdad, y la fecha que corresponde)

  · INCLUYE y45 a y49. Se sube este solo.

  · SABER SI LA PLATA DE INSTAGRAM TRAJO GENTE QUE JUEGA. Meta sabe contar
    clicks y visitas, y las cobra. Lo que Meta no puede saber es lo unico
    que importa: de los que entraron, cuantos simularon. Una visita que
    rebota a los tres segundos y una que arma un once y aprieta simular
    valen lo mismo en el panel de anuncios y no valen lo mismo para nada
    mas.

    Sin esto, el lunes tenemos una sola cifra -"costo $95 la visita"- y
    ninguna manera de saber si esas visitas sirvieron. Ahora tenemos la
    cadena entera: llegaron N, simularon M, se instalaron K, hicieron
    cuenta J.

    La direccion del anuncio lleva un codigo: armael11.com/?c=ig1

  · SIN UN SOLO SCRIPT DE TERCEROS, que era la condicion. El pixel de Meta
    contaria mejor y a cambio meteria un script que sigue a la persona por
    todos los sitios que lo tengan puesto. No entra. Esto es un fetch a
    nuestra propia base, igual que todo lo demas.

  · QUE SE GUARDA, EXACTAMENTE. Una fila por codigo, dia e hito, con un
    numero al lado:

        ig1 | 2026-09-19 | llego  | 143
        ig1 | 2026-09-19 | simulo |  38

    Y nada mas. No hay identificador de persona, no hay IP, no hay hora, no
    hay nada que permita volver de una fila a alguien. El "a este telefono
    ya lo conte" vive en el localStorage y NUNCA sale de ahi.

    El costo de esa decision es real y esta aceptado: no se puede
    reconstruir el recorrido de una persona. No hace falta. La pregunta es
    "de cien que entraron, cuantos jugaron", y para eso alcanzan dos
    numeros.

  · UN HITO, UNA VEZ. El que simula veinte veces en una tarde es una
    persona, no veinte. Si no, el porcentaje deja de querer decir nada.
    Se marca como mandado ANTES de que conteste el servidor, a proposito:
    preferimos perder un hito antes que contar de mas. Un numero inflado
    se parece demasiado a una buena noticia.

  · EL ?c= SE SACA DE LA BARRA apenas abre la pagina, y no es cosmetica. Si
    se queda, el primero que comparta el link por WhatsApp le manda a
    quince amigos una direccion que dice "vengo del anuncio", y el lunes el
    anuncio figura trayendo gente que llego por un amigo. El numero se
    ensucia solo.

  · LOS PARTIDOS DE LA LIGA ERAN DE DOS FECHAS DISTINTAS. Fausto: "los
    partidos de la fecha de la liga de Inglaterra no son los que
    corresponden". Estaba mal de verdad, y el error era de fondo.

    `ligas-api.mjs` publicaba `.slice(0, 12)` sobre TODOS los partidos por
    jugar ordenados por fecha, mientras la app decia "cualquier partido de
    la proxima fecha". No es lo mismo. Una fecha de la Premier tiene diez
    partidos y el tope era doce: sobraban dos, y ese sobrante se llenaba
    con la fecha siguiente. Peor todavia: a medida que el sabado avanza y
    los partidos arrancan, la fecha en curso se achica y el relleno crece.

    Hoy, 19/9, la Premier mostraba cinco partidos del fin de semana y siete
    del 10 de octubre -tres semanas despues- uno al lado del otro y sin
    nada que los distinguiera.

    Ahora se toma la ronda del primer partido que viene y se publican TODOS
    los de esa ronda. Ademas sale mas barato: doce partidos son veinticuatro
    equipos y una fecha de la Premier son veinte. El tope subio de 12 a 15
    pero ya no elige nada, solo frena el gasto: quince es la fecha mas
    grande de las once ligas, la argentina, con treinta equipos.

    El domingo a la noche la fecha esta casi jugada y quedarian uno o dos
    partidos. Ahi, y solo ahi, se suma la siguiente: una lista de un partido
    es peor que una que mezcla, SIEMPRE QUE SE DIGA CUAL ES CUAL. Por eso
    ahora cada partido escribe su fecha: "Fecha 5 · sab 20/09 · 13:00".

    OJO: esto lo arregla la proxima corrida completa de "Traer y publicar".
    Los archivos de liga los escribe el workflow, no el zip.

  · LA REGLA VIVE EN `fecha-de-liga.mjs`, en su propio archivo y con nueve
    pruebas que IMPORTAN la funcion de verdad en vez de copiarla. No se
    podia probar donde estaba: `ligas-api.mjs` arranca solo y sin la clave
    de la API se apaga con `process.exit`, asi que una prueba tendria que
    haber copiado la regla. Una regla copiada en la prueba es una regla que
    tarde o temprano deja de ser la que corre -y esta ya se equivoco una
    vez-.

  · LOS PARTIDOS, CON BOTON. Misma correccion que la de los planes y mismo
    motivo. Eran filas de texto de 13.5px con un ">" al final y un
    role="button" que solo ve el lector de pantalla: para el ojo, una lista.
    Ahora el partido es texto y al lado va un boton que dice "Simular".

    Y algo que se podria haber colado: al pasar de div a <button>, Enter y
    Espacio ya disparan click solos. El keydown viejo llamaba a simular DOS
    VECES, y cada una gasta una del cupo. Ahora el teclado se atiende a mano
    solo si el elemento no es un boton, con una prueba que lo fija.

  · LA PANTALLA BAJA SOLA A LOS PARTIDOS. Fausto: "que se desplace
    automaticamente hacia abajo". Con once ligas agrupadas por continente,
    el que elige Italia la tiene abajo de todo y la lista aparece FUERA de
    la pantalla: la app se ve como si el toque no hubiera hecho nada.

    Baja al ABRIR y no al cerrar -bajar al cerrar seria perseguir a alguien
    que acaba de decir que no queria ver eso-, deja un margen arriba para
    que se siga viendo de que liga son, y respeta a quien pidio menos
    movimiento en su sistema.

  · UN AVISO DE SI LAS FORMACIONES ESTAN CONFIRMADAS. Fausto lo pidio y
    destapa algo que la pantalla daba por sabido y no lo es: LOS ONCE DE LA
    CANCHA SIEMPRE LOS ARMO LA APP, con los que vienen jugando. Nunca son
    los del DT, ni siquiera cuando el DT ya los publico: el once del DT se
    simula aparte, con el boton de abajo.

    Sin decirlo, alguien que entra media hora antes del partido puede creer
    que esta mirando la formacion confirmada y que la app se equivoco en
    cuatro nombres. Es la clase de malentendido que hace que una persona
    deje de confiar en todo lo demas.

    Tres estados: las dos confirmadas, una sola, o ninguna. El de "ninguna"
    dice ademas cuando sale la de verdad, que es lo unico accionable.

  · LA HORA DEL PARTIDO, ESCRITA A MANO Y NO CON toLocaleString. Se probo
    con toLocaleString("es-AR") y en un navegador con ICU recortado devolvio
    "sab 05-09 · 02:00 p. m.": mes y dia al reves, y en doce horas, que no
    es como se lee una hora aca. Cinco lineas de padStart dan siempre lo
    mismo en todos lados.

  · LOS PLANES AHORA TIENEN UN BOTON DE VERDAD. Fausto: "que los botones
    de compra se vean realmente como botones y no como texto encasillado".
    Tenia razon y el diagnostico era exacto: cada plan ERA el boton, un
    `.boton.flaco` de ancho completo, o sea un rectangulo transparente con
    borde gris fino, texto a la izquierda y el precio adentro en gris
    claro. Tres de esos, uno abajo del otro, no se leen como botones: se
    leen como tres cajitas de texto. No habia ni una sola senal de que eso
    se pudiera apretar -ni relleno, ni sombra, ni una palabra que dijera
    que pasa si lo tocas-.

    Ahora el texto es texto -nombre, precio, que incluye- y al lado hay un
    boton con el MISMO verde, el mismo radio y la misma sombra que el de
    Simular, que es el boton que todo el mundo ya aprendio a apretar en
    esta app. Y dice "Comprar": un boton que no dice que hace no es mucho
    mejor que una caja.

    Se fue tambien el recuadro de cada plan. Con el boton adentro, el borde
    no sumaba nada y agregaba ruido; una linea fina entre planes alcanza
    para separarlos y ocupa la mitad del alto.

    El `data-plan` sigue en el boton, que es lo que engancha la compra:
    cambio como se ve, no como se compra. Cuatro pruebas nuevas en el
    navegador fijan que no vuelva a pasar -que el boton siga siendo `.acc`,
    que siga diciendo que hace, que el precio NO este adentro y que tenga
    relleno y no sea un rectangulo transparente-.

  · HAY QUE PEGAR UN SQL. `contador-campana.sql`, entero, en Supabase ->
    SQL Editor -> Run. Se puede correr las veces que haga falta. Adentro,
    comentada, esta la consulta para mirar el lunes.

    Sin ese SQL la app anda igual: el contador manda al vacio y se calla.

  · 21 pruebas nuevas en `probar-campana.mjs` y 9 en el navegador. Lo que
    mas se prueba no es que cuente: es que NO cuente cuando no corresponde
    -dos veces el mismo, un codigo inventado, una visita de hace dos
    meses- y que no explote cuando el navegador niega el almacenamiento.

    Y la de siempre: que ninguno de los nombres nuevos haya pisado algo. El
    sitio son <script> clasicos en UN solo alcance global; ya nos costo una
    portada en blanco con `hayRed`. Por eso todo lleva `camp` o `Campana`.

Arma el 11 - version 2026-09-19 y49 (instalar desde la web, y por que Play no contesta)

  · INCLUYE y45 a y48. Se sube este solo.

  · SE PUEDE INSTALAR DESDE LA WEB, Y AHORA SE OFRECE. El sitio ya era
    instalable -manifest, service worker, iconos- y no lo proponia nunca:
    quedaba escondido en el menu de Chrome, donde no lo encuentra nadie.

    Dos costos concretos de eso. El que llega de Instagram se queda en una
    pestana: sin icono, no vuelve solo, y sobre todo NO PUEDE RECIBIR EL
    AVISO del once del DT, que es el gancho para que vuelva cada fecha. Y
    una app instalada desde la web no es una app de Play: el cobro sigue
    por Mercado Pago, sin comision. Play se lleva 15%.

    El boton se comporta distinto en cada lado:
      - Android con oferta del navegador: un toque y se instala.
      - iPhone: no existe el evento, van las instrucciones de Compartir ->
        Agregar a inicio. Y ahi no es comodidad: en iOS los avisos SOLO
        llegan si la app se agrego a la pantalla de inicio.
      - Ya instalada, o adentro de la app de Play: no aparece nada.
      - Sin oferta del navegador: tampoco. Un boton que no puede hacer lo
        que dice es peor que ninguno.

  · EL CARTEL DE PLAY AHORA DICE EL ERROR DE GOOGLE. `servicioDePlay` se
    tragaba la excepcion en un catch vacio, y ese texto es lo unico que
    distingue "no se instalo desde Play" de "todavia no propagaron los
    productos". Nos costo una noche de adivinar. Ahora se muestra.

  · LAS TARJETAS DE PLAN, UNA DEBAJO DE LA OTRA. Con `flex:1 1 150px` los
    tres planes envolvian en el telefono y cada uno quedaba de un ancho
    distinto, escalonados como una escalera rota. Tres precios desalineados
    se leen como un error de la app, no como una lista de planes.

  · PRUEBAS: 58 en el puente nativo (14 nuevos, la decision de instalar es
    pura y se prueba sin navegador) y 286 en la suite del navegador (4
    nuevos). Todo el resto verde.

  · ARCHIVOS: nativo.js, app.tpl.html, probar-nativo.mjs, probar-sitio.cjs,
    VERSION.txt, mas todo lo de y45/y46/y47/y48 (cuentas.js,
    construir-sitio.mjs, publicar.mjs, formaciones.mjs, ligas-api.mjs,
    probar-formaciones.mjs).

Arma el 11 - version 2026-09-19 y48 (los planes en la portada, y el cartel que dice por que)

  · INCLUYE y45, y46 y y47. Se sube este solo.

  · LOS PLANES EN LA PORTADA. Fausto: "la compra deberia estar tambien en
    la portada, esta todo muy en scroll infinito y si no bajo no me entero".
    Antes el cupo y los planes aparecian unicamente abajo del resultado, o
    sea DESPUES de simular: el que abria la app y queria pagar no encontraba
    donde. Ahora van despues del gancho y antes de la lista de ligas. En ese
    orden y no en otro: primero que es la app, despues cuanto te queda y
    como seguir, y recien ahi la lista larga.

  · EL CARTEL AHORA DICE CUAL DE LOS TRES PROBLEMAS ES. "La compra desde la
    app todavia no esta disponible" se mostraba igual para tres cosas
    completamente distintas, que se arreglan de maneras distintas:

      - la app instalada es una version sin facturacion (hay que
        actualizarla desde Play),
      - Google Play no contesta en ese telefono,
      - la facturacion esta pero los productos todavia no propagaron.

    Adivinamos mal una vez y costo una noche. Ahora cada rama deja escrito
    que fallo y que hacer, y `mirarPlay` ademas PIDE los productos, que es
    lo que separa "la app no tiene la puerta" de "la puerta esta y adentro
    no hay nada".

  · LO QUE COSTO, Y ES UNA TRAMPA QUE VALE ANOTAR. Al dibujarse en la
    portada, `tarjetasDePlan()` corre en el PRIMER pintado y nombra
    variables (`conBackend`, `PLANES`) declaradas mas abajo con `const`.
    Eso no devuelve undefined: tira ReferenceError, y como pasa adentro de
    `pintar()` deja LA PORTADA EN BLANCO.

    Y el atajo obvio no sirve: `typeof` NO salva de esto. Con una variable
    que no existe devuelve "undefined"; con un `const` declarado mas abajo
    tira igual. Es la diferencia entre "no existe" y "todavia no nacio", y
    solo la segunda es un error. Va un try/catch, que es feo y es lo unico
    que funciona. Hay un caso de prueba que falla si alguien lo saca.

  · PRUEBAS: 282 en la suite del navegador (4 nuevos), todo el resto verde.

  · ARCHIVOS: app.tpl.html, probar-sitio.cjs, VERSION.txt, mas todo lo de
    y45/y46/y47 (nativo.js, probar-nativo.mjs, cuentas.js,
    construir-sitio.mjs, publicar.mjs, formaciones.mjs, ligas-api.mjs,
    probar-formaciones.mjs).

Arma el 11 - version 2026-09-19 y47 (se puede comprar, que es lo minimo)

  · INCLUYE y45 y y46. Se sube este solo.

  · EL PROBLEMA. Fausto abrio la app aprobada por Google y dijo: "no hay un
    solo boton para comprar. Esta el aviso con los precios y en ningun lado
    la opcion de comprar, que deberia estar muy a la vista". Tenia razon y
    era grave: la app ya estaba aprobada con facturacion y no se podia pagar.

  · POR QUE PASABA. Los botones existian SOLO adentro del panel de cuenta, y
    ese panel dibuja los planes unicamente en la rama de "ya entraste y ya
    elegiste usuario". Abajo del boton de Simular habia tres <span> con los
    precios, de adorno. O sea: el precio se mostraba a todo el mundo y el
    boton a casi nadie.

    Lo peor es que el razonamiento original estaba bien y escrito en el
    codigo -"el precio se ve donde se siente el tope"- y quedo a medio
    hacer: se puso el precio y no el boton. Las dos cosas van juntas.

  · QUE CAMBIA. Una sola definicion de los tres botones, `tarjetasDePlan()`,
    usada en los dos lugares: abajo de Simular y adentro del panel. Estaban
    duplicados, y por eso uno de los dos se quedo sin botones.

    Y el enganche: el selector decia `#cuenta [data-plan]`, asi que los
    botones de la vista principal existian sin escuchar a nadie. Ahora
    engancha todos, y se vuelve a enganchar en cada pintar().

  · SIN CUENTA NO SE ESCONDE EL BOTON, SE ABRE LA CUENTA. El pase se
    acredita a un perfil: sin cuenta no hay a quien acreditarselo. Antes eso
    habria sido un boton que no responde. Ahora abre el panel y explica por
    que justo ahi: "el pase queda atado a tu cuenta y asi lo tenes en
    cualquier telefono".

  · PRUEBAS: 5 casos nuevos que fijan las dos mitades del arreglo -que los
    botones esten donde se ve el precio, y que escuchen-. Un boton dibujado
    sin escuchador es peor que no tenerlo: parece roto. 278 en la suite del
    navegador.

  · ARCHIVOS: app.tpl.html, probar-sitio.cjs, VERSION.txt, mas todo lo de
    y45 y y46 (nativo.js, probar-nativo.mjs, cuentas.js, construir-sitio.mjs,
    publicar.mjs, formaciones.mjs, ligas-api.mjs, probar-formaciones.mjs).

Arma el 11 - version 2026-09-19 y46 (el puente nativo, para la App Store)

  · INCLUYE y45, que no llego a subirse. Se sube este solo.

  · QUE ES. `nativo.js`, la pieza que contesta la regla 4.2 de Apple: "tu
    app tiene que ofrecer algo mas que un sitio web reempaquetado". Son
    cuatro cosas concretas, y la primera es la que se demuestra en diez
    segundos:

      1. EL TELEFONO VIBRA EN CADA GOL del partido simulado. Safari en
         iPhone no tiene haptica: es una diferencia real, no un argumento.
      2. Compartir usa la hoja del sistema, no un link de wa.me.
      3. Los links del feed abren DENTRO de la app. Esto ademas es
         obligatorio: un webview que se va navegando a un diario es la
         definicion del rechazo.
      4. La app sabe si hay conexion de verdad (navigator.onLine miente:
         dice que hay red con un wifi sin internet).

  · LA REGLA QUE ORDENA EL ARCHIVO: nada de esto puede romper la web. El
    mismo sitio corre en armael11.com, en la TWA de Android y adentro de la
    app de iPhone. Cada funcion pregunta primero si hay con que; si no lo
    hay devuelve false y el que llamo sigue como antes. Sin imports, sin
    bundler, sin npm: los plugins se leen de window.Capacitor.Plugins en el
    momento de usarlos. Meter un paso de compilacion romperia lo unico que
    hace que esto se pueda arreglar sin una maquina de desarrollo al lado.

  · LO QUE COSTO, Y QUEDA ANOTADO. `nativo.js` definia `hayRed`, que en la
    app ya existia y significaba OTRA COSA: "hay red publicitaria". Los dos
    archivos entran como <script> sueltos y comparten el alcance global, asi
    que la colision mataba el script entero y la portada dejaba de
    dibujarse. Lo agarro la suite del navegador. Ahora se llama
    `hayConexion` y hay un caso que falla si alguien vuelve a pisarlo.

  · EL MAIL QUE NO SALE, AHORA LO DICE. Era la falla mas traicionera que
    tenia la app porque era invisible: la pantalla decia "te mandamos un
    mail", el mail no salia, y la persona se quedaba mirando una bandeja
    vacia convencida de que la app estaba rota. Y pasa justo en el peor
    momento: un dia de campana, cuando se llega al tope de envios. Ahora
    dice que no es su casilla, que es nuestro correo, y que pruebe en un
    rato. Los dos topes de envio ya los traducia `mensajeDe` y NO se pisan.

  · PRUEBAS: 44 casos nuevos en `probar-nativo.mjs`, cada funcion probada
    con Capacitor y sin Capacitor. 5 casos nuevos en la suite del navegador.
    273 en total ahi, todo el resto en verde.

  · ARCHIVOS: nativo.js (nuevo), probar-nativo.mjs (nuevo), app.tpl.html,
    cuentas.js, construir-sitio.mjs, publicar.mjs, probar-sitio.cjs,
    formaciones.mjs, ligas-api.mjs, probar-formaciones.mjs, VERSION.txt.

Arma el 11 - version 2026-09-18 y45 (el once del DT, en las once ligas)

  · QUE CAMBIA. Hasta ahora la formacion confirmada del DT se bajaba sola
    solo para los treinta clubes argentinos: eran los unicos con pagina
    propia y con cache. Desde y45 la ronda corta -la que corre cada quince
    minutos- tambien mira los partidos de las OTRAS DIEZ LIGAS y baja el
    once en cuanto sale, una hora antes del pitazo.

  · DONDE SE GUARDA. En un archivo nuevo por liga, `onces-<liga>.js`, y no
    adentro del archivo de la liga. El archivo de la liga lo rehace la
    corrida completa una vez por dia: si el once viviera ahi, cada dia se
    perderia. El archivo nuevo se acumula y se poda solo: pasadas 24 horas,
    afuera.

  · SE GUARDA SOLO LO QUE LA APP USA. La respuesta de la API son unos cinco
    kilobytes por partido -banco, cuerpo tecnico, fotos, coordenadas- y de
    eso se usan once nombres y el dibujo. Se guarda eso. Es un archivo que
    baja TODO el que abre la app, no un cache de servidor.

  · EN LA APP. Un partido de otra liga ahora es un partido de verdad: tiene
    id, fecha y los dos equipos, asi que anda el mismo revelado que en tu
    club -"salio el once del DT", "simulalo contra el tuyo"-. Lo que NO
    aparece, a proposito, son las dos cosas que son del club de la pagina:
    el aviso al telefono (nadie se suscribio al Bayern) y el link del
    pronostico (guarda el club, volveria con otro partido).

  · LO QUE CUESTA. Un pedido por partido en ventana y por ronda, hasta que
    el once sale. Una fecha completa de las once ligas son unos 600 pedidos
    repartidos en cuatro dias, sobre 7.500 diarios. Hay un tope de 60
    pedidos por ronda como seguro, no como plan.

  · QUE NO CAMBIA. Lo de tu club sigue exactamente igual, con su cache y su
    aviso al telefono. La comparacion con lo que PASO -el resultado, las
    expulsiones- sigue siendo solo de los clubes argentinos.

  · ARCHIVOS: formaciones.mjs, probar-formaciones.mjs, ligas-api.mjs,
    construir-sitio.mjs, app.tpl.html, probar-sitio.cjs, VERSION.txt.

Arma el 11 - version 2026-09-18 y44 (el interruptor del cobro se muda a la base)

  · INCLUYE y42 y y43. Se sube este solo.

  · EL PROBLEMA QUE ARREGLA. Prender el cobro estaba en sitio.json -que
    viaja en el zip- y el freno de verdad en sumar_simulacion. Dos lugares
    para una sola decision. Y peor: la fecha en que conviene prenderlo no la
    decidimos nosotros, depende de que Google apruebe el paquete con su
    facturacion. Hasta que eso pase, adentro de la app instalada NADIE puede
    pagar, asi que frenar a la gente seria ponerle una puerta sin llave.

  · Ahora el interruptor es una fila de la tabla `ajuste` y se prende con UNA
    LINEA en el editor SQL, sin zip, sin publicar y sin esperar a nadie:

        update ajuste set valor = 'si', cambiado = now() where clave = 'cobra';

    Arranca en 'no'. Apagado, el contador CUENTA todo igual -las
    simulaciones y las ligas tomadas- y no frena a nadie: el dia que se
    prenda, el contador ya viene con la verdad adentro.

  · `mi_cupo` y `sumar_simulacion` devuelven ademas `cobra`, y la app frena
    por eso y no por sitio.json. El booleano del zip quedo de respaldo para
    el rato en que todavia no hubo ninguna respuesta del servidor. Los
    precios se siguen viendo: eso lo decide `cobrando`, que queda en true.

  · 259 casos del navegador, 55 del backend, 43 de pagos.

Arma el 11 - version 2026-09-18 y43 (Mercado Pago en la web, Google Play adentro de la app; un aviso cada dos simulaciones)

  · INCLUYE TODO LO DE y42 (que no llego a subirse): el cobro por ligas en
    vez de por cantidad de simulaciones, los planes liga / tres / todas, y
    los dos interruptores prendidos. Se sube este y listo.

  · DONDE SE PAGA. Google exige su facturacion para todo bien digital
    vendido ADENTRO de una app publicada en Play, y prohibe mandar al usuario
    a pagar afuera. Un checkout de Mercado Pago adentro de la app es motivo
    de baja: en la prueba cerrada nadie mira, al pedir produccion si. Asi que
    Mercado Pago queda para la web y Play para la app.

  · La app se da cuenta sola de donde esta corriendo: el envoltorio de Play
    abre el sitio con el referrer `android-app://<paquete>`, que solo viene
    en la PRIMERA navegacion, asi que se anota la primera vez y despues se
    recuerda. Sin eso, la segunda pantalla adentro de la app se creeria la
    web y ofreceria Mercado Pago.

  · Lo que se compro en la web sirve igual adentro de la app: el derecho vive
    en NUESTRO servidor, no en la tienda. Lo unico que cambia es por donde
    entra la plata.

  · funcion-pago-play.ts (nueva) confirma la compra con Google antes de
    acreditar: al comprobante del telefono no se le cree, se le pregunta a la
    API de Google Play con nuestra clave, y recien ahi se llama al mismo
    `registrar_pago` que usa Mercado Pago. Despues la consume, que reconoce
    la compra (Google la devuelve a los tres dias si nadie lo hace) y ademas
    deja volver a comprarla el mes que viene. Nueve pruebas nuevas.

  · Si la facturacion de Play todavia no esta disponible -la version
    instalada es anterior o los productos no estan de alta-, los botones
    quedan apagados con el motivo escrito. Lo que NO se hace es ofrecer
    Mercado Pago ni poner un link a pagar afuera.

  · Los pasos de la Play Console y de Google Cloud estan en COBRAR.md,
    seccion 7.

  · UN AVISO CADA DOS SIMULACIONES para el gratuito. Antes era solo tiempo
    -uno cada sesenta segundos-, que salia parecido pero dependia del ritmo
    de cada uno y en el modo sin animacion salia mucho menos seguido. Ahora
    se cuenta lo que se quiere contar: simulaciones. La primera sigue sin
    aviso -esa es la que decide si la app le gusta-. Los sesenta segundos
    quedan de piso porque no son eleccion nuestra: es el minimo de AdSense.
    Y si un aviso se saltea por ese piso, la cuenta sigue desde donde quedo
    en vez de desfasarse para siempre.

  · 258 casos del navegador, 43 de pagos, 55 del backend.

Arma el 11 - version 2026-09-18 y42 (se dejan de vender simulaciones y se venden LIGAS; el cobro se prende)

  · EL MODELO NUEVO. Simular no cuesta nada -corre en el telefono-, asi que
    cobrar por cantidad cobraba por algo que no existe y castigaba justo al
    que mas usa la app. Lo que vale para el hincha es CUANTAS LIGAS puede
    tocar:

      gratis  10 simulaciones por mes, en UNA liga
      liga    sin limite en UNA liga          US$2  ($3.000)
      tres    sin limite en TRES a eleccion   US$5  ($7.500)
      todas   sin limite en las once          US$8  ($12.000)

  · CUAL ES "TU LIGA" NO SE ELIGE EN NINGUNA PANTALLA: es la primera que
    simulas en el periodo, y queda tomada hasta que el periodo termina. Sin
    selector y sin arrepentimiento. El que paga tres las va tomando a medida
    que las usa. Las ligas tomadas viven en `uso_ciclo`, asi que se liberan
    solas cuando empieza el ciclo siguiente.

  · EL QUE MANDA ES EL SERVIDOR. `sumar_simulacion` ahora recibe la liga y
    corta con 'otra liga' o 'sin cupo' ANTES de sumar, asi que un no del
    servidor no gasta ninguna simulacion. La pantalla lo dice antes, pero si
    su foto del cupo estaba vieja -otra pestaña, otro telefono- el servidor
    gana y la app explica cual de los dos casos fue.

  · SE PRENDIERON LOS DOS INTERRUPTORES de sitio.json: `bloquea` y
    `cobrando`. Estuvieron apagados los catorce dias de la prueba cerrada.
    Van junto con el freno del servidor: apagar uno solo hace que la pantalla
    deje apretar y la base conteste que no, que es peor que las dos.

  · Los precios en pesos salen de US$ 2, 5 y 8 con el dolar a $1.500, y viven
    en UN SOLO lugar (funcion-crear-pago.ts). Si el dolar esta en otro lado
    se cambian esos tres numeros y se vuelve a desplegar: en la app no hay
    ningun precio escrito.

  · Los planes viejos ('chico','medio','libre') se convierten a 'todas' antes
    de poner la regla nueva: el que pago algo no puede terminar con menos por
    un cambio nuestro. Hoy no hay ninguno, pero el archivo se corre igual en
    cualquier base.

  · 256 casos del navegador (once nuevos, todos sobre la liga), 55 del
    backend, 34 de pagos. HAY QUE PEGAR DE NUEVO LAS DOS EDGE FUNCTIONS: los
    planes nuevos viven ahi.

Arma el 11 - version 2026-09-17 y41 (la medida de cada liga, dicha por el lado del acierto)

  · Las etiquetas pasan de la jerga del backtest al idioma del hincha:
    "le gana al promedio" -> "Indice de acierto mas alto", "le gana poco"
    -> "Indice de acierto alto", y "casi no le gana" -> "Liga mas
    impredecible". La ultima cambia de sujeto a proposito: nombra a la LIGA
    y no al modelo, porque Argentina no es un problema del modelo, es la
    liga mas pareja de las once, y eso cualquier hincha lo reconoce.

  · Lo que NO cambio es el numero que decide el escalon. Y el orden coincide
    con el acierto real en todas las ligas donde lo tenemos medido: Peru
    54,4%, Portugal 53,9%, Alemania 52,0%, Mexico 51,5% en el escalon de
    arriba; Brasil 49,7% y Colombia 46,5% en el del medio. La regla que
    queda escrita: si un escalon deja de coincidir con su etiqueta, se
    cambia la etiqueta, no el numero.

  · Abajo de la lista, estadistica dura en vez de la explicacion de que era
    "el promedio historico": contra cuantos partidos se probo el modelo (mas
    de 12.000, de doce ligas) y que se recalibra con cada temporada. El
    numero va redondeado hacia abajo a proposito: un numero que se queda
    corto no hay que salir a defenderlo nunca.

  · Se probo a 390px: las tres etiquetas entran en una linea y ningun nombre
    de liga se corta. Las pruebas del navegador fijan las tres etiquetas, la
    estadistica de abajo, y que la app no prometa acertar el proximo
    partido, que es la frontera que no se cruza.

Arma el 11 - version 2026-09-17 y40 (que la app se recupere sola de quedarse sin ligas)

  · y39 arreglo que las ligas no se borren, pero no alcanzaba para VOLVER:
    el paso ya estaba sellado del dia anterior -se sello habiendo publicado
    la lista vacia- y el sello dura 24 horas, asi que todas las rondas lo
    saltearon prolijamente. La unica salida era que una persona apretara
    "Run workflow". Depender de eso es el mismo error de siempre.

  · Un sello vale por lo que produjo, no por la hora a la que corrio. Ahora,
    si la lista de ligas publicada esta VACIA, el paso se rehace aunque el
    sello este fresco. La app se recupera sola en la ronda siguiente.

  · `listaVacia()` vive en frescura.mjs y tiene sus cuatro pruebas: un
    archivo ilegible no cuenta como vacio (fallar hacia bajar de mas es
    caro; fallar hacia no bajar deja la app rota, que es peor).

Arma el 11 - version 2026-09-17 y39 (las once ligas en la pantalla, con lo que el modelo rinde en cada una; y que nunca mas se quede sin ninguna)

  · LA APP SE QUEDO SIN LIGAS, Y FUE UNA CADENA DE ERRORES MIOS. Al sumar
    cuatro ligas seguidas, cada una disparo una publicacion con
    `gh workflow run`. Para GitHub eso es un workflow_dispatch, y
    publicar.mjs tiene escrito que un workflow_dispatch -"alguien apreto Run
    workflow"- trae TODO de nuevo ignorando los sellos. Cuatro corridas
    completas forzadas en una hora, ~1.200 pedidos cada una, y la cuota
    diaria de 7.500 se acabo. Con la cuota agotada, ligas-api.mjs -que
    BORRABA los archivos viejos antes de bajar los nuevos- borro las seis
    ligas y no pudo escribir ninguna: la app publico LIGAS_DISPONIBLES=[].

  · Dos arreglos. Uno: el que dispara la publicacion desde el backtest es el
    bot, y GitHub dice quien fue (GITHUB_TRIGGERING_ACTOR); si es el bot, es
    una ronda normal. La puerta de emergencia queda solo para una persona.
    Dos, y es la regla general: PRIMERO SE CONSIGUE LO NUEVO, DESPUES SE
    TIRA LO VIEJO. Ahora solo se borra la liga que salio de ligas.json; si
    una no se pudo bajar, queda la de la corrida anterior y el log lo avisa.
    Cero ligas corta con error para no sellar el paso y reintentar.

  · LAS ONCE EN LA PANTALLA. La tira que se corria al costado mostraba
    cuatro de seis; con once era peor. Ahora es una lista agrupada por zona
    -America, Europa- con la bandera de cada pais. Cinco banderas nuevas
    (Alemania, Brasil, Colombia, Mexico, Peru); la de Mexico lleva un
    circulo en el medio porque sin eso es igual a la de Italia.

  · Y LA APP DICE CUANTO RINDE EL MODELO EN CADA LIGA. El numero estaba
    medido y guardado desde agosto y no se mostraba. Tres escalones: "le
    gana al promedio", "le gana poco", "casi no le gana". Argentina dice lo
    ultimo, que es la verdad: es de las mas parejas que medimos. Decir solo
    lo bueno seria marketing; decir las dos cosas es lo que hace creible a
    lo bueno. Abajo, que es "el promedio historico", sin jerga y sin
    prometer acertar nunca.

  · ligas.json queda con las once, con nombre y pais en castellano
    (Brasileirao, Liga MX, Bundesliga...) y su zona. Lo que llegue del
    backtest de ahora en mas se traduce solo al sumarse.

  · 247 casos del navegador (ocho nuevos) y 36 del backtest.

Arma el 11 - version 2026-09-16 y38 (que sumar una liga funcione aunque el workflow sea el viejo)

  · El arreglo de y37 obliga a editar a mano .github/workflows/backtest.yml,
    que no sube arrastrando. Fausto lo edito dos veces y las dos veces cayo
    en la COPIA de la raiz -mismo nombre, un directorio mas arriba-: el
    segundo commit dice "0 files changed" porque pego encima lo mismo que ya
    habia dejado el zip. El workflow de verdad seguia en su primera version.
    Culpa del diseno, no de el: la cadena entera colgaba de un archivo
    escondido en una carpeta con punto.

  · Ahora backtest.mjs, cada vez que suma una liga, escribe tambien
    backtests/sumadas.json (que liga entro y cuando). Con eso la carpeta
    backtests/ EXISTE siempre que haya algo para sumar, y aquel
    `git add ligas.json backtests/` de la version vieja del workflow ya no
    aborta: la liga entra igual, con el yml viejo o con el nuevo.

  · Dos arreglos para el mismo agujero, a proposito: el de y37 hace que la
    corrida FALLE cuando dice que sumo y no sumo; este hace que no falle. El
    yml nuevo sigue valiendo la pena, pero ya no es el unico camino.

  · El registro sirve por si mismo: queda escrito que liga se sumo, cuando y
    con cuanta ventaja. Dos pruebas nuevas (34 en total).

Arma el 11 - version 2026-09-16 y37 (el partido que se mira, sin rarezas; la liga que se suma, sin perderse)

  · EL MARCADOR QUE SE VE. "Que dificil estos resultados, algo se
    descalibro": un 6-1 y un 5-2 en LaLiga, con 1,66 y 1,54 de gol
    esperado. Nada estaba roto -la barra y el xG eran correctos, y el
    partido dibujado es UNA tirada: con 1,66 esperados, meter cinco o mas
    pasa el 2,7% de las veces y un 6-1 exacto es 1 de cada 500-. Pero el
    7% de los partidos dibujados eran marcadores de menos de 1 en 100, y
    cada uno de esos le rompe la confianza a quien lo ve, justo en una app
    cuya promesa es "no tira un dado".

  · Ahora se podan: si el marcador sorteado sale menos de una vez cada
    cien, se sortea otro. Y se poda SIN mover la barra: el que se vuelve a
    tirar tiene que terminar igual -ganas, empatas o perdes-, asi que
    ganar/empatar/perder sale exactamente de la distribucion sin podar y la
    barra sigue describiendo lo que se ve. (Sortear de nuevo a secas subia
    los empates punto y medio y bajaba las visitas otro tanto: las colas no
    son simetricas.) En partidos de muchisimos goles esperados el piso baja
    solo, porque ahi un 4-3 es normal. Hay pruebas de las dos cosas.

  · Y la pantalla lo cuenta: abajo del marcador dice "este sale 1 de cada
    N", y una nota explica que el insolito se vuelve a sortear. Contar la
    rareza convierte la sorpresa en dato en vez de en desconfianza.

  · LAS TRES LIGAS QUE SE PERDIERON EN SILENCIO. Peru, Mexico y Colombia
    pasaron el backtest, se apreto "Sumar a la app", la base las marco
    sumadas y la pantalla dijo "en la app". En el repo no habia pasado
    nada: ligas.json sin tocar desde el 30/8 y la app con seis ligas.

    El workflow agregaba las dos rutas juntas (ligas.json y backtests/) en
    un solo git add. La carpeta backtests/ todavia no existe en el repo, y
    cuando una ruta no existe git add ABORTA sin agregar NADA, ni siquiera
    lo que si habia cambiado. El "|| true" tapaba el error, el commit salia
    vacio y el paso de publicar igual se disparaba: por eso hubo una
    corrida de "Traer y publicar" lanzada por el bot que publico lo mismo.

    Ahora cada ruta se agrega por separado y, si el backtest dijo que sumo
    una liga y no quedo nada para commitear, la corrida FALLA. Un error a
    la vista antes que una liga que se pierde callada.

  · Y la pantalla del backtest deja de creerle a la base: "en la app" sale
    del ligas.json PUBLICADO (los ids se escriben al construir el sitio).
    Si la base la dio por sumada y en la app no esta, lo dice -"pedida,
    todavia no esta"- y deja el boton para volver a pedirla. Eso es lo que
    hay que apretar para Peru, Mexico y Colombia.

  · HAY QUE EDITAR backtest.yml EN GITHUB (no sube arrastrando): lapiz,
    seleccionar todo, pegar el backtest.yml de este zip.

Arma el 11 - version 2026-09-16 y36 (la pantalla del backtest, en el sitio)

  · La pantalla del backtest se MUDA a armael11.com/backtest.html. La
    version en claude.ai no podia hablar con Supabase: las paginas
    publicadas alla corren en un sandbox que bloquea cualquier pedido a
    otro servidor, sin error a la vista. Error mio. En el sitio no hay esa
    traba. Se arma desde backtest.tpl.html con las dos claves publicas de
    sitio.json; hay prueba de que se publica y de que no lleva ninguna
    clave de servidor. Todo lo demas es igual que y35.

Arma el 11 - version 2026-09-16 y35 (la portada es Arma el 11; el backtest desde GitHub)

  · LA PORTADA ES ARMA EL 11. Con la devolucion de la prueba cerrada en la
    mano -"la principal funcion es simular"- la portada deja de ser la
    grilla de clubes y pasa a ser el simulador: la misma plantilla de la
    app, en modo PORTADA (window.PORTADA). El 11 abre con el resumen de dos
    lineas ("Tocas una perilla y se mueve el resultado" / "Cada partido se
    juega 6.000 veces... esta medido en 10.860 partidos") y la lista de
    ligas; el Feed es ahora "Tu club": la grilla de clubes argentinos, con
    el recordado primero. Numeros sigue siendo la tabla. Los datos de la
    portada van en datos/portada.js, como el feed de un club.

  · Ya no redirige al club recordado (la portada es el simulador). El club
    recordado aparece como tarjeta "Tu club: Boca" arriba de todo, con link
    a su pagina. "Cambiar de club" del pie sigue yendo a index.html?elegir,
    que abre directo en la grilla. El rescate del link del mail que tenia
    la portada vieja vive ahora adentro de la app.

  · La portada NO promete acertar ni eficacia (no esta medido en Argentina)
    y dice cuantas simulaciones son gratis. Hay pruebas que fijan las dos
    cosas y los cuatro numeros del respaldo.

  · EL BACKTEST DESDE GITHUB. backtest.mjs reescribe la prueba que se les
    hizo a las nueve ligas (modelo v3.1: goles, k=6, arrastre 0,55,
    Dixon-Coles -0,10, minimo 6 previos) para correr sola en Actions. La
    pantalla es el formulario "Run workflow" de backtest.yml: ligas (ids, o
    buscar:Pais), temporadas, y "sumar". El informe sale en el resumen de
    la corrida con el veredicto y si la liga trae rating por jugador (sin
    eso no se arma el once). Con "sumar", las que le ganan a la vara van a
    ligas.json y se dispara la publicacion. probar-backtest.mjs (22 casos)
    verifica que encuentra la senal cuando la hay y que NO la inventa
    cuando no la hay.

  · backtest.yml SE CREA EN GITHUB: Add file > Create new file, nombre
    .github/workflows/backtest.yml, pegar el contenido del backtest.yml de
    la raiz. La primera corrida conviene hacerla con Portugal (94), que
    tiene que dar cerca de +0,08 y t 8, como en el documento.

  · sw.js pasa a v4 para que la portada nueva reemplace a la vieja en los
    telefonos que la tenian guardada. Pie con Privacidad y Borrar mi cuenta
    en todas las paginas.

  · Y35 (agregado el mismo dia): EL BACKTEST TIENE PANTALLA. Un HTML en
    claude.ai ("Backtest de ligas") donde se escriben las ligas, las
    temporadas y la clave de administrador; el pedido queda en Supabase
    (backtest_pedido, via pedir_backtest, que valida y frena el abuso), y
    backtest.yml -que ahora corre cada quince minutos- lo levanta con las
    claves de verdad, corre el backtest y deja el resultado en
    backtest_resultado, que la pantalla lee sola. "Sumar a la app" es otro
    pedido: si la liga paso, va a ligas.json y se publica. HAY QUE: pegar
    esquema.sql (dos tablas y una funcion nuevas), crear el secreto
    BACKTEST_CLAVE en GitHub (una frase cualquiera, la misma que se escribe
    en la pantalla) y crear .github/workflows/backtest.yml.

  · Sigue pendiente de y32/y33: esquema.sql, VAPID_PRIVATE_KEY, el yml del
    workflow y funcion-crear-pago.ts, si todavia no estan.

Arma el 11 - version 2026-09-10 y34 (el club se elige una vez; el boton Simular siempre a la vista)

  · EL CLUB SE ELIGE UNA VEZ. La app instalada arranca en la portada, y la
    portada preguntaba el club en CADA apertura: un peaje antes de llegar
    a la pantalla de armar. Ahora la pagina de un club se anota en el
    telefono (armaEl11.club) y la portada, si lo encuentra, manda derecho a
    esa pagina antes de dibujarse. No hace falta reempaquetar la app de
    Play: el start_url sigue siendo "/", la redireccion la hace la pagina.
    Solo manda a clubes que existen en el sitio (un club anotado sin pagina
    no puede mandar a un 404). Para cambiar, el pie de la app tiene
    "Cambiar de club": abre la portada con ?elegir, que ahi no redirige y
    dice "Ahora estas en X. Toca otro para cambiar, o volver a X".

  · La campanita sigue al club. Un telefono es una suscripcion y una
    suscripcion es un club: el que cambia de club ve la campanita apagada
    en la pagina nueva aunque la suscripcion exista, y tocarla la pasa al
    club nuevo (anotar_aviso pisa la fila). Antes se veia prendida y los
    avisos seguian llegando del club viejo.

  · EL BOTON SIMULAR SIEMPRE A LA VISTA. Los testers dijeron "engorroso" y
    el motivo era el scroll: cinco pantallazos de perillas antes de poder
    simular. La caja del boton es sticky al pie: se ve desde el primer
    segundo, pegada arriba de la barra, con el renglon "con que estas
    simulando" debajo; recien cuando el scroll llega a su lugar natural
    -el final de los ajustes- se queda ahi y deja ver el resultado. La
    eleccion "ver el partido / solo el resultado" quedo en su lugar de
    siempre, arriba de la caja, que una preferencia no tiene por que
    viajar pegada al pie. Mientras se ve el partido la caja se suelta:
    taparia el tanteador del campo y no hay nada que apretar.

  · Pruebas nuevas en probar-sitio (226): la caja es sticky y esta en
    pantalla con la pagina arriba de todo; el club queda anotado; la
    portada redirige; ?elegir no; un club sin pagina no redirige; el pie
    tiene "Cambiar de club".

  · No cambia nada en Supabase ni en el workflow. Sigue pendiente todo lo
    de y32/y33: pegar esquema.sql, el secreto VAPID_PRIVATE_KEY, el yml y
    funcion-crear-pago.ts.

Arma el 11 - version 2026-09-08 y33 (la campanita: "new row violates row-level security policy for table aviso")

  · El error del telefono al tocar "Avisame cuando salga el once del DT".
    La tabla `aviso` tenia politicas de INSERT, UPDATE y DELETE para el
    anonimo, pero ninguna de SELECT, a proposito: nadie tiene por que leer
    las suscripciones de los demas. Lo que no sabia es que Postgres, para
    hacer un UPDATE o un DELETE con WHERE -y para el ON CONFLICT DO UPDATE
    de un upsert- primero tiene que LEER la fila que ya esta, y esa lectura
    pasa por la politica de SELECT. Sin ella, la fila es invisible, el
    upsert cree que es nueva, va por el INSERT... y ahi choca. Por eso el
    mensaje habla de "new row".

  · La salida NO es abrir el SELECT: seria regalar la lista de telefonos
    suscriptos a cualquiera con la clave anon. Se hace lo mismo que con
    crear_liga y sumar_simulacion: dos funciones `security definer`,
    `anotar_aviso(endpoint, claves, club)` y `borrar_aviso(endpoint)`, que
    corren con permisos del dueno y validan lo que les llega (endpoint
    https de hasta 2000 caracteres, claves con p256dh y auth, club con el
    formato de los ids). La tabla queda con RLS prendido y SIN politicas: el
    anonimo no puede tocarla directo de ninguna forma, solo a traves de
    esas dos puertas. La clave de servicio, que usa el workflow para mandar
    los avisos, salta RLS como siempre.

  · cuentas.js: activar/desactivar llaman a /rest/v1/rpc/anotar_aviso y
    /rest/v1/rpc/borrar_aviso en vez de escribir la tabla. Las pruebas
    esperan eso (38/38) y el backend cuenta 14 funciones security definer
    (50/50).

  · HAY QUE VOLVER A PEGAR esquema.sql en el proyecto wbqxmoerzofzierurxfb
    (el de siempre, no el otro). Es idempotente: borra las tres politicas
    viejas si estan y crea las funciones. Nada mas cambia en la base.

  · Todo lo pendiente de y32 sigue pendiente: secreto VAPID_PRIVATE_KEY,
    el yml del workflow con el lapiz de GitHub, y funcion-crear-pago.ts.

Arma el 11 - version 2026-09-07 y32 (salio el once del DT: antes del partido, y al telefono)

  · Fausto: "la simulacion del DT deberia poder hacerse antes del partido;
    despues ya no tiene gracia". Tenia razon y el problema era de reloj: las
    formaciones oficiales aparecen en la API una hora antes del pitazo, y la
    corrida completa pasaba cada tres horas y solo las bajaba de los partidos
    ya terminados. Para cuando el sitio las tenia, el partido habia terminado.

  · LA RONDA CORTA. El workflow pasa de cada tres horas a cada QUINCE
    MINUTOS, pero no para bajar todo: `formaciones.mjs` mira, club por club,
    si el proximo partido esta por empezar -entre dos horas antes y una
    despues- y si si, pide la formacion. Si la API la tiene, la mete en el
    cache del club con la MISMA clave que la app ya busca, y publica. Si no,
    termina en veinte segundos y no publica nada. Cuesta unos cien pedidos
    en un domingo de ocho partidos, sobre 7.500.

  · La COMPLETA sigue siendo cada tres horas, y se decide por sello y no por
    reloj: GitHub atrasa las corridas programadas diez, veinte, treinta
    minutos, asi que "la de las 9 en punto" a veces corre a las 9:25. La
    primera ronda que pasa despues de dos horas cincuenta desde la ultima
    completa, es completa. Subir un zip o Run workflow siempre es completa.

  · datos-juego.mjs ahora tambien pide la formacion del PROXIMO partido -un
    pedido por club-, asi cuando rehace el cache no pisa lo que la ronda
    corta habia metido.

  · EN LA APP. Cuando la formacion esta, la tarjeta del resultado dice
    "Salio el once del DT" y ofrece simularlo. Se simula con la misma cuenta
    de siempre, se muestran las dos columnas -tu once, el del DT- y la
    pregunta abierta: "con tu once el modelo te da 6 puntos mas de ganar que
    con el del DT". La tercera columna, lo que paso, llega cuando termina. No
    gasta del cupo. La seccion se llama "El once del DT" y no "La realidad",
    porque todavia no paso nada.

  · Y EL AVISO AL TELEFONO. Un boton en la tarjeta del partido que viene:
    "Avisame cuando salga el once del DT". Web Push, sin ninguna libreria:
    este proyecto no tiene node_modules y no lo va a tener por una funcion.
    Lo que hace `web-push` es criptografia estandar que Node trae adentro:
    una firma ES256 para probar que el que manda somos nosotros (VAPID) y un
    cifrado con la clave del telefono para que ni Google ni Mozilla puedan
    leer el mensaje (RFC 8291). Son sesenta lineas en `avisos.mjs`.

  · Y SE PUEDE PROBAR QUE ESTA BIEN SIN MANDAR NADA: el RFC 8291 trae un
    ejemplo completo con todas las claves y el resultado esperado byte por
    byte. probar-avisos.mjs lo reproduce: identico. Es la forma mas fuerte
    de probar criptografia que existe. Tambien se verifica la firma con la
    clave publica, como hace Google, y el envio con una red de mentira: la
    suscripcion que contesta 410 se borra.

  · LO QUE SE GUARDA de cada telefono es lo minimo: la direccion que le dio
    Google o Mozilla, las dos claves de cifrado, y el club. Sin usuario, a
    proposito: el aviso no necesita cuenta y con eso no se puede saber quien
    es la persona. Tabla `aviso`, seccion 12 de esquema.sql. Nadie la lee
    desde el navegador: la lee el workflow con la clave de servicio.

  · TRES COSAS QUE HAY QUE HACER A MANO, en este orden (estan en el chat):
      1. El secreto VAPID_PRIVATE_KEY en GitHub, con el contenido de
         vapid-privada.txt. Ese archivo NO va al repo.
      2. Pegar esquema.sql en Supabase (tabla `aviso`).
      3. Editar .github/workflows/publicar.yml EN GITHUB, con el lapiz,
         pegando el contenido de publicar.yml de la raiz.

  · El service worker sube a v3 para que los telefonos tomen el nuevo
    enseguida. Pruebas nuevas: 13 en formaciones, 15 en avisos, 11 en el
    navegador (218) y 4 en la cuenta (38). Todo en verde.

Arma el 11 - version 2026-09-07 y31 (tu once, el del DT, lo que paso)

  · Fausto, por segunda vez: "armo el mio, lo simulo, armo las formaciones
    oficiales, simulo, y despues veo el resultado final. Sigo sin verla ni
    estar claro". Tenia razon en lo que faltaba: el paso del medio. Al
    revelar se mostraba el once del DT pero NO SE LO SIMULABA. Entonces la
    comparacion era "mi simulacion contra el resultado", que es la mitad de
    la gracia. La gracia entera es "con los mios ganabamos?", contestada con
    un numero.

  · AHORA AL REVELAR SE SIMULA EL ONCE DEL DT. Se arma con los mismos
    jugadores del plantel -asi tienen su nivel-, en los puestos de la
    formacion oficial, y se corre la MISMA cuenta que corrio el tuyo: mismo
    rival, mismas perillas, mismas indicaciones, semilla propia. Lo unico que
    cambia es quien juega. Un juvenil que debuta y no esta en el plantel
    entra con el nivel medio de la liga, que es lo que el modelo sabe de el.
    No gasta del cupo: es la segunda mitad de la simulacion que ya pagaste.

  · TRES COLUMNAS: tu once, el del DT, lo que paso. Ganas / empatas / perdes,
    el marcador mas probable y el xG, con un tilde en la fila que se dio.

  · QUIEN LEYO MEJOR EL PARTIDO. No "quien acerto el ganador" -con tres
    resultados posibles es casi una moneda- sino cuanto le daba cada once a
    LO QUE PASO. "Tu once leyo mejor: le daba 34% a lo que paso; el del DT,
    27%". Con menos de tres puntos de diferencia, empate tecnico.

  · El calculo del gol esperado salio de adentro de `simular11` a una
    funcion propia, para poder correrlo con otro once. `simular11` no cambio
    de comportamiento.

  · Los apellidos: "G. Baez Corradi" es Baez Corradi; "Santiago Fernandez"
    es Fernandez. Antes las dos reglas se pisaban.

  · Cinco pruebas nuevas en el navegador (209): que se simula el once del
    DT con sus once, que los porcentajes suman cien, que la tabla tiene las
    dos columnas, que dice quien leyo mejor, y que no gasta del cupo.

Arma el 11 - version 2026-09-05 y30 (la bajada de a pocas)

  · Las corridas automaticas venian fallando una si y una no. El log decia:
    94 de 148 fuentes con "fetch failed", el freno de mano cortaba -"NO
    escribo nada, para no pisar los feeds que ya estaban"- y la corrida
    terminaba en rojo a los 45 segundos. El freno hizo su trabajo: el sitio
    nunca se publico roto. Pero se quedo sin refrescar la mitad de las veces.

  · "fetch failed" no es un 404 del medio: es una falla de red DE NUESTRO
    LADO -sin DNS, conexion cortada-. Y 94 medios distintos no se caen
    juntos. Lo que si se cae junto es una rafaga: `traerTodas` disparaba las
    148 fuentes al mismo tiempo, y el resolvedor de DNS de la maquina de
    GitHub no aguanta 148 consultas en el mismo instante.

  · DOS ARREGLOS. Las fuentes se piden de a dieciseis, no todas juntas:
    tarda unos segundos mas y no rompe nada. Y si igual muchas fallan por
    red, se espera veinte segundos y se vuelven a pedir SOLO esas, de a seis.
    Un 404 o un feed vacio no se reintentan: esos son del medio, y pedirlos
    de nuevo no cambia nada.

  · Cinco pruebas nuevas en probar.mjs: que el tope de concurrencia se
    respeta, que el orden se conserva aunque terminen desordenadas, y que se
    distingue una falla de red de una del medio.

  · Lo que NO cambio: el freno de mano sigue ahi. Si despues de la segunda
    vuelta contesta menos de la mitad, no se publica. Ese dia el problema es
    otro.

Arma el 11 - version 2026-09-03 y29 (tu once contra el del DT)

  · Fausto: "estaba la opcion de elegir tu once, despues poner el once que
    eligio el tecnico y despues contrastar ambos con el resultado. Ahora no la
    veo". Estaba -en los partidos ya jugados, el boton "Revelar el once del
    DT"- pero se habia vuelto invisible por dos lados: los renglones de
    jugados no decian para que tocarlos, y al revelar salia una lista de once
    nombres. Una lista no es una comparacion.

  · AHORA SE CRUZAN LOS DOS ONCES. "Tu once contra el del DT · 6 de 11". Las
    dos formaciones, la tuya y la del DT. Y tres grupos de fichas: el DT puso
    y vos no, vos pusiste y el DT no, los dos. Se cruza por id cuando la API
    lo trae -siempre, en las formaciones reales- y por apellido cuando no.

  · EL VEREDICTO DICE LAS DOS COSAS: que decia tu simulacion (los tres
    porcentajes, y el marcador si viste el partido) y que paso. Y cuando tu
    once no era el del DT, lo aclara: la simulacion es de LO QUE VOS
    ELEGISTE. Ese es el juego entero en una frase.

  · Los partidos jugados ahora lo dicen arriba: "Arma tu once, simula, y
    compara con el once que puso el DT y con lo que paso de verdad". Y la
    tarjeta del partido futuro promete lo mismo para cuando vuelvas con el
    link.

  · Los apellidos compuestos no se cortan: "G. Baez Corradi" es Baez Corradi,
    no Corradi. Se sacan las iniciales de adelante y se deja el resto.

  · Seis pruebas nuevas en el navegador (205): que los jugados invitan a
    comparar, que el resultado ofrece revelar, que al revelar aparece la
    comparacion, que los grupos suman once, que estan las dos formaciones y
    que el veredicto dice que decia la simulacion.

Arma el 11 - version 2026-09-03 y28 (no es un dado)

  · Un tester grabo un video: doce veces "Simular" seguidas, sin tocar nada,
    con "Solo el resultado". Doce marcadores distintos: 4-4, 1-1, 0-3, 4-1,
    0-0... Los porcentajes casi no se movian -eso ES el modelo-, pero el
    numero grande cambiaba cada vez, y el numero grande es lo que se mira.
    Conclusion del que mira: "es un dado que siempre cae distinto". Justo lo
    que la portada promete que no es.

  · EL ERROR ERA NUESTRO, NO DEL TESTER. En el modo sin animacion el numero
    grande era UNA realizacion suelta -uno de los 6.000 partidos- presentada
    como "el resultado". Cuando se mira el partido eso tiene sentido: viste
    un partido, ese es su marcador. Cuando no se mira, es una tirada
    disfrazada de respuesta.

  · TRES ARREGLOS, y los tres apuntan a lo mismo: que simular sea una cuenta
    que se repite, no una tirada.

    1. SEMILLA. Las 6.000 simulaciones se sortean con un generador con
       semilla, y la semilla sale de los datos: el partido, los dos onces, las
       formaciones, las perillas de los dos, las indicaciones, desde donde. Los
       mismos ajustes dan los mismos porcentajes hasta el ultimo decimal;
       cambias una perilla y cambian. El partido que se MIRA sigue siendo al
       azar a proposito: es uno de los 6.000, y ver otro distinto con las
       mismas probabilidades es exactamente lo que eso significa.

    2. EL NUMERO GRANDE ES EL QUE NO SE MUEVE. Sin ver el partido, el
       tanteador muestra el marcador MAS PROBABLE, con cuantas veces sale.
       Cuando se vio un partido, muestra ese, dice "uno de los 6.000 posibles"
       y debajo cual era el mas probable.

    3. NADA CAMBIO, NADA QUE SIMULAR. Apretar "Simular" sin haber tocado nada
       ya no gasta ni cambia: explica. "No cambiaste nada desde la ultima
       simulacion, asi que el resultado es el mismo: con los mismos datos el
       modelo da los mismos numeros." Y ofrece ver uno de los 6.000 partidos
       posibles, que si es otro. Tambien protege el cupo: el del video se
       comio las diez en veinticinco segundos.

  · La tarjeta lo dice con todas las letras: "Mismos ajustes, mismo
    resultado. Cambia algo y volve a simular para ver cuanto mueve". Que es
    ademas la invitacion correcta: la gracia del simulador es comparar.

  · Sin semilla, todo sigue como antes: el backtest y las pruebas viejas
    llaman a simular() igual que siempre y siguen dando lo mismo.

  · Ocho pruebas nuevas en el navegador (199) y siete en el motor (81).

  · APARTE, VISTO EN EL MISMO VIDEO: los tres planes decian "Sin limite" con
    precios $2.500, $6.000 y $20.000. Esos son los nombres de respaldo y los
    precios viejos: la funcion crear-pago que esta corriendo en Supabase es
    la anterior. Hay que pegar funcion-crear-pago.ts de nuevo en Edge
    Functions. No es de esta version; es un despliegue que quedo atras.

Arma el 11 - version 2026-09-03 y27 (dos caminos, dichos con todas las letras)

  · Fausto, sobre el flash: "no se entiende que es como una simulacion
    estandar, y que con la tradicional hay perillas que modifican el
    resultado". Tenia razon, y el error era de nombres: "Simular ya" y
    "Simular y verlo jugar" se leian como DOS simulaciones -una rapida y una
    lenta-, cuando la unica diferencia era si se mira. Y como las perillas
    estaban arriba de los dos botones, no habia forma de saber a cual le
    entraban. Les entraban a los dos.

  · LA DECISION QUE IMPORTA NO ES CUANTO TARDA: ES SI LO ARMAS VOS. Asi que
    el partido que viene dejo de ser un renglon y paso a ser una tarjeta con
    los dos caminos dichos por lo que hacen:
        Armar mi partido ›     el once, la formacion y el planteo son tuyos
        Simular tal cual       sin tocar nada, como viene
    La bajada de arriba lo dice en una frase: el once, la formacion y el
    planteo CAMBIAN EL RESULTADO.

  · ADENTRO, UN SOLO BOTON DE SIMULAR. Y un selector de como verlo: "Ver el
    partido (20 s)" o "Solo el resultado". Es un selector y no un segundo
    boton porque es lo que es: una eleccion de como mirarlo, no de que se
    simula. Con eso desaparece la duda de a cual boton le entran las
    perillas.

  · DEBAJO DEL BOTON DICE CON QUE SE VA A SIMULAR. Sin tocar nada: "Tal cual
    viene: el once que viene jugando, formacion 4-2-3-1 y planteo normal.
    Toca un jugador, la formacion o las perillas para cambiarlo: todo entra
    en la cuenta". Con algo tocado: "Con tus ajustes: 2 cambios en el once ·
    planteo: va a buscarlo · 1 indicacion". Se actualiza con cada toque. Es
    lo que hace visible la relacion entre mover una perilla y el numero que
    sale, que hasta ahora era un acto de fe.

  · Y LA TARJETA DEL RESULTADO LO REPITE. "Tal cual viene: ..." o "Con tus
    ajustes: ...". Si fue tal cual, ofrece "Cambiar el once o el planteo y
    volver a simular": ese es el momento en que el que vino por el atajo se
    entera de que las perillas existen. "El partido que acabas de ver" solo
    se dice si de verdad lo vio; si no, "el partido simulado".

  · Al elegir un partido se resetean las perillas, las indicaciones y la
    foto de lo automatico. Antes las perillas del partido anterior quedaban
    puestas para el siguiente sin que nadie lo viera.

  · "Los mejores" paso a decir "Volver a los que vienen jugando" y vive junto
    a las formaciones, que es de lo que habla.

  · Nueve pruebas nuevas en el navegador (191): que el partido que viene
    ofrece los dos caminos y los llama por lo que hacen; que hay un solo
    boton de simular y un selector; que la linea de "con que" dice tal cual
    sin tocar nada y cambia con una perilla; que el resultado repite con que
    se simulo y ofrece el camino a las perillas.

Arma el 11 - version 2026-09-03 y26 (el acabado, y el bloque que cruza)

  · LOS GLOBITOS VOLVIERON A QUEDARSE EN SU MITAD. Segunda vez la misma
    queja, y esta vez con la prueba en verde: con la pelota en el area rival,
    cuatro cruzaban. Pero MEDIDO EN EL PARTIDO ANIMADO -donde la pelota rara
    vez llega al fondo- el defensor mas adelantado llegaba a 64, el volante a
    46 y solo el delantero cruzaba. Cuatro que cruzan con la pelota en el
    area no es un equipo atacando: es un delantero y tres que asoman.

  · DOS CAUSAS. La fase era lineal: la pelota a veinte puntos de la mitad
    daba 0,4 de ataque, y como la pelota casi nunca va al fondo, el equipo
    atacaba "a medias" todo el partido. Ahora la pelota a un tercio del campo
    rival ya es fase entera: un equipo que tiene la pelota en campo rival YA
    esta volcado. Y los factores de estirado eran cortos: subieron a 1,0 /
    1,2 / 1,5 y el empuje a 30. Medido de nuevo en el partido animado: el
    defensor llega a 49, el volante a 28, el delantero a 12.

  · TRES PRUEBAS NUEVAS que miden lo que se ve: con la pelota en el area
    rival, TODOS los volantes y delanteros del otro lado, la linea de
    defensores pisando la mitad -no cruzandola: un central en el area rival
    es otro error-, y los volantes bien adentro.

  · EL PLANTEL PROPIO SE ARMABA CON UN PARTIDO. Aparecio buscando lo otro.
    "Los ultimos cinco" del club propio salian de la lista ya recortada a los
    dos partidos jugables, asi que eran UNO; el rival si tenia cinco. Con el
    cache real de Talleres: 16 jugadores con minutos y una aparicion cada
    uno, contra 26 y cinco del rival. Nivel de ruido, medio plantel "sin
    minutos" y el dibujo del ultimo partido en vez del habitual. Ahora los dos
    lados se arman igual, con sus cinco. Dos pruebas.

  · EL ACABADO. Todo lo de abajo sigue igual -colores del club, tokens, hoja
    con el patron-; cambio como se ve:
      TIPOGRAFIA PROPIA. Poppins en dos pesos, alojada en el sitio (veinte
        kilobytes) y no en Google Fonts: ningun pedido a un tercero por abrir
        la pagina. Va en lo que se lee de un vistazo -nombres, numeros,
        titulos, botones-; el texto corrido sigue en la letra del sistema.
      TARJETAS QUE SE APOYAN CON SOMBRA, no con un borde de un pixel. Radios
        de 10, 14 y 20. Nada mas.
      LA CANCHA COMO PIEZA CENTRAL: cesped con luz, lineas mas finas,
        globitos con anillo y sombra, el apellido en un cartel oscuro que se
        lee sobre cualquier color, y el marcador como una tira de television
        arriba del cesped, con el reloj en el medio.
      EL RESULTADO en numero grande. Los porcentajes en su barra.
      LA BARRA DE ABAJO con iconos dibujados en vez de simbolos de texto, y
        una marca arriba de la pestana activa.
      LA PORTADA con la marca sobre cesped y el gancho en oscuro, como una
        afirmacion y no como un parrafo.
      Botones con relieve y respuesta al toque; hojas que suben; la vista
        que entra con un desvanecido.

  · EL COLOR QUE EMPUJA. River es blanco, y un boton blanco sobre una pagina
    clara no es un boton, es un hueco. Para lo que tiene que resaltar
    -botones, chips prendidos, el monograma, la marca de la pestana- se elige
    entre los dos colores del club el que mas contrasta con el fondo. En
    Boca sigue siendo el azul; en River pasa a ser el rojo de la banda.

  · LAS CUATRO CAPTURAS DE PLAY se regeneraron con el acabado nuevo. Estan en
    png/play-*.png, listas para reemplazar las de la ficha.

  · TODO EN VERDE: 183 en el navegador, 78 en el fantasy, 34 en la cuenta, 74
    en el motor.

Arma el 11 - version 2026-09-03 y25 (el modo flash y el hueco que abre la lista)

  · Las dos quejas de los testers eran la misma dicha dos veces: "tarda mucho
    prepararla" y "me da paja ir hasta abajo para scrollear un jugador".
    Nadie dijo que no se entendia. Entendieron y no quisieron.

  · PRIMERO PASA EL PARTIDO, DESPUES SE MIRA. Estaban pegados: los goles se
    sorteaban adentro de la animacion, asi que la unica forma de saber como
    termino era mirar los veinte segundos, y la unica forma de volver a verlo
    era simular de nuevo -gastando otra del cupo por un partido que ya estaba
    pagado-. Ahora `generarPartido` sortea el guion y `mostrarPartido` lo pone
    en escena. De esa separacion salen gratis las dos funciones nuevas.

  · MODO FLASH. Un boton que simula con el once y el planteo que ya vienen
    puestos y muestra el resultado sin los veinte segundos. Y un atajo en la
    lista de partidos: "Simular ya" al lado del proximo hace de un toque lo
    que antes eran cuatro.

  · GASTA UNA DEL CUPO, como cualquier otra, Y EL BOTON LO DICE: "Simular ya
    · te quedan 7". Un contador al costado no lo mira nadie antes de apretar;
    en el boton, si. Un toque sin querer que se come una de diez es de las
    cosas que hacen desinstalar.

  · UNA SIMULACION ES UN PARTIDO, Y ESE PARTIDO SE MIRA LAS VECES QUE UNO
    QUIERA. Despues del flash aparece "Ver este partido", que no gasta nada y
    no mueve ningun numero: es el mismo partido. Si mirarlo costara otra, el
    que quiere ver lo que simulo pagaria dos veces por lo mismo.

  · EL AVISO PUBLICITARIO NO SE ROMPE. `tocaAviso` mide TIEMPO desde el
    ultimo, no simulaciones, asi que tres flashes seguidos no muestran tres
    avisos: muestran menos. Es un problema de ingreso, no de politica.

  · EL HUECO ABRE LA LISTA ENCIMA. Tocar un hueco de la cancha del fantasy ya
    filtraba la lista por ese puesto - pero la lista vive abajo de la cancha,
    del banco, del presupuesto y de los avisos. Filtrar algo que hay que ir a
    buscar no ahorra nada. Ahora abre una hoja como la del simulador, que
    hace exactamente esto desde el primer dia.

  · TRES SUGERIDOS, NO UNO, Y CON EL CRITERIO ESCRITO. Si la sugerencia es
    buena y todos la aceptan, los once equipos del torneo salen parecidos y
    el juego se convierte en un empate: una sugerencia lo bastante buena como
    para tomarla siempre le saca la gracia al juego. Y "recomendado" a secas
    no dice segun que, asi que no se puede discutir. Van tres, se ve si es
    por rinde, por puntos o por precio, y se cambia.

  · LOS QUE NO ENTRAN SE MUESTRAN, apagados y con el motivo: "te pasas por
    1,2", "ya tenes 3 de Boca". Esconderlos deja al que arma preguntandose
    por que no esta su jugador; mostrarlos con el motivo ensena la regla sola.

  · UNA PRUEBA NUEVA QUE NO ES DE ESTO Y VALE MAS QUE VARIAS. Van TRES veces
    que una pagina entera deja de arrancar por un nombre repetido:
    `tintaSobre`, `diaDe` y hoy `bv`. Dos const con el mismo nombre en el
    mismo bloque no dan un aviso: dan una pagina muerta. Y se ve horrible
    desde afuera: el navegador abria, ningun script corria, y la prueba
    fallaba con "tab is not defined" en un caso que no tenia nada que ver.
    Ahora, antes de abrir el navegador, se compilan los scripts de la pagina
    con `new Function` -que compila sin ejecutar- y el mensaje dice lo que
    pasa.

  · DOS PRUEBAS QUE VENIAN EN ROJO DE ANTES, arregladas: una exigia que al
    abrir no se le pidiera NADA al backend, y desde que los precios se
    muestran sin cuenta se pide la lista publica de planes. Se acoto a lo que
    de verdad importaba: que no se pida nada SOBRE LA PERSONA. La otra
    buscaba una frase de la oferta que cambio cuando entraron los tres
    planes; ahora exige lo que la frase tiene que decir, no como.

  · 178 pruebas en el navegador, 78 en el fantasy, 34 en la cuenta, 74 en el
    motor. Todas en verde.

Arma el 11 - version 2026-09-03 y24 (cada equipo con su dibujo)

  · Segunda devolucion: "toma todas las formaciones como 4-3-3, no las adapta
    al equipo". Era literal. El simulador arrancaba con esa cadena escrita a
    mano para los DOS equipos, y como la mayoria entra antes de que salgan las
    formaciones oficiales, esa era la que se veia casi siempre.

  · EL DATO YA ESTABA BAJADO. Cada /fixtures/players trae, por jugador, si fue
    titular y en que categoria jugo. Contar los titulares por categoria da el
    dibujo sin pedir nada nuevo: cero pedidos de mas.

  · CONTRA EL CACHE DE VERDAD:
      Talleres        3-4-3, 3-4-3, 4-2-3-1, 4-2-3-1, 4-4-2, 3-4-3  →  3-4-3
      Central Cordoba 4-4-2, 4-2-3-1, 4-2-3-1, 4-2-3-1, 3-4-3, 3-4-3 → 4-2-3-1
    Ninguno de los dos era 4-3-3.

  · SE TOMA LA QUE MAS SE REPITE, no la del ultimo partido. Un equipo que
    juega 4-4-2 todo el año y una vez se paro con tres atras porque iba
    perdiendo no cambio de dibujo. Si empatan, gana la mas reciente.

  · ELEGIR ENTRE 4-2-3-1, 4-5-1 Y 4-1-4-1 NO CAMBIA LA SIMULACION. Los tres
    son cuatro defensores, cinco volantes y un delantero, y el modelo agrupa
    por categoria: lo unico que cambia es como se dibuja. El desempate es el
    orden de la lista, y esta escrito que es una decision y no un descuido.

  · CUANDO EL DATO NO ALCANZA no se inventa: menos de diez titulares, o
    ninguna categoria reconocible, devuelve null y recien ahi entra el dibujo
    de respaldo. Que ahora se llama FORMA_DE_RESPALDO y es solo eso.

  · EN LOS PARTIDOS DE OTRAS LIGAS se vuelve al de respaldo en vez de
    arrastrar el del club de la pagina: el archivo de liga no trae quien fue
    titular, y un partido de la Premier dibujado con la forma de Talleres es
    peor que uno dibujado con la forma por defecto.

  · TRECE PRUEBAS NUEVAS: 74 en el motor, 167 en el navegador. Los titulares
    del cache sintetico estan elegidos a proposito para que den 5-3-2 y NO
    4-3-3: si el dibujo deducido fuera igual al de respaldo, la prueba pasaria
    sola sin deducir nada. Es la misma trampa del marcador empatado.

  · Se sigue pudiendo cambiar la formacion a mano, igual que siempre.

Arma el 11 - version 2026-09-03 y23 (los que ya no estan)

  · Primera devolucion de los testers: hay jugadores en el plantel que se
    fueron en este mercado de pases. Es la queja mas grave de las cuatro, y no
    por el jugador: el argumento entero de la app es que los datos son reales,
    asi que un nombre que se fue en julio no se lee como "falta actualizar el
    plantel", se lee como que TODO esta viejo. Ensucia los numeros, que no
    tienen nada que ver.

  · POR QUE PASABA. El plantel se arma con los que jugaron los ultimos cinco
    partidos MAS la lista oficial de la API, que se agrego a proposito para
    que no falte el lesionado ni el que acaba de llegar. Esa lista oficial se
    actualiza tarde.

  · MIRANDO EL CACHE DE VERDAD DE TALLERES: la lista oficial trae 31
    jugadores, 6 sin un minuto en los ultimos cinco partidos - y al mismo
    tiempo hay 3 que SI jugaron y no figuran en la lista. Se equivoca para los
    dos lados. No se puede usar sola ni tirarla.

  · LO QUE NO SE HIZO, y es la tentacion facil: sacar al que no jugo. Eso se
    lleva puesto al lesionado y al recien llegado, que es justo para lo que la
    lista oficial existe. Tampoco una lista de transferencias mantenida a
    mano: la regla del proyecto es que ningun nombre de club esta escrito en
    el codigo, y por la misma razon no puede haber ninguno de jugador.

  · LO QUE SE HIZO: se pregunta lo que se quiere saber. Un pedido de
    /transfers por equipo. De cada jugador se mira su movimiento MAS RECIENTE
    con ese club; si ese movimiento lo saca, se fue.

  · LA VALVULA, que es la mitad importante: si despues de esa fecha volvio a
    jugar para nosotros, se queda. El dato de transferencia puede estar mal, o
    el tipo puede haber vuelto, pero los minutos jugados no se discuten. Sin
    esa valvula un dato equivocado en la API nos borra un titular - un error
    mucho MAS visible que el que estamos arreglando. Para poder compararlo,
    ahora cada jugador guarda la fecha de su ultimo partido.

  · Y si el pedido falla o viene vacio no cambia nada: se sigue como antes.

  · DE PASO, en el selector de jugador: los que no sumaron un minuto van al
    final y con un cartelito "sin minutos". Su fuerza es la media de la liga
    porque no hay con que calcularla, y sin el cartel eso se lee como un
    jugador promedio en vez de como lo que es: un jugador del que no sabemos
    nada.

  · CUATRO PRUEBAS NUEVAS (164 en el navegador). Y se probaron al reves antes
    de darlas por buenas: apagando el filtro, la del que se fue falla. Una
    prueba que pasa igual con el arreglo apagado no prueba nada, que ya nos
    paso con el marcador empatado.

  · Costo: dos pedidos mas por club, sobre mil doscientos de una corrida.

Arma el 11 - version 2026-09-02 y22 (la segunda huella)

  · Entro la huella SHA-256 de la clave con la que GOOGLE firma la app, que es
    la que de verdad tiene el telefono que la instala desde Play. La primera
    -la de PWABuilder- es la de CARGA: sirve para subir el .aab y nada mas.
    Google reemplaza la firma al distribuir, asi que sin esta segunda huella
    el assetlinks no coincide y la app abre con la barra del navegador arriba.

  · Se saca de Play Console → Protegido con Play → Firma de apps, columna
    "Clave clasica". La columna de al lado, la poscuantica, NO sirve para
    esto: Android todavia no la usa para verificar los enlaces.

  · Las dos huellas son PUBLICAS: son certificados, no llaves. Van en
    sitio.json a proposito, y de ahi sale .well-known/assetlinks.json.

  · Solo cambia sitio.json. No hay que reempaquetar la app: el .aab envuelve
    la direccion del sitio, y el archivo que autoriza vive en el sitio.

Arma el 11 - version 2026-09-02 y21 (el tipo se resuelve al crear, no al correr)

  · "type liga does not exist", y la tabla `liga` existe hace meses.

  · La linea `set search_path = public` que llevan todas nuestras funciones
    vale cuando la funcion CORRE. Pero el TIPO que devuelve se resuelve
    cuando la funcion se CREA, y ahi manda el search_path de la sesion que la
    esta creando - el del editor SQL de Supabase, que viene acotado. Asi que
    `returns setof liga` no encontraba la tabla, aunque estuviera ahi.

  · El mensaje engania: suena a que falta la tabla. No falta: no la esta
    buscando donde esta. Ahora dice `returns setof public.liga`.

  · Es la misma leccion que veniamos teniendo toda la tarde, en otra forma:
    un nombre solo significa algo dentro de un contexto, y el contexto de
    cuando se escribe una funcion no es el de cuando se ejecuta.

Arma el 11 - version 2026-09-02 y20 (la funcion devuelve una fila de liga)

  · Tercer intento con el mismo torneo. El anterior tapaba el problema; este
    lo saca.

  · `returns table (id uuid, nombre text, codigo text)` DECLARA tres
    variables con esos nombres adentro de la funcion. Con `codigo` siendo a
    la vez columna de `liga` y variable de salida, cualquier mencion suelta
    de `codigo` tiene dos significados y Postgres corta.

  · Se puede tapar de dos formas -escribiendo `liga.codigo` en todos lados, o
    con la directiva `#variable_conflict use_column`- y las dos funcionan.
    Pero lo que la funcion devuelve ES una fila de `liga`. Decirlo asi,
    `returns setof liga`, saca el problema de raiz: no hay variables de
    salida, no hay con que chocar. Ademas, el dia que la tabla tenga una
    columna nueva, la devuelve sin tocar nada. El navegador ya pedia
    `liga?select=*` en la otra consulta, asi que recibe exactamente lo mismo.

  · La regla que queda: cuando una funcion devuelve una fila de una tabla que
    ya existe, se dice `setof esa tabla`. Inventar una lista de columnas es
    repetir algo que ya estaba escrito, y lo repetido se desincroniza.

  · `sumar_simulacion` SI necesita la directiva y la tiene: devuelve columnas
    de dos tablas distintas, asi que no hay una sola tabla que nombrar, y
    `ciclo` -que es columna Y variable de salida- aparece solo en el
    `on conflict (perfil, ciclo)`, donde la columna va sola por obligacion.
    Ese iba a explotar la primera vez que alguien simulara con cuenta.

  · La prueba nueva se corrigio dos veces mientras se escribia, y las dos
    valen anotarse. La primera version buscaba `returns table` con un salto
    corrido y se colaba hasta la funcion SIGUIENTE: marcaba `es_de_zona`,
    que devuelve un booleano. La segunda encontraba `returns table` DENTRO
    del comentario que explica por que ya no dice `returns table`. Ahora se
    corta por definicion y se leen las definiciones sin sus comentarios: una
    prueba que busca texto en un archivo que habla de si mismo tiene que
    mirar el codigo y no la explicacion.

  · 50 pruebas en el esquema, todas en verde.

Arma el 11 - version 2026-09-02 y19 (el nombre de salida tambien es una variable)

  · "column reference codigo is ambiguous". La funcion ya existia -eso del
    y18 funciono- pero se caia adentro.

  · POR QUE. `returns table (id uuid, nombre text, codigo text)` no solo
    describe lo que sale: DECLARA tres variables con esos nombres adentro de
    la funcion. Entonces cuando el cuerpo dice `where codigo = c`, Postgres
    ve dos cosas llamadas `codigo` -la columna de la tabla `liga` y la
    variable de salida- y en vez de elegir una, corta.

  · Es la misma clase de error que la clase `.gol` repetida en el CSS: un
    nombre que ya estaba, usado de nuevo para otra cosa. Ahi el numero se
    volvia invisible; aca la funcion se cae. Nunca es el nombre nuevo el que
    esta mal: es que el viejo no se miro.

  · EL ARREGLO, en dos capas. La columna se escribe con la tabla adelante
    -`liga.codigo`-, que es lo que habria que haber hecho desde el principio.
    Y arriba de cada funcion plpgsql que devuelve una tabla va la directiva
    `#variable_conflict use_column`: ante la duda, gana la columna. Hace
    falta porque hay lugares donde la columna va sola por obligacion y no se
    le puede poner la tabla adelante: el `on conflict (perfil, ciclo)` de
    `sumar_simulacion` es exactamente eso, y `ciclo` es columna Y variable de
    salida. Ese iba a explotar la primera vez que alguien simulara.

  · UNA PRUEBA MAS (50 en total): toda funcion plpgsql que devuelve una tabla
    tiene que declarar la directiva, tenga hoy el choque o no. El choque
    aparece el dia que agregamos una columna que se llama igual que algo que
    ya devolviamos, y ese dia nadie se va a acordar de esto.

  · Lo que quedo claro de los tres intentos seguidos: las pruebas leen el SQL,
    no lo corren. Un error de Postgres -tipo de retorno, nombre ambiguo,
    funcion duplicada- solo aparece al pegarlo. Cuando haya un rato, la
    prueba que falta es una base de verdad, aunque sea vacia.

Armá el 11 — versión 2026-09-02 y18 (el archivo entero se caía por una función)

  · SEGUIMOS SIN PODER CREAR TORNEOS después del y17, con el mismo cartel:
    "Could not find the function public.crear_liga(p_nombre) in the schema
    cache". O sea: el sitio estaba al día y la base no.

  · POR QUÉ NO ENTRÓ EL SQL. El editor de Supabase corre TODO lo que uno pega
    dentro de UNA transacción. Un solo error en cualquier línea deshace el
    archivo completo — no se crea nada, ni siquiera lo que estaba perfecto.
    Y había un error garantizado para el que ya había corrido la versión
    anterior: `create or replace function` NO puede cambiarle a una función
    lo que devuelve. `mi_cupo` y `sumar_simulacion` cambiaron de forma cuando
    el cupo pasó de mes calendario a ciclo desde la fecha de pago, así que
    Postgres cortaba con "cannot change return type of existing function" y
    se llevaba puesto a `crear_liga`, que venía cientos de líneas antes y no
    tenía nada que ver.

  · DESDE AFUERA ESO SE VE COMO SI EL ARCHIVO NUNCA SE HUBIERA PEGADO. No hay
    forma de darse cuenta salvo leer el renglón rojo del final. Es el peor
    tipo de falla: la acción se hizo, se ve hecha, y no quedó nada.

  · EL ARREGLO. Toda función que devuelve una TABLA lleva ahora su
    `drop function if exists` justo antes: crear_liga, tabla_liga, tabla_zona,
    mi_cupo, sumar_simulacion. Son las que cambian de forma cuando les
    agregamos una columna. Ahora el archivo se puede pegar de nuevo cuantas
    veces haga falta, que era la idea desde el principio.

  · Y LA OTRA MITAD DEL MISMO PROBLEMA: `registrar_pago` pasó de siete
    parámetros a ocho. Un parámetro nuevo —aunque tenga valor por defecto— no
    reemplaza a la función vieja: la deja al lado. Con las dos vivas, una
    llamada de siete argumentos no sabe a cuál ir y Postgres contesta
    "function is not unique". Un pago que no se acredita porque hay dos
    funciones con el mismo nombre es exactamente el tipo de cosa que se
    descubre el día que alguien paga. Se borra la de siete a mano.

  · NO se pueden borrar todas por las dudas: `es_miembro` y `es_de_zona` las
    usan las políticas de seguridad, y una función de la que depende una
    política no se borra sin llevarse la política puesta. Esas se reemplazan
    y ya está, que además nunca cambian de forma: devuelven sí o no.

  · TRES PRUEBAS NUEVAS en probar-backend.mjs (49 en total): que cada función
    que devuelve una tabla tenga su borrado, que el borrado esté ANTES de la
    creación —uno escrito después borra justo lo que se acaba de crear— y que
    la registrar_pago de siete parámetros se borre.

  · LO QUE HAY QUE HACER: pegar `esquema.sql` de nuevo, entero, en el SQL
    Editor de Supabase. No hace falta volver a empaquetar la app para Play:
    el .aab envuelve la dirección del sitio, no el código.

Armá el 11 — versión 2026-09-02 y17 (crear un torneo estaba roto)

  · NO SE PODÍA CREAR UN TORNEO DE AMIGOS, y el mensaje que salía mandaba a
    buscar al lugar equivocado: "La fecha ya cerró: no se puede cambiar el
    equipo". Nada que ver.

  · QUÉ PASABA. La app hacía dos inserts seguidos: primero en `liga`, con
    `Prefer: return=representation`, y después en `liga_miembro`. Esa cabecera
    le pide a Postgres que devuelva la fila reción creada — y para devolverla,
    Postgres tiene que poder LEERLA. La política de lectura de `liga` es
    `es_miembro(id)`, que en ese instante es FALSA: la fila de miembro se
    insertaba recién en el segundo pedido. El insert entraba y la lectura lo
    rebotaba con un error de política.

  · Y HABÍA UN SEGUNDO AGUJERO, más silencioso: eran dos pedidos. Si el
    segundo no llegaba —se cortó la red, se cerró la pestaña—, quedaba un
    torneo sin ningún miembro, invisible hasta para el que lo había creado.

  · LOS DOS SE ARREGLAN IGUAL: `crear_liga`, una función en la base, como
    `entrar_a_liga`. Una sola transacción, y la lectura ya no pasa por la
    política. El código del torneo lo sortea el servidor: antes lo sorteaba
    el navegador y reintentaba cinco veces si chocaba, o sea que usaba el
    `unique` de la base como mecanismo de reintento a través de la red.

  · EL MENSAJE MENTÍA, Y ESO ES LO QUE COSTÓ EL TIEMPO. La traducción de
    errores agarraba CUALQUIER violación de política y contestaba siempre lo
    mismo. Ahora solo lo dice cuando el error es de la tabla `equipo`, que es
    de lo que habla. Para el resto muestra el error de verdad: un mensaje
    amable y falso hace perder más tiempo que uno feo y cierto.

  · POR QUÉ NINGUNA PRUEBA LO VIO. Las de backend leen el SQL y las de
    cuentas simulan el servidor: ninguna corre contra una base real, así que
    ninguna podía descubrir que una política rebotaba un `returning`. La que
    lo encontró fue Fausto, apretando el botón. Quedaron cuatro casos nuevos
    que fijan la forma —un solo pedido, por función, sin mandar el código— y
    uno que usa el texto COMPLETO que manda Postgres, con el nombre de la
    tabla, y verifica que un error en `liga` NO diga que la fecha cerró.

  · 48 casos en cuentas, 46 en backend, 160 en el navegador. Todo en verde.


Armá el 11 — versión 2026-09-02 y16 (el ciclo empieza el día que pagás)

  · EL CUPO NO SE CUENTA POR MES CALENDARIO. Se contaba así y estaba mal de
    una forma que se ve sola con un ejemplo: el que compraba el 30 se llevaba
    cuarenta simulaciones por un día, y el 1 le entraban cuarenta más. Un plan
    que se paga el 20 tiene que durar hasta el 20 del mes que viene.

  · EL ANCLA. Para el que paga es el día del pago (`perfil.plan_desde`, que lo
    escribe `poner_plan` cuando Mercado Pago confirma); para el que no pagó
    nunca, el día que empezó. El ciclo se calcula SIEMPRE desde el ancla
    sumando meses enteros — `ancla + n meses` — y nunca sumándole un mes al
    ciclo anterior.

    LA DIFERENCIA APARECE CON LOS DÍAS 29, 30 Y 31. El 31 de enero más un mes
    es el 28 de febrero, porque el 31 no existe. Sumando de a uno, ese 28
    quedaría clavado para siempre y el que pagó un día 31 terminaría cobrando
    tres días antes cada mes. Contando desde el ancla, marzo vuelve a caer 31.
    Hay tres casos que lo fijan.

  · LA MISMA CUENTA DE LOS DOS LADOS. `inicio_de_ciclo` en Postgres y
    `inicioDeCiclo` en el navegador tienen que dar lo mismo: si no coinciden,
    el contador que muestra la pantalla no es el que cuenta el servidor.

  · Y LA FECHA SE DICE. "Te quedan 9 de 10 simulaciones. Se renueva el 2 de
    octubre." Antes decía "se reinicia el 1 de cada mes", que ahora sería
    mentira: el día es distinto para cada persona, así que hay que decirlo.

  · Dos errores propios en el camino, los dos del mismo tipo —algo que ya
    existía y no miré—:

      · `diaDe` ya estaba, y sirve para el feed: dice "Hoy", "Ayer",
        "Martes". La declaré de nuevo para una fecha futura y la página entera
        murió con "Identifier 'diaDe' has already been declared". Ahora la
        nueva se llama `diaFuturo`.
      · `toISOString` para guardar el día. Alguien en Madrid que abre la app a
        las 00:30 del 20 habría quedado guardado el 19: la conversión a UTC le
        corre el día para atrás. El ciclo de una persona es el día que esa
        persona ve en su calendario, así que la fecha se arma con la zona del
        teléfono.

  · Y la prueba de las funciones con llave maestra volvió a hacer su trabajo,
    dos veces. Primero se puso roja con las nuevas; después descubrió que su
    propia forma de detectarlas estaba mal —un `[\s\S]*?security definer`
    corrido le hacía atribuir la llave de una función a la anterior—. Una
    prueba de seguridad que se equivoca de función no protege nada. Ahora
    corta el SQL en definiciones y mira cada una por separado, y las que NO
    necesitan llave están en una lista aparte con el motivo.

  · 160 casos en el navegador, 46 en el backend, 34 en pagos. Todo en verde.


Armá el 11 — versión 2026-09-02 y15 (el cupo de simulaciones)

  · LOS PRECIOS NUEVOS, en la función de cobro, que sigue siendo el único
    lugar del proyecto donde vive un precio:

        chico   $5.500    40 simulaciones por mes
        medio  $12.000   100 simulaciones por mes
        libre  $20.000   sin límite

    Y diez por mes gratis, con un aviso.

  · EL CONTADOR AHORA EXISTE. El que había era una variable de JavaScript:
    se borraba al recargar la página, o sea que el tope se reiniciaba solo.
    Un tope que se reinicia solo no es un tope.

      · Con cuenta, el contador vive en la base, en `uso_mes`. Esa tabla NO
        tiene política de escritura: la única forma de moverlo es la función
        `sumar_simulacion`. Si hubiera política de update, el tope sería una
        sugerencia.
      · Sin cuenta, lo lleva el navegador junto con el mes, así se reinicia
        solo el 1 y nadie tiene que correr nada. Eso se esquiva borrando los
        datos del sitio, Y ESTÁ BIEN QUE SE ESQUIVE: pedirle cuenta a alguien
        para simular una vez ahuyenta justo al que todavía no sabe si le
        gusta.
      · `perfil.plan` tiene el update revocado por columna, igual que
        `premium_hasta`. Las políticas de RLS son por FILA y la de perfil deja
        que cada uno edite la suya —la necesita para el nombre de usuario—,
        así que sin el revoke cualquiera se pone plan 'libre' desde la
        consola. Es la misma lección, clavada ahora para las dos columnas.

  · LOS PRECIOS SE VEN DESDE ANTES DE QUE SE COBRE. Abajo del botón de
    simular, para todos, tengan cuenta o no. Un precio que aparece el día que
    empieza a cobrarse se lee como una trampa; este se ve desde el primer día
    con el cartel de que todavía es gratis.

    Y ahí apareció un error que valía la pena: los planes se pedían adentro
    del `if(quienSoy())`, así que el ÚNICO que veía cuánto sale era el que ya
    se había registrado. Justo al revés de para quién es el precio.

  · DOS INTERRUPTORES, LOS DOS APAGADOS, en sitio.json:

      cupo.bloquea = false    el contador cuenta y se muestra, pero no frena
      cupo.cobrando = false   los planes se ven y los botones están apagados

    Durante la prueba cerrada tiene que ser así. Un tester frenado a la mitad
    no puede probar nada, y los catorce días no se repiten. Se prenden
    cambiando dos booleanos.

  · La referencia del pago pasó de "<uuid>:<meses>" a "<uuid>:<meses>:<plan>".
    El campo se agregó AL FINAL a propósito: un pago que ya estaba en vuelo,
    hecho con la versión anterior, se sigue leyendo y se sigue acreditando.
    Un plan desconocido se descarta en vez de viajar a la base — allá lo
    rechazaría una excepción, el webhook contestaría 500 y Mercado Pago
    reintentaría para siempre un pago que ya cobró.

  · Y la prueba de las funciones con llave maestra hizo exactamente lo que
    tenía que hacer: se puso roja al aparecer tres `security definer` nuevas.
    Existe para que agregar una sea una decisión y no un descuido. Ahora son
    once y cada una está explicada.

  · 156 casos en el navegador, 31 en los de pagos. Todo en verde.


Armá el 11 — versión 2026-09-01 y14 (el sitio autoriza a la app de Android)

  · ASSETLINKS. Con el paquete `com.armael11.app` y la huella de firma que
    generó PWABuilder, el sitio ya publica `.well-known/assetlinks.json`.
    Es el archivo que le da permiso a la app de Android para abrir
    armael11.com a pantalla completa, sin la barra del navegador arriba.

  · FALTA LA SEGUNDA HUELLA. Google vuelve a firmar la app cuando se sube a
    Play, así que la app que la gente instala NO está firmada con la llave de
    PWABuilder sino con la de Google. Esa segunda huella sale de Play Console
    → Configuración de la app → Integridad de la app → Firma de apps, y se
    agrega al array `huella` de sitio.json. Hasta entonces la app funciona,
    pero se ve con la barra del navegador.

  · LA PRUEBA DEL ASSETLINKS CAMBIÓ DE PREGUNTA. Decía "no tiene que existir",
    que era cierto mientras no había huella y se volvió falso el día que la
    hubo. Ahora la regla es la que va a seguir siendo cierta: si el archivo
    existe, tiene que estar bien — relación correcta, paquete con forma de
    paquete y huellas SHA-256 enteras (32 bytes en hexa). Una huella copiada
    a medias no falla en silencio: falla en la cara del usuario, con la barra
    del navegador, y nadie sabe por qué.

  · 139 casos en probar-sitio.cjs, todos en verde.


Armá el 11 — versión 2026-09-01 y13 (el tanteador con nombres, y el gancho de la portada)

  · EL MARCADOR YA NO ES UN "2-0" PELADO. Dice "Talleres 2 - Belgrano 0",
    en los tres lugares donde aparece: la línea de abajo de la cancha
    mientras el partido corre, la tarjeta de tu simulación y la tarjeta de
    la realidad. Antes había que acordarse de quién iba primero — y la
    tarjeta del resultado queda a pantalla y media de la cancha.

  · Mientras juega, cada gol va PEGADO A SU EQUIPO y el reloj cuenta solo
    minutos: "Belgrano Cordoba 1 · 72' · 0 Huracán". Ya no hay nada que
    deducir del orden.

  · Los dos nombres del tanteador se achican por igual, así los números
    quedan siempre centrados aunque de un lado diga "Central Córdoba".

  · Un error que casi no se ve y conviene recordar: la clase del número se
    llamaba `.gol`, que YA ERA el cartel de "¡GOL!" que ocupa la cancha
    entera. El número heredó `position:absolute; inset:0; display:grid` y
    quedó invisible, estirado sobre toda la línea. Una clase repetida no da
    error: da un elemento que está y no se ve. Ahora se llama `.tanto`.

  · LA PORTADA CUENTA POR QUÉ ESTO NO ES UN DADO, EN DOS FRASES. Un bloque
    abajo de la grilla de clubes —después, nunca antes: elegir el club sigue
    siendo lo primero—, con un título, dos oraciones y una línea de letra
    chica. La primera versión lo explicaba en tres párrafos: "este testamento
    no lo lee nadie". En una portada el respaldo se muestra, no se argumenta.

      · El título: "El simulador no tira un dado."
      · Las dos frases: tu partido se juega 6.000 veces con los goles reales
        de cada liga; y la argentina es la más imprevisible — está medido.
        (Sin el "de las nueve que probamos": la comparación ya está abajo,
        en la letra chica, y en el titular restaba en vez de sumar.)
      · La letra chica, un renglón, para el que quiera verificar: 10.860
        partidos reales · nueve ligas · desde 2019 · 1.783 de tres ligas que
        el modelo nunca había visto.

    Los números salen de contar los fixtures del backtest y están anotados
    uno por uno en `claude/modelo-backtest.md`. Hay una prueba que falla si
    alguien los cambia en la portada, para obligarlo a pasar por el respaldo:
    prometer algo que no está medido es la única forma de que esto se vuelva
    mentira, y ninguna prueba automática lo detecta sola.

  · 136 casos en probar-sitio.cjs, más las de Node. Todo en verde.


Armá el 11 — versión 2026-09-01 y12 (el marcador, la letra y el descargo)

  · EL RELOJ SE FUE DE LA CANCHA. Estuvo arriba del arco rival y después en
    la esquina, y en los dos lugares tapaba jugadores: con el bloque
    moviéndose ±36 no queda un rincón por donde no pase nadie. Ahora va
    ENTRE LOS DOS NOMBRES DE LOS EQUIPOS, abajo de la cancha, donde se lee
    como lo que es: un marcador.

  · Y ESO OBLIGÓ A ORDENAR LOS NOMBRES. Un "1-0" entre dos nombres se lee de
    izquierda a derecha. El marcador siempre contó desde tu club, y los
    nombres estaban al revés: el mismo partido decía dos cosas distintas.
    Ahora TU CLUB VA PRIMERO en las cuatro puntas — la línea de equipos, el
    reloj, el número grande y el xG. Hay una prueba que simula hasta que
    haya un ganador (en un 1-1 el error no se ve) y falla si alguien da
    vuelta una sola de las cuatro.

  · LA TARJETA DEL RESULTADO YA DICE QUIÉN CONTRA QUIÉN. Queda a pantalla y
    media de la cancha: llegabas a un "1-1" sin saber de qué partido
    hablaba.

  · LA LETRA DEL GLOBITO SALÍA SIEMPRE EN BLANCO. En River, Huracán y Vélez
    el fondo TAMBIÉN es blanco: la G, la D, la M y la F desaparecían, y el
    anillo blanco terminaba de convertir a los once en manchas. En Rosario
    Central pasaba lo mismo con el amarillo. Ahora la tinta se calcula por
    contraste, con la misma función que usan las tarjetas de la portada, y
    hay una prueba que la mide con los colores de los treinta clubes.

  · NADIE SE PARA ARRIBA DEL ARQUERO. Un central arranca a doce puntos de su
    arco; cuando el repliegue le sumaba treinta y cuatro, los cuatro
    defensores y el arquero terminaban en el mismo metro cuadrado. El
    repliegue de cada uno se limita AL ESPACIO QUE TIENE DETRÁS: el que está
    lejos se hunde, el que ya está hundido se queda. El bloque se comprime
    en vez de amontonarse.

  · EL DESCARGO LEGAL NOMBRABA AL CLUB EQUIVOCADO. Decía "sin relación con
    el Club Atlético Talleres" en las treinta páginas: la de River aclaraba
    que no tenía relación con Talleres. Ahora sale del club de cada página,
    y la construcción se aborta si falta el marcador — igual que con la
    versión.

  · foto-play.cjs: las capturas de la tienda dejaron de ser a mano. Levanta
    el sitio, carga el cache real de cada club y saca las cuatro a 1080×1920.
    Si el cache de un club es el sintético de las pruebas, NO saca la
    captura: una foto de tienda con nombres inventados es publicidad falsa.

  · 129 casos en probar-sitio.cjs, todos en verde.


Armá el 11 — versión 2026-09-01 y11 (la cancha se mueve)

  · "LOS GLOBITOS NO PASAN LA MITAD DE LA CANCHA". Tenía razón y era
    estructural, no una impresión. Los números de antes:
      · Las formaciones ocupan el 36% del alto: el de abajo va del 93 al 57,
        el de arriba del 7 al 43, y la mitad está en el 50. EN REPOSO NO
        HABÍA UN SOLO JUGADOR PASANDO LA MITAD, y quedaba una franja de 14
        puntos en el medio que nadie pisaba nunca.
      · El movimiento estaba topeado en ±17. Un delantero parado en 57
        llegaba como mucho a 40. Para pisar el área rival (13) necesitaba
        moverse 44, casi el triple del tope.
      · Y el desplazamiento de bloque competía contra ESE MISMO tope junto
        con la atracción a la pelota, así que se anulaban entre ellos.

  · TRES ARREGLOS DISTINTOS:
      · EL BLOQUE TIENE SU PROPIO PRESUPUESTO, de ±36, separado del de la
        atracción a la pelota. Un equipo que ataca se lleva a sus once
        treinta puntos arriba.
      · Y NO SE MUEVE EN UNA PIEZA: SE ESTIRA. El delantero avanza 1,4 veces
        lo que avanza un central. Sin eso, o los delanteros no llegaban
        nunca, o los defensores terminaban de delanteros.
      · AL REPLEGAR SE INVIERTE: el que más sube atacando es el que menos
        baja defendiendo, así el 9 queda arriba esperando la contra. Con el
        mismo factor para los dos lados, el 9 defendía adentro de su propia
        área — que es exactamente lo que no hace un 9.
      · El bloque también BASCULA de costado hacia la pelota. Sin eso los
        once suben y bajan como un ascensor y todo pasa por el medio.
      · Y el desorden pasó de ±2 a ±5 por el ritmo: ahora las líneas se
        mezclan en vez de temblar cada una en su casilla.

  · Cada equipo lee las perillas de SU técnico: el bloque del rival responde
    a lo que puso el rival, no a lo tuyo.

  · Medido: en reposo el más adelantado está en 57 (su propio campo, como en
    el saque); atacando llega a 22 —el borde del área rival— y seis jugadores
    cruzan la mitad; defendiendo el delantero se queda en 72 y no vuelve a su
    área. Nadie se va de la cancha y el arquero no se despega del arco.

  · 605 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-31 y10 (entrar de verdad)

  · AHORA SE ENTRA CON MAIL Y CONTRASEÑA. Empezamos solo con el link por
    mail y el argumento era bueno —la contraseña que no guardamos es la que
    no podemos perder— pero el link falla de dos maneras que NO dependen de
    nosotros, y las dos las sufrimos:
      · Supabase solo redirige a las direcciones de su lista blanca. Si la
        de destino no está, manda a la Site URL. La persona vuelve a una
        página que no es la suya, sin sesión y SIN NINGÚN ERROR. Parece que
        el link no anda.
      · En el teléfono, tocar el link en Gmail lo abre en el navegador
        INTERNO de Gmail. La sesión queda guardada AHÍ. La persona vuelve a
        Chrome y no está: hizo todo bien y no entró.
    La contraseña no sale de la pantalla en la que estás, así que no le
    puede pasar ninguna de las dos. El link queda para el que se la olvidó.
    La contraseña la guarda Supabase, hasheada; en la app no se escribe en
    ningún lado.

  · LA PORTADA RESCATA LA SESIÓN. Si el link cae en el inicio —el caso 1 de
    arriba—, la portada agarra el token, lo guarda y devuelve a la persona a
    la página de su club. El guardado es por ORIGEN, así que con eso queda
    la sesión puesta para todo el sitio. Es una red, no la solución: lo
    correcto es tener bien la lista blanca en Supabase. Pero una
    configuración que no se ve no puede ser lo único que separa a alguien de
    entrar a su cuenta.

  · EL BOTÓN "ENTRAR CON MI MAIL" DE LOS TORNEOS ERA DE ADORNO, Y LO ERA.
    Funcionaba: abría el panel de la cuenta, que vive arriba de todo,
    mientras la persona estaba abajo en los torneos. Se abría fuera de la
    pantalla. Un botón que anda pero cuyo efecto no se ve es un botón roto.
    Ahora lleva hasta el panel y pone el cursor en el mail.

  · 599 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-31 y9 (dejar de comernos la cuota)

  · POR QUÉ SE AGOTÓ LA API. No la gastaron usuarios: la gastamos
    publicando. Una corrida completa son unos 1.200 pedidos —las seis ligas
    ~800, los treinta clubes ~400— y el workflow corre cada 3 horas Y en
    cada push. Seis versiones publicadas en una tarde más las ocho del reloj
    dieron catorce corridas: unos 16.000 pedidos contra un tope de 7.500.

  · LA SOLUCIÓN: NO VOLVER A BAJAR LO QUE YA BAJAMOS HACE UN RATO.
      · `frescura.mjs` decide qué pasos vale la pena rehacer. Cada paso deja
        un sello con la hora en que terminó bien; si lo que produjo sigue
        estando y se bajó hace poco, se saltea.
      · Los sellos sobreviven entre corridas porque el workflow los guarda
        en su cache (bloque `actions/cache`, agregado al .yml). Sin ese
        bloque los sellos estarían siempre vacíos y esto no serviría.
      · Cada cuánto: ligas 24 h (es el paso más caro y el que menos cambia),
        juego 6 h, tabla 6 h, fantasy 3 h (es barato y sin fecha no hay
        pestaña), puntos 6 h. Da unos 2.500 pedidos por día contra 7.500.
      · `Run workflow` a mano trae TODO igual: es la salida de emergencia.

  · LA REGLA QUE HACE QUE SEA SEGURO: un paso solo se saltea si lo que
    produjo SIGUE ESTANDO. El sello dice "se bajó hace dos horas"; si el
    archivo no está, el sello miente. Sin eso, un cache a medias publicaría
    un sitio sin datos del juego y la app le pediría la API key al usuario
    — que es el síntoma exacto que nos hizo encontrar todo esto.

  · Y EL AVISO QUE NOS FALTÓ. Los pasos que hablan con la API no son
    obligatorios: si no contesta, el sitio se publica igual. Es la decisión
    correcta y tiene un costo que pagamos hoy: la corrida sale VERDE con
    todo vacío. Ahora, si TODO lo que depende de la API viene sin nada, el
    log lo grita y nombra la causa más probable, que es la cuota. Tres pasos
    independientes no fallan juntos por casualidad.

  · 593 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-31 y8 (la lista de a veinte)

  · LA LISTA DE JUGADORES VA DE A VEINTE, CON ANTERIORES Y SIGUIENTES. Es
    la tercera versión de la misma decisión, y conviene tener las tres:
      60, callado    se veían los sesenta más caros y parecía que el resto
                     no existía. Un límite callado se lee como un error.
      150, dicho     mejor, pero seguía escondiendo gente: al resto solo se
                     llegaba adivinando un nombre para buscarlo.
      20, paginado   se ve TODO, en pedazos que entran en una pantalla, y
                     el pie dice "1-20 de 200", que es lo que prueba que la
                     lista no esconde nada.
    Cualquier cosa que cambie QUÉ se lista —el puesto, el club, el orden,
    la búsqueda— vuelve a la primera página. Sin eso, cambiar de puesto
    estando en la página 7 deja una pantalla vacía y parece que ese puesto
    no tiene jugadores.

  · LOS TORNEOS DE AMIGOS, ARRIBA DE LA LISTA. Con ciento cincuenta
    renglones, todo lo que venía después quedaba a un scroll que nadie
    hace, y ahí abajo vivían los torneos. Con la lista corta, el bloque
    entra en la pantalla.

  · SE SACÓ EL ATAJO que habíamos puesto arriba para llegar a los torneos.
    Existía porque el bloque estaba lejos; ahora está a la vista, y un
    cartel que señala algo que ya se ve es ruido.

  · 577 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-31 y7 (saber qué está publicado)

  · LA VERSIÓN, A LA VISTA, EN EL PIE DE LA APP. Sale de este archivo y le
    agrega la fecha y hora de la corrida. Existe por una tarde perdida: la
    app se veía vieja y no había forma de saber si la corrida no había
    publicado o si el navegador estaba mostrando algo guardado. Son dos
    problemas distintos, con dos arreglos distintos, y sin este renglón no
    se distinguen sin abrir el inspector. Ahora se mira el pie y se sabe.
    Si falta el marcador en la plantilla, la corrida CORTA: publicar sin
    poder saber qué se publicó es lo que había antes.

  · EL SERVICE WORKER YA NO PUEDE SERVIR UNA PÁGINA VIEJA HABIENDO RED.
    "Primero la red" no alcanzaba: `fetch` también pasa por la caché HTTP
    del navegador, y GitHub Pages manda sus páginas con diez minutos de
    vida. Con eso, publicar y recargar podía seguir mostrando lo de antes
    sin que nada estuviera roto — que es de lo más difícil de diagnosticar,
    porque no falla: miente.
    Ahora las NAVEGACIONES van con `cache: "no-store"`. Los datos y las
    imágenes siguen con la caché normal: ahí diez minutos no cambian nada y
    ahorran tráfico de verdad.

  · CACHÉ v2. Cambiar ese número tira todo lo guardado en todos los
    teléfonos, y es exactamente para esto que lo dejamos escrito.

  · La columna `liga.paga` queda, documentada como PENDIENTE y no
    conectada: la decisión es cobrar el torneo de amigos después de la
    prueba con los doce testers.

  · 570 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-31 y6 (el repaso, y el enchufe de la publicidad)

  · EL EQUIPO SE GUARDABA Y LA APP NUNCA LO VOLVÍA A LEER. Es lo peor que
    quedaba. Armabas quince jugadores, tocabas guardar, cerrabas, y al
    volver la cancha estaba vacía; desde otro teléfono, también. Quedaba
    guardado de verdad —se puntuaba bien— pero no había forma de verlo, así
    que la única lectura posible era "se borró".
    Se arregló por dos lados, que son dos problemas distintos:
      · EL BORRADOR, en el teléfono, en cada cambio. Armar quince jugadores
        son cincuenta toques; que un cierre de pestaña se los lleve es la
        clase de cosa que hace que alguien no vuelva a intentarlo. No
        necesita cuenta ni conexión.
      · EL GUARDADO, en el servidor, que MANDA cuando existe: es el que se
        puntúa. Si ganara el borrador local, el que armó algo en la compu y
        no lo guardó vería su versión vieja acá y creería que perdió lo
        otro.
    La formación no se guarda: se deduce de los puestos de los once. Un dato
    que se puede calcular y además se guarda es un dato que algún día va a
    estar en desacuerdo consigo mismo.

  · LA PESTAÑA DEL FANTASY DESAPARECÍA ENTRE FECHAS. La regla era "solo si
    hay fecha publicada", y era correcta cuando adentro solo se armaba un
    equipo. Dejó de serlo sin que nadie lo notara: ahí adentro ahora están
    los torneos de amigos, las zonas y los puntos de la fecha pasada, y todo
    eso se mira JUSTAMENTE entre una fecha y la siguiente. El lunes —cuando
    la gente quiere ver cuánto sacó— la pestaña no existía.
    Ahora aparece si hay fecha O si hay backend. Sin ninguna de las dos
    sigue escondida, que es lo correcto.

  · EL ENCHUFE DE LA PUBLICIDAD, listo y apagado. Hoy `sitio.json` no tiene
    bloque `publicidad`, así que no entra un solo script de terceros — una
    propiedad del proyecto que ahora tiene su propia prueba y que hay que
    romper A PROPÓSITO. El día que AdSense apruebe la cuenta se agrega una
    línea con el id de cliente y queda encendido: el script entra y
    `window.SITIO.publicidad` pasa a existir, que es lo único que mira la
    app. Las reglas de cuándo corresponde un aviso ya estaban escritas y
    probadas.
    probar-publicidad.mjs construye el sitio dos veces —apagada y
    encendida— y compara. Deja sitio.json como estaba pase lo que pase: una
    prueba que deja el sitio con avisos encendidos es peor que no tenerla.

  · 568 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-31 y5 (el torneo que sigue)

  · LA FASE CLASIFICATORIA. Un torneo de amigos se muere solo: doce
    personas juegan cinco fechas, gana uno, y no queda nada por hacer. El
    que ganó no tiene contra quién seguir y los otros once ya saben que no
    ganan; la quinta fecha la juega la mitad.
    Ahora el grupo de amigos ES la clasificatoria: el que gana su grupo
    pasa a una zona donde compite contra ganadores de OTROS grupos, gente
    que no conoce. El que quedó afuera sigue jugando su liga igual, porque
    la liga no se termina.

  · LAS CUATRO REGLAS, y cada una tiene su prueba:
      · Una fase es un RANGO DE FECHAS ("de la 8 a la 12"), no una
        cantidad de días. El fantasy ya está organizado así, todos las
        entienden, y una fecha postergada no rompe nada.
      · Una fase se cierra cuando su última fecha está PUNTUADA, no cuando
        pasó el tiempo. Repartir zonas con la última fecha a medio calcular
        sería repartirlas con puntajes que todavía van a cambiar.
      · LOS EMPATES NO SE ROMPEN: PASAN LOS DOS. Cualquier desempate que
        inventáramos le saca el lugar a alguien que hizo exactamente los
        mismos puntos. Que una zona tenga once en vez de diez no le molesta
        a nadie; que a uno lo eliminen por una regla que no sabía que
        existía, sí.
      · LAS ZONAS SE ARMAN EN SERPENTINA. Cortando la lista por puntaje,
        los diez mejores clasificados quedarían todos en la zona A: nueve
        de los diez mejores afuera en la ronda siguiente, y la última zona
        ganada por alguien que hizo la mitad de puntos.

  · ARCHIVOS NUEVOS
      fases.mjs         cierra la fase y arma las zonas. Corre en GitHub,
                        después de puntuar (el orden importa: una fase se
                        cierra cuando su última fecha está puntuada).
      fases-reglas.mjs  quién pasa y en qué zona cae. Separado porque no
                        toca la base y por eso se puede probar de verdad.
      fases.json        los cuatro números que definen el torneo, cada uno
                        explicado adentro del archivo.
      probar-fases.mjs  18 casos sobre las dos decisiones que dejan gente
                        afuera sin que falle nada.

  · EN LA BASE: fase, zona y zona_miembro, las tres sin política de
    escritura — las escribe el servidor, como los puntos.
      · `zona_miembro` no se lee desde ningún teléfono: adentro hay uuid de
        perfil y no hacen falta. La pantalla pide `tabla_zona`, que
        devuelve usuarios y puntos. Es la misma regla de siempre.
      · Cada rival de la zona viene con el NOMBRE del torneo que ganó, no
        con su id. Es lo que hace que la zona no sea una lista de
        desconocidos.

  · 546 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-31 y4 (la sesión y lo tuyo)

  · POR QUÉ SE CAÍA LA SESIÓN AL RATO. No se caía: la cerrábamos nosotros,
    por dos motivos que se sumaban.
      · El refresh token de Supabase es de UN SOLO USO. La pantalla dispara
        varios pedidos juntos —el usuario, el premium, los torneos, la
        tabla de cada torneo— así que al vencer el token de una hora todos
        daban 401 al mismo tiempo y todos intentaban renovar con el MISMO
        refresh. El primero funcionaba; los demás recibían "ya usado" y con
        eso se cerraba la sesión de alguien que estaba perfectamente
        adentro. Ahora la renovación es una sola y los demás la esperan.
      · Y un error de red NO es una sesión vencida. Antes cualquier
        excepción —el subte, un timeout— borraba el token del teléfono, y
        eso no se recupera: hay que volver a pedir el mail. Ahora solo se
        cierra si el servidor dijo que ese refresh no sirve.
      · Además se renueva ANTES de disparar la tanda, no después de que
        seis pedidos fallen.

  · ENTRAR AHORA CAMBIA ALGO. El servidor calculaba los puntos de cada
    fecha y guardaba el detalle abierto, y la app no los mostraba en ningún
    lado: se armaba el equipo, se jugaba la fecha, y no pasaba nada. Los
    puntos existían en la base y no existían en la pantalla, que para el
    que juega es lo mismo que no existir.
      · Arriba de la pestaña del fantasy, tu última fecha puntuada: el
        total, en qué puesto quedaste en cada torneo, y qué sacó cada uno
        de tus jugadores.
      · Y el PORQUÉ, a un toque. "Te dieron 47" es un número que hay que
        creer; "tu 9 hizo un gol, jugó los 90, le pusieron 8.1, fue el más
        valioso y era tu capitán" es un número que se puede discutir. Esa
        diferencia es la que hace que alguien vuelva la fecha siguiente.
      · El que no jugó lo dice, en vez de mostrar un cero sin explicación.

  · 515 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-31 y3 (que se encuentren las cosas)

  · LAS OTRAS LIGAS, CON BANDERA Y ARRIBA. Estaban al final de la pantalla,
    después de doce partidos ya jugados: es la función que se puede vender
    en cualquier país y había que scrollear para encontrarla. Ahora va
    entre "lo que viene" y "ya jugados", con una ficha por país.
      · Las banderas son SVG escrito acá, no imágenes traídas de ningún
        lado. La app no carga NADA de terceros y esa regla no tiene
        excepciones. Además son banderas de países, no escudos de clubes:
        es la única imagen que esta app puede mostrar sin pisar la
        propiedad de nadie.
      · Están simplificadas (el sol argentino es un círculo, la esfera
        portuguesa un anillo): a treinta píxeles el detalle no se ve.

  · COMPARTIR ANDABA MAL EN LA COMPU. El botón de compartir el pronóstico
    tenía su propia copia de la lógica, sin el paso de wa.me: cuando el
    portapapeles fallaba —pasa seguido, pide foco y permiso— terminaba
    diciendo "copiala de la barra de direcciones", que es el botón
    admitiendo que no hizo nada. Ahora usa el mismo `compartir` que el
    fantasy, y se le agregó la red vieja del textarea con execCommand, que
    es fea y anda siempre.

  · LOS TORNEOS DE AMIGOS SE ENCUENTRAN. Dos problemas distintos:
      · Estaban al final de la lista de jugadores. La lista se queda donde
        está —es cómo se arma el equipo— pero ahora hay un renglón arriba
        que dice que existen y lleva hasta ahí.
      · Y desaparecían entre una fecha y la siguiente, porque vivían
        adentro del "todavía no hay fecha abierta". El torneo es
        justamente lo que dura entre fechas: el amigo que entraba con el
        código veía una pantalla vacía y se iba.

  · 504 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-31 y2 (el cobro)

  · SE PUEDE COBRAR. Dos funciones nuevas que NO corren en GitHub: corren
    en Supabase, y se pegan a mano en el editor del navegador. Están en el
    repo igual, versionadas y con una prueba que las lee.
      · funcion-crear-pago.ts    arma el link de Mercado Pago para UNA
        persona concreta. El precio sale de una tabla que vive adentro de
        esta función: el navegador manda el NOMBRE del plan y nada más. Si
        mandara el precio, alguien cambia 2500 por 1 en la consola, paga un
        peso, y Mercado Pago confirma un pago legítimo de un peso. Eso no se
        arregla después.
      · funcion-pago-avisado.ts  recibe el aviso de Mercado Pago y NO LE
        CREE NADA. Del aviso toma solamente el número de pago; el estado, el
        monto y a quién corresponde los va a buscar a la API de Mercado Pago
        con nuestro token. La dirección del webhook es pública y no puede
        pedir contraseña: si se creyera el cuerpo del mensaje, cualquiera se
        hace premium mandando un JSON.

  · REGISTRAR_PAGO, en la base. Anotar el pago y acreditarlo pasan adentro
    de la misma función o no pasan. Si fueran dos pedidos separados, dos
    avisos que llegan en el mismo segundo leen los dos "todavía no" y
    acreditan los dos: un mes pago, dos meses entregados.
      · Y lo que se anota no es si el pago se VIO, sino si se COBRÓ. Que el
        id sea la clave primaria alcanza para no anotar dos filas y no
        alcanza para esto: un pago avisa varias veces y cambia de estado en
        el camino (pendiente primero, aprobado después). Con la defensa
        vieja, el que paga en efectivo no cobraba nunca su premium porque el
        aviso bueno rebotaba contra la fila del aviso malo.

  · LO QUE SE VENDE, dicho en una línea. Los veinte segundos SON el aviso:
    son el rato durante el cual la app puede mostrar uno sin interrumpir
    nada. Por eso lo que se vende es sacarlos, y por eso la espera y el
    aviso salen del mismo lugar y no pueden quedar desacopladas. Un premium
    que no ve avisos pero igual espera veinte segundos no compró nada.
    Con el pase, el partido dura seis segundos y no aparece ningún aviso.

  · LA PÁGINA DE VUELTA (gracias.html) no puede afirmar que el pago entró.
    Lo único que sabe es lo que dice la dirección, y la dirección la escribe
    cualquiera. Dice lo que sí sabe: volvé a la app y fijate. La app, al
    volver, vuelve a preguntarle a la base unas cuantas veces, porque el
    aviso de Mercado Pago y la vuelta de la persona son dos carreras
    distintas y a veces gana la persona.

  · probar-pagos.mjs, nueva. Las reglas que protegen la plata no fallan
    cuando se rompen: DEJAN PASAR. Nadie se entera de que el precio empezó a
    salir del navegador hasta que alguien paga un peso.

  · ARREGLADO ANTES DE SUBIRLO: la app pedía los precios con la clave
    pública solo en `apikey`. Las funciones tienen una puerta ANTES del
    código —Supabase revisa el token y contesta "Missing authorization
    header" sin ejecutar nada— y esa puerta mira `Authorization`. El efecto
    habría sido invisible: `planesPremium` devuelve lista vacía cuando
    falla, y el panel sin planes no dibuja ningún botón. Un botón que no
    aparece no se parece a un error. Hay una prueba nueva que lo fija.

  · 498 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-30 x (el orden, los puntos y el premium)

  · LA APP ABRE EN EL SIMULADOR. El orden de las pestañas es El 11 ·
    Fantasy · Números · Feed, y no es decorativo: primero lo que hicimos
    nosotros, último el contenido de otros medios. Dos razones que apuntan
    al mismo lado — la app se llama Armá el 11 y abría en algo que no es
    armar el 11, y una app que se presenta como agregador de contenido
    ajeno es la que las redes de publicidad miran con lupa.
    OJO: el feed era el hábito diario. Si en octubre baja la gente que
    vuelve, este cambio es el primer sospechoso.

  · SE CALCULAN LOS PUNTOS (puntos-api.mjs). Es la pieza que convierte los
    torneos en un juego: sin esto, la gente arma su equipo, se juega la
    fecha y la tabla sigue en cero.
      · Los escribe el SERVIDOR. `puntaje` no tiene ninguna política de
        escritura: ni el teléfono ni la clave pública pueden tocarla.
      · Se guarda el DETALLE, no solo el total. "Te dieron 47" es un número
        que hay que creer; el detalle abierto se puede revisar.
      · Recalcular da lo mismo: si la API corrige una estadística el lunes,
        se vuelve a correr y la tabla se acomoda sola.
      · Usa el MISMO fantasy.mjs que la pantalla. Una sola tabla de puntos.

  · EL PREMIUM, en la base. Todavía sin cobrar nada: la columna y sus
    defensas primero.
      · Es una FECHA, no un sí/no. Un booleano se prende y no se apaga
        nunca: quedaría gente premium para siempre por una compra de un mes.
      · Y acá había una trampa que la RLS sola NO tapa: las políticas son
        por FILA, no por columna, y la de `perfil` ya deja que cada uno
        edite la suya (la necesita para el nombre de usuario). Sin más,
        cualquiera se ponía premium hasta el 2099 desde la consola del
        navegador. La defensa correcta es un permiso por COLUMNA: se le
        revoca el update de `premium_hasta` a los usuarios.
      · Tabla `pago` con el id de Mercado Pago como clave primaria: MP
        puede mandar el mismo aviso varias veces, y así el segundo choca y
        no acredita de nuevo. Sin políticas: no se lee desde ningún
        teléfono.
      · `acreditar_premium` suma sobre lo que quedaba, así el que renueva
        antes de vencer no pierde los días que le sobraban. No se la puede
        llamar desde el navegador.

  · 478 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-30 w (lista para empaquetar)

Los cuatro archivos que Play exige, y las dos páginas:

  · SERVICE WORKER (sw.js). Hace que la app conteste algo sin señal. Va
    primero a la red SIEMPRE —la caché es el paracaídas, no el avión—, solo
    guarda lo que salió bien, y el nombre de la caché lleva versión: cambiar
    ese número tira todo lo guardado en todos los teléfonos. Es la única
    salida de emergencia de un sitio estático, y por eso está escrita.

  · MANIFEST DE LA APP en la raíz, con UNA puerta de entrada: la lista de
    clubes. Los treinta manifests por club siguen igual, para el que agrega
    el sitio a la pantalla de inicio.

  · ÍCONOS PNG de 192, 512 y uno "maskable". Los hace iconos.cjs a mano y
    viven en el repo: generar un PNG necesita un navegador y en la nube no
    hay ninguno. El maskable deja libre el 20% del borde porque Android
    recorta el ícono con la forma de cada teléfono.

  · ASSETLINKS: NO se escribe hasta que sitio.json traiga el paquete y la
    huella de la firma que da Play. Uno con datos inventados no falla en
    silencio: falla en la cara del usuario cada vez que abre la app. Igual
    que el CNAME, si se saca la configuración el archivo se borra.

  · PRIVACIDAD y BORRAR-CUENTA, dos páginas públicas. Sin las dos no se
    publica, pero además son la parte del trato que le toca al que dejó su
    mail. Se generan desde construir-sitio.mjs para que el que agregue un
    dato nuevo pase por el archivo que dice qué se guarda: una política que
    vive en un HTML suelto envejece mintiendo.

  · BORRAR LA CUENTA, de verdad. Desde la app y desde la web, en el acto y
    sin escribirle a nadie. Va en dos toques porque no hay deshacer.
      · La función de la base no recibe NINGÚN parámetro: borra al que pidió
        y no hay forma de nombrar a otro.
      · Se borran mail, usuario, equipos, puntajes y membresías.
      · NO se borran las ligas que la persona creó. Si se fueran con ella,
        el día que uno se va desaparece el torneo de once que no tienen nada
        que ver. Hubo que sacarle el cascade a liga.dueno.
      · Hay una prueba que falla si aparece una función con llave maestra
        nueva sin justificar: agregar una tiene que ser una decisión.

  · El log de la corrida dice ahora si el sitio está listo para empaquetar y
    qué falta.

  · 464 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-30 v (el planteo del rival, y planteos armados)

  · AHORA SE PLANTEA AL RIVAL. Tiene su propia tanda de perillas. Ese era
    el límite de verdad: si el que se metía atrás con diez era él, no había
    forma de decirlo. Cada planteo pesa sobre su propio ataque y sobre el
    del otro; con el rival en neutro la cuenta da EXACTAMENTE lo de antes,
    y hay una prueba que lo fija.

  · PLANTEOS ARMADOS, para los dos equipos: se para atrás · normal ·
    espera y sale de contra · va a buscarlo · se juega la vida. Cada uno
    dice qué hace y ninguno es una mejora gratis: hay una prueba que falla
    si alguno genera más y concede menos que el neutro.

  · Y LAS DOS COSAS SON LA MISMA COSA. Un planteo no es otro modo de
    juego: es un atajo que escribe las mismas perillas. Elegís uno y se
    acomodan; después movés la que quieras a mano y el botón se apaga
    solo. Cuál está activo NO se guarda en ningún lado, se DEDUCE de las
    perillas — un botón encendido sobre valores que ya no son los suyos es
    un botón que miente.

  · Cuando cargás un expulsado, se OFRECE el planteo que corresponde
    (atrás si vas ganando, de contra si vas perdiendo) con un botón. No se
    aplica solo: cambiarle el planteo a alguien sin que lo pida es decidir
    por él en su propio juego.

  · El botón de simular pasó al final, después de todas las decisiones.


  · CORREGIDO UN CARTEL QUE MENTÍA. En la versión u, el panel del partido
    empezado decía "lo que NO hace: ajustar por cómo va el partido". Está
    mal dicho y hace creer que el planteo no entra en la cuenta. Entra: el
    gol esperado que se reparte por el tiempo que queda YA trae las
    perillas y las indicaciones, así que si vas ganando con uno menos y te
    parás atrás con transiciones rápidas, la cuenta lo toma. Medido: con
    diez, meterse atrás pasa de 74,5% a 79,8% de ganar.
    Lo que el modelo no hace es ADIVINAR el planteo. Lo pone el que juega,
    que es justamente la gracia del juego.

  · Y se dice cuál es el límite de verdad, que es otro: LAS PERILLAS SON
    DE UN SOLO EQUIPO. No se puede plantear al rival, así que si el que se
    mete atrás con diez es él, eso todavía no se puede expresar. Lo único
    que se le aplica es el promedio de jugar con uno menos.

  · Anotado en el motor: ese promedio (0,68 de ataque, 1,42 de goles en
    contra) se midió sobre equipos con uno menos, y el equipo promedio con
    uno menos YA se para atrás. Si además se pone bloque bajo a mano, una
    parte se cuenta dos veces. Es chico y va hacia el lado conservador,
    pero conviene saberlo antes de calibrar.

  · 439 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-30 u (cinco ligas, indicaciones y el partido empezado)

Lo más nuevo:

  · LAS CONSTANTES YA NO SON ARGENTINAS. `juego.js` tenía tres números
    fijos: media de rating 6,5 y 2,48 goles por partido entre los dos.
    Para Argentina están bien; para la Premier no, y todos los marcadores
    salían bajos. Ahora cada liga trae los suyos, calculados con sus
    propios partidos jugados. El documento del backtest ya decía que cada
    liga corre con sus promedios; el motor no lo hacía.
    Si una liga tiene menos de 40 partidos jugados NO se calibra: se usa
    el respaldo conocido en vez de un promedio de ruido con autoridad de
    dato. Y la app dice con qué está calibrada cada una.

  · CINCO LIGAS MÁS PARA SIMULAR: Portugal, Italia, Inglaterra, España y
    Francia, elegidas por el backtest — son donde el modelo de verdad le
    gana a la vara (Portugal +0,098; Argentina +0,005). Se elige la liga,
    se elige el partido y se arma el once. Sumar una liga es agregar una
    fila en ligas.json.
    Los datos van COCINADOS: por jugador, su promedio de rating y sus
    minutos, que es todo lo que mira el motor. Una liga entera pesa unas
    decenas de kilobytes en vez de megas de JSON crudo.

  · SIMULAR DESDE UN PARTIDO EMPEZADO: minuto, marcador y expulsados. Se
    simula lo que falta y se le suma lo que ya está. La animación arranca
    en ese minuto con ese marcador.
    Lo que NO hace, dicho en la pantalla: ajustar por cómo va el partido.
    El que pierde se vuelca y se expone, y eso es real — pero no está
    medido en este modelo, y acá no se agrega nada que no se haya podido
    probar.

  · TRES INDICACIONES DEL PLANTEO, cada una con su efecto publicado:
    marca por zona o personal al mejor del rival · atacar repartido o por
    el lado flojo · salida jugada o pelotazo. Ninguna es gratis.
    NO son por jugador, y el motivo está escrito en el código: el gol
    esperado sale de comparar LÍNEAS, no hay aporte individual al que
    restarle una marca. Inventarle un efecto rompería lo único que hace
    discutible a este juego.

  · SE FUE EL SERMÓN de abajo del resultado ("ese fue uno de los 6.000
    partidos simulados… la barra de arriba es la que hay que mirar"). Era
    entrar a un show de magia y recordarle al espectador que es
    ilusionismo. El dato no se pierde: la barra y el porcentaje siguen
    arriba, que es donde corresponden.

  · 415 casos de prueba. Uno de los nuevos me corrigió a mí: había
    escrito que la media de la liga movía a todos los jugadores, y no es
    cierto — al que ya jugó tres partidos completos no lo toca. El
    comentario del motor decía otra cosa y ahora dice la verdad.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-29 t (el partido dura veinte segundos)

Lo más nuevo:
  · EL PARTIDO ANIMADO DURA 20 SEGUNDOS, y el reloj avanza de a un minuto
    en vez de a tres. Se ve mejor —es un reloj de partido y no un
    contador—, pero la razón de fondo es otra: tres simulaciones seguidas
    suman sesenta segundos, que es justo el mínimo que AdSense impone
    entre dos avisos. Con eso la cadencia de publicidad sale sola, una
    cada tres, sin programar ninguna regla y sin pasarse de lo que
    permite la política de AdMob (uno cada dos acciones, como mucho).
  · Por eso el RITMO YA NO ACELERA EL RELOJ. Antes lo acortaba hasta un
    22% y eso movía la duración. El ritmo se sigue viendo, pero en cómo
    se mueven los jugadores, que es donde tiene sentido que se vea.
  · Hay una prueba que MIDE la duración y falla si baja de veinte
    segundos. Si alguien vuelve a acelerar la animación, esa cuenta se
    rompería en silencio, y en silencio es como se rompen las cosas que
    después nadie entiende.

  · EL LUGAR DEL AVISO. Ojo: NO hay publicidad en la app. Hay un lugar
    donde algún día va a haber una, con las reglas de cuándo corresponde
    ya escritas y probadas:
      · una cada 60 segundos de reloj de pared (el mínimo de AdSense);
      · la PRIMERA simulación de alguien nunca lleva aviso — ahí está
        decidiendo si la app le gusta, y eso vale más que centavo y medio;
      · el reloj vive en el teléfono, no en memoria, así que recargar la
        página no lo pone en cero;
      · si el navegador no deja guardar nada, se falla hacia NO mostrar.
    Mientras `sitio.json` no tenga una sección "publicidad", esto resuelve
    en el acto y la app sigue sin cargar un solo script de terceros. La
    prueba que lo cuida sigue en verde.
  · El aviso va antes de simular y antes de revelar el once del DT. El
    resumen con el resultado se ve GRATIS, siempre.

  · 368 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-27 s (compartir y torneos de amigos)

Lo más nuevo:
  · COMPARTIR MI 11 por WhatsApp. Sale como TEXTO: nada de imágenes con
    escudos ajenos y nada de scripts de botones de compartir, que son
    justamente lo que esta app no carga. Usa el menú del sistema en el
    teléfono, el link de wa.me en la compu, y si el navegador bloquea la
    ventana, copia al portapapeles. Nunca se queda sin hacer nada: un
    botón que no hace nada se lee como roto.
  · El mensaje cuenta lo que DECIDISTE —formación, capitán, cuánto
    gastaste—, no la lista de los quince. Un mensaje que hay que
    scrollear no se reenvía, y filtrar el equipo entero antes del cierre
    sería regalarle la tarea al que lo recibe.
  · El botón aparece recién con el equipo armado. Compartir un equipo a
    medio hacer manda a un amigo a mirar una cancha vacía.
  · TORNEOS DE AMIGOS, con pantalla: crear uno, entrar con el código de
    un amigo, ver la tabla y invitar por WhatsApp. El motor y las reglas
    ya vivían en la base desde la versión d; esto es la pantalla.
  · La invitación viaja en el link (#liga=CODIGO) y SOBREVIVE el viaje al
    correo: si el que la abre nunca entró, el código se guarda, se le
    manda el mail, y al volver entra solo al torneo. Sin eso, el que
    llega por WhatsApp tendría que acordarse del código después de
    revisar el correo, y no se acuerda.
  · Y el código se borra de la dirección apenas se lee, como los tokens:
    un link reenviado no puede arrastrar la invitación de otro.

  · LA FECHA ES LA FECHA ENTERA, menos los postergados. Entra toda la
    ronda —viernes a lunes—; los TBD también, que todavía no tienen hora
    pero se juegan. Los postergados no: sus jugadores tienen el cero
    asegurado y ofrecer un cero asegurado es una trampa para el que no
    miró el calendario.

  · 358 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-27 r (buscar, ordenar y filtrar)

Lo más nuevo:
  · FILTRO POR CLUB: un desplegable con los clubes de la fecha. Treinta no
    entran en una fila de botones. Y cuando hay un club elegido, debajo
    dice cuántos de ese club ya tenés y cuántos podés sumar — el máximo son
    tres y hasta ahora eso se descubría recién al intentar guardar, que es
    descubrirlo tarde.
  · La lista se puede ORDENAR: Más caros · Más baratos · Más puntos ·
    Mejor rinde · Por nombre. Con mil jugadores, un solo orden es un solo
    juego: el que busca una figura entra por arriba, y el que ya gastó el
    presupuesto y le faltan tres fichas entra por abajo — ahí es donde de
    verdad se decide una fecha.
  · "Mejor rinde" es puntos por partido dividido el precio: el que arma
    bien no compra al mejor, compra al que rinde más de lo que sale.
  · El tope de 150 se aplica DESPUÉS de ordenar, así que dar vuelta el
    orden trae jugadores que antes ni aparecían. El cartel del final lo
    dice: "los 150 más baratos" y "los 150 más caros" son listas
    distintas.
  · La búsqueda ignora las tildes. Nadie escribe "Álvarez" con el acento
    en el teclado del teléfono, y quedarse sin resultados por una tilde se
    siente como que el jugador no está.
  · 346 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-27 q (los precios, en serio)

Los tres problemas que se vieron en la primera fecha real, y qué se hizo:

  · FALTABAN JUGADORES. No faltaban: la lista mostraba los 60 más caros de
    962 y no lo decía. Un límite callado se lee como un error. Ahora entran
    150 y, cuando quedan afuera, la lista dice cuántos son y que se llega
    al resto buscando por nombre o club.

  · LOS MÁS CAROS NO ERAN LOS MEJORES. El precio salía del promedio crudo
    de puntos por partido, así que el más caro de la liga terminaba siendo
    alguien que jugó UN partido y lo hizo bien. Y como ese promedio inflado
    fijaba la escala, todos los demás quedaban comparados contra una
    casualidad. Ahora a cada jugador se le suman cuatro fechas imaginarias
    jugadas como el jugador mediano de su puesto: con un partido el
    promedio es casi todo prestado, con quince ya es suyo. Nadie es el
    mejor de la liga por una tarde.

  · TODO SALÍA BARATO. El precio se medía como distancia al mejor; ahora
    sale del LUGAR dentro de su puesto. Dos cosas cambian: ya no depende de
    un solo jugador raro, y cada línea tiene su caro y su barato — antes
    los arqueros salían todos baratos porque con el acumulado no se pueden
    reconstruir las vallas invictas, que es un problema nuestro y no del
    arquero. Elegir arquero vuelve a ser una decisión.

    El jugador mediano de cada puesto vale SIEMPRE 5, cualquiera sea la
    fecha, y quince medianos cuestan 75: el presupuesto entero. Ahí está el
    juego: para tener una figura hay que encontrar una ganga.

  · En la lista, al lado del promedio ahora dice en cuántas fechas lo hizo.
    Un 8 en una fecha y un 8 en quince no son la misma cosa.

  · El log dibuja cómo quedaron repartidos los precios y quiénes son los
    caros de cada puesto. Si la fórmula mide mal, se ve ahí y no el domingo.

  · 336 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-27 p (la hora del cierre, en serio)

Lo más nuevo:
  · ARREGLADO, y era grave: la primera fecha real se publicó anunciando
    que cerraba a las 7 de la mañana. Ningún partido argentino empieza a
    esa hora. Era el horario de relleno de un partido sin hora confirmada
    (TBD) o postergado (PST), que la API devuelve igual. Ahora el cierre
    sale SOLO de los partidos con horario confirmado; los otros siguen
    contando como partidos de la fecha, pero no deciden cuándo cierra.
  · El log muestra los primeros cuatro partidos con su estado, así el
    cierre se puede auditar de un vistazo, sin entrar a la API. Y avisa
    si aun así el cierre cae antes de las 11 de la mañana.
  · Todas las horas se imprimen en hora de Argentina. Antes el resumen
    salía en UTC —la corrida vive en un servidor de Londres— y decía
    "cierra 10:00" para un partido de las 7. Un renglón que miente sobre
    la hora del cierre es peor que no tener renglón.
  · La conversión se hace restando tres horas a mano, no con la tabla de
    zonas horarias del sistema: hay Node que viene con la tabla recortada
    y ahí las 22:00 salían impresas como "10:00", sin AM/PM, así que ni
    se notaba. Hay una prueba que falla si Argentina vuelve al horario
    de verano.
  · 329 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-27 o (la fecha se publica sola)

Lo más nuevo:
  · fantasy-api.mjs: saca del calendario cuál es la próxima fecha y a qué
    hora arranca, calcula los precios con la fórmula publicada y escribe
    la fila de `fechas` en la base, que es donde vive el cierre de verdad.
  · ARREGLADO: la pestaña Fantasy se veía aunque no hubiera fecha. El
    atributo `hidden` esconde con display:none y la regla de la barra le
    ponía display:grid encima.
  · Una fecha sin jugadores se trata como si no existiera, y los archivos
    de una fecha vieja se borran en vez de quedar dando vueltas.

  OJO: para que la fecha se escriba en la base hay que agregar UNA LÍNEA
  al archivo .github/workflows/publicar.yml, y ese archivo NO se sube
  arrastrando. Editalo directo en GitHub con el lápiz. Está explicado en
  el mensaje que acompaña a esta versión.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-27 n (formación y cancha)

Lo más nuevo:
  · Se elige la FORMACIÓN: 4-3-3, 4-4-2, 3-5-2, 3-4-3, 4-5-1, 5-3-2 y
    5-4-1. Ella dice cuántos van en cada línea, así que ya no se puede
    guardar un equipo que no sea una formación real.
  · El equipo se ve en una CANCHA, no en una lista. Los lugares vacíos
    son botones que filtran la lista de abajo por ese puesto.
  · Cambiar de formación no borra el equipo: los que sobran vuelven a la
    lista y se avisa quiénes.
  · 315 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-27 m (el fantasy, con pantalla)

Lo más nuevo:
  · La pantalla de armar el equipo: quince fichas, el presupuesto bajando
    en cada toque, capitán y vice, y el reloj del cierre a la vista.
  · La MISMA revisión corre en la pantalla y en el servidor: una sola
    tabla de puntos, una sola idea de qué equipo es legal.
  · La pestaña aparece SOLO si hay una fecha publicada. Lo que sigue es
    publicar la primera con precios de una fecha real.
  · 307 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-27 l (el motor del fantasy)

Lo más nuevo:
  · fantasy.mjs: el reglamento hecho código. La tabla de puntos, las
    suplencias automáticas, el capitán y el vice, el más valioso del
    partido y la fórmula de los precios. No toca la red ni la base.
  · 72 casos que prueban cada regla con una situación concreta. Si
    mañana discutimos cuánto vale el gol de un defensor, se cambia un
    número en una tabla y las pruebas dicen qué se movió.
  · TODAVÍA NO HAY PANTALLA. Esto es el motor: lo que sigue es la fecha
    con sus precios y la pantalla para armar el equipo.
  · 294 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
Armá el 11 — versión 2026-08-26 k (el nombre y las tarjetas)

Lo más nuevo:
  · La app se llama Armá el 11, no TSTE. Cambió el título de cada página,
    el nombre que muestra WhatsApp al compartir y el de la pantalla de
    inicio del teléfono. Hay una prueba que falla si vuelve a aparecer
    un TSTE a la vista.
  · La pestaña del juego pasa a llamarse "El 11": si la app entera se
    llama Armá el 11, no puede haber una pestaña con el mismo nombre.
  · En la portada, la ciudad va DEBAJO del nombre del club. Estaba al
    costado y con los nombres largos se cortaba: "Independiente
    Rivadavia" se comía a "Mendoza". Una prueba mide que las treinta
    entren enteras.

── lo de antes ───────────────────────────────────────────────────────────
TSTE — versión 2026-08-26 j (armael11.com)

OJO CON EL ORDEN: este zip trae el dominio ya configurado. NO lo subas
hasta que https://armael11.com abra la app. Si lo subís antes, GitHub
empieza a mandar a todo el mundo a una dirección que no contesta y el
sitio queda inalcanzable por las dos puntas. Los pasos, en DOMINIO.md.

── lo de antes ───────────────────────────────────────────────────────────
TSTE — versión 2026-08-26 i (listo para el dominio propio)

Lo más nuevo:
  · sitio.json tiene un campo "dominio". Cuando se pone, la corrida escribe
    el CNAME que GitHub necesita y TODAS las direcciones absolutas del sitio
    salen de ahí. Cuando se saca, el CNAME se borra: dejarlo sería peor que
    no haberlo puesto.
  · DOMINIO.md: los cinco lugares que hay que tocar, en orden.

── lo de antes ───────────────────────────────────────────────────────────
TSTE — versión 2026-08-26 h (mensajes honestos del mail)

Lo más nuevo:
  · Los dos frenos del envío de mail dicen cosas distintas: la espera corta
    avisa cuántos segundos faltan; el tope por hora avisa que insistir no
    sirve. Cuando decían lo mismo, se perdía una hora probando.

── lo de antes ───────────────────────────────────────────────────────────
TSTE — versión 2026-08-26 g (todo en la raíz, sin carpetas)

Lo más nuevo:
  · esquema.sql ya NO está en una carpeta: vive en la raíz como todo lo
    demás. La carpeta supabase/ no se subía al arrastrar a GitHub, y una
    corrida entera falló por eso. Si la subida pierde carpetas, la
    respuesta no es aprender a arrastrar carpetas: es no tener carpetas.
  · Si igual falta un archivo, la corrida lo dice por su nombre en el
    primer renglón del log.

── lo de antes ───────────────────────────────────────────────────────────
TSTE — versión 2026-08-26 f (aviso de archivos faltantes)

Lo más nuevo:
  · publicar.mjs revisa PRIMERO que estén todos los archivos y, si falta
    alguno, lo dice por su nombre en el primer renglón del log en vez de
    fallar más adelante con un error críptico. Al arrastrar a GitHub hay
    que subir también las CARPETAS: supabase/ es una de ellas.

── lo de antes ───────────────────────────────────────────────────────────
TSTE — versión 2026-08-26 e (backend enchufado)

Lo más nuevo:
  · sitio.json ya tiene las dos claves públicas del backend, así que las
    cuentas se copian al sitio y el botón aparece.
  · 214 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
TSTE — versión 2026-08-26 d (las cuentas andan)

Lo más nuevo:
  · cuentas.js: entrar por mail (sin contraseña), elegir usuario, guardar
    el equipo y las ligas de amigos. Habla con Supabase con fetch y nada
    más: la app sigue sin cargar un solo script de terceros.
  · Sin backend configurado en sitio.json, no se copia ni se carga: la app
    es exactamente la de antes y no hay botón que prometa algo que no anda.
  · 213 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────
TSTE — versión 2026-08-26 c (jerarquía del feed + esquema del backend)

Lo más nuevo:
  · supabase/esquema.sql y BACKEND.md: las tablas del fantasy y las ligas,
    con la regla escrita en la base — el teléfono escribe el equipo y solo
    antes del cierre; los puntos los escribe el servidor.
  · El esquema no usa comentarios con dos guiones: hay editores que los
    convierten en una raya larga al abrir el archivo y el SQL deja de
    correr. Abrilo arrastrandolo a una pestaña de Chrome, no con Word.
  · 158 casos de prueba.


Lo nuevo de esta subida:
  · El feed dejó de ser treinta renglones iguales. Ahora hay tres tamaños:
    la primera historia con imagen grande, las cuatro siguientes con
    miniatura al costado, y el resto en renglones agrupados por día.
  · Las miniaturas se enlazan al medio, nunca se alojan acá, y van con
    carga diferida. Se apagan desde sitio.json ("miniaturas").
  · 134 casos de prueba.

── lo de antes ───────────────────────────────────────────────────────────


Este archivo existe para una sola cosa: que se pueda mirar el repo y saber,
sin abrir nada más, si la última subida entró o quedó a mitad de camino.

Qué trae esta versión:
  · El filete de los clubes a bastones pasa de 15px a 44px por segmento.
    Con 15px son unos 56 segmentos a lo alto de una pantalla y se lee como
    un rayado, no como una camiseta.
  · Huracán, Rosario Central y Platense quedan como están: los confirmó
    Fausto el 2026-08-26. Tienen la nota en clubes.json y una prueba que
    falla si alguien los cambia.
  · 112 casos de prueba.

Cómo saber si esta subida entró: en la lista de archivos de GitHub, la fecha
de VERSION.txt tiene que decir "now" o "X minutes ago". Si dice otra cosa,
los archivos no se reemplazaron.
